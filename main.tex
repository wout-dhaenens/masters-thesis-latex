\documentclass[11pt,a4paper]{report}
\usepackage[utf8]{inputenc}
\usepackage{a4wide}
\usepackage[english]{babel}
\usepackage{graphicx}
\usepackage{siunitx}
\usepackage[margin=1in]{geometry}
\usepackage{lastpage}		
\newcommand{\npar}{\vspace{0.5cm} \par \noindent} 
\usepackage[utf8]{inputenc}
\usepackage{newunicodechar}
\usepackage{float}
\usepackage{graphicx}
\usepackage{array}
%\usepackage{subcaption}
\usepackage{hyperref}
\usepackage{amsmath}
\usepackage{cleveref}
\usepackage{amsmath}
\usepackage{circuitikz}
\usepackage{textcomp}
\usepackage{multicol}
\usepackage{gensymb}
\usepackage{rotating}
\usepackage{subcaption}
\usepackage{fixltx2e}
\usepackage{notoccite}

\usepackage[final]{pdfpages}
\usepackage{enumerate}
\usepackage{chemformula}
\usepackage{listings}
%\usepackage{subfig}
\usepackage{xcolor}
\usepackage{pdfpages}
\usepackage{nomencl}
\makenomenclature

\usepackage{etoolbox}
\renewcommand\nomgroup[1]{%
  \item[\bfseries
  \ifstrequal{#1}{A}{Abbreviations}{%
  \ifstrequal{#1}{L}{Letters}{%
  \ifstrequal{#1}{G}{Greek Symbols}{}}}%
]}

\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
\usepackage{ragged2e}
\usepackage{pdflscape}
\usepackage{eurosym}
\usepackage{amssymb}


%%%%%%%%%%%%%%%%%%%%%%%%%% For python code
% Default fixed font does not support bold face
\DeclareFixedFont{\ttb}{T1}{txtt}{bx}{n}{10} % for bold
\DeclareFixedFont{\ttm}{T1}{txtt}{m}{n}{10}  % for normal

% Custom colors
\usepackage{color}
\definecolor{deepblue}{rgb}{0,0,0.5}
\definecolor{deepred}{rgb}{0.6,0,0}
\definecolor{deepgreen}{rgb}{0,0.5,0}

\usepackage{listings}

% Python style for highlighting
\newcommand\pythonstyle{\lstset{
language=Python,
basicstyle=\ttm,
otherkeywords={self},             % Add keywords here
keywordstyle=\ttb\color{deepblue},
emph={MyClass,__init__},          % Custom highlighting
emphstyle=\ttb\color{deepred},    % Custom highlighting style
stringstyle=\color{deepgreen},
frame=tb,                         % Any extra options here
showstringspaces=false,
breaklines = true,
}}


% Python environment
\lstnewenvironment{python}[1][]
{
\pythonstyle
\lstset{#1}
}
{}

% Python for external files
\newcommand\pythonexternal[2][]{{
\pythonstyle
\lstinputlisting[#1]{#2}}}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\begin{document}

\includepdf[pages=-]{TitlePage.pdf}
\newpage
\vspace*{\fill}
\noindent
''The author gives permission to make this master dissertation available for consultation and to copy parts of this master dissertation for personal use. In all cases of other use, the copyright terms have to be respected, in particular with regard to the obligation to state explicitly the source when quoting results from this master dissertation.''

\addvspace{2cm}

\noindent Ghent, June [year]\\
\noindent [Name]

\newpage
\addcontentsline{toc}{chapter}{Preface}
\section*{Preface}
\npar
\noindent
[Preface]

%  Overzichtsbladzijde met samenvatting

\newpage
\addcontentsline{toc}{chapter}{Abstract}
{
\setlength{\baselineskip}{12pt}
\setlength{\parindent}{0pt}
\setlength{\parskip}{6pt}



\renewcommand{\baselinestretch}{1.1}
\noindent \fontseries{bx}
\fontsize{14pt}{21pt}
\selectfont
\vspace{-2.5cm}
\begin{center}
Title\\
\renewcommand{\baselinestretch}{1.3}
\fontseries{m}
\fontsize{12pt}{14pt}
\selectfont
\vspace{0.5cm}
[Name]\\
\end{center}

\renewcommand{\baselinestretch}{1.3}
\fontseries{m}
\fontsize{12pt}{12pt}
\selectfont

Supervisor: \\
Counsellors: \\

Master's dissertation submitted in order to obtain the academic degree of\\
\textsc{Master of Science in Electromechanical Engineering}\\

Departement of Electromechanical, Systems and Metal Engineering\\
Chair: \\
Faculty of Engineering and Architecture\\
Ghent University\\
Academic year 2025--2026\\

\textbf{Summary}



\textbf{Keywords:} 
}

\newpage % strikt noodzakelijk om een header op deze blz. te vermijden
\addcontentsline{toc}{chapter}{Extended Abstract}
\includepdf[pages=-]{template_6_page_paper_Latex.pdf}
\addcontentsline{toc}{chapter}{Table of Contents}
\tableofcontents
\newpage
\addcontentsline{toc}{chapter}{List of Figures}
\listoffigures

\newpage
\addcontentsline{toc}{chapter}{List of Tables}
\listoftables

\newpage
\addcontentsline{toc}{chapter}{Nomenclature}
\nomenclature[A]{\textbf{DER}}{\textbf{Distributed Energy Resource}}
\nomenclature[A]{\textbf{ANF}}{\textbf{Annuity Factor}}
\nomenclature[A]{\textbf{BESS}}{\textbf{Battery Energy Storage System}}
\nomenclature[A]{\textbf{COP}}{\textbf{Coefficient of Performance} (HP efficiency metric)}
\nomenclature[A]{\textbf{DHW}}{\textbf{Domestic Hot Water}}
\nomenclature[A]{\textbf{HPS}}{\textbf{Heat Pump System}}
\nomenclature[A]{\textbf{HP}}{\textbf{Heat Pump}}
\nomenclature[A]{\textbf{MILP}}{\textbf{Mixed-Integer Linear Programming}}
\nomenclature[A]{\textbf{MINLP}}{\textbf{Mixed-Integer Non-Linear Programming}}
\nomenclature[A]{\textbf{OP}}{\textbf{Operation Point} (for linearization reference)}
\nomenclature[A]{\textbf{PV}}{\textbf{Photovoltaic}}
\nomenclature[A]{\textbf{PWL}}{\textbf{Piecewise Linear} (approximation)}
\nomenclature[A]{\textbf{SH}}{\textbf{Space Heating}}
\nomenclature[A]{\textbf{TES}}{\textbf{Thermal Energy Storage}}
\nomenclature[A]{\textbf{TRY}}{\textbf{Test Reference Year} (used for clustered data)}


\nomenclature[L]{$\boldsymbol{A_{\text{ref.}}}$}{Surface area of one PV module (reference)}
\nomenclature[L]{$\boldsymbol{c_{w}}$}{Specific heat capacity of water [J/(kg K)]}
\nomenclature[L]{$\boldsymbol{C}$}{Electricity Cost [\euro]}
\nomenclature[L]{$\boldsymbol{CC}$}{Cooling Capacity during defrost operation [W]}
\nomenclature[L]{$\boldsymbol{D}$}{Comfort Index (Quantifies thermal discomfort) [\degree C]}
\nomenclature[L]{$\boldsymbol{d}$}{TES diameter}
\nomenclature[L]{$\boldsymbol{DT}$}{Acceptable Water Temperature Differential [\degree C]}
\nomenclature[L]{$\boldsymbol{FD}$}{Fluid Density [g/L]}
\nomenclature[L]{$\boldsymbol{f}$}{Objective function (Total minimized cost/discomfort)}
\nomenclature[L]{$\boldsymbol{G_{\text{POA}}}$}{Solar irradiance in the Plane of Array [W/m$^2$]}
\nomenclature[L]{$\boldsymbol{h}$}{Height of TES}
\nomenclature[L]{$\boldsymbol{MDD}$}{Max Defrost Duration [s]}
\nomenclature[L]{$\boldsymbol{P_{el,HP}}$}{Electric power consumed by HP [W]}
\nomenclature[L]{$\boldsymbol{P_{0}}$}{Rated heating power of EWH [W]}
\nomenclature[L]{$\boldsymbol{P_{\text{DC}}}$}{DC power output of PV system [W]}
\nomenclature[L]{$\boldsymbol{P_{\text{STC}}}$}{Nominal DC power for one PV module [W]}
\nomenclature[L]{$\boldsymbol{\dot{Q}_{HP}}$}{Heat output of HP [W]}
\nomenclature[L]{$\boldsymbol{Q_{\text{N}}}$}{Nominal capacity of TES}
\nomenclature[L]{$\boldsymbol{Q_{t}}$}{Stored energy in TES at time $t$}
\nomenclature[L]{$\boldsymbol{R}$}{Water heater thermal resistance [K$\cdot$day/kWh]}
\nomenclature[L]{$\boldsymbol{SH}$}{Specific Heat of fluid [J/(g$\cdot$\degree C)]}
\nomenclature[L]{$\boldsymbol{T_{\text{amb}}}$}{Ambient outdoor air temperature (OAT) [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{\text{biv}}}$}{Bivalence temperature (Reference design point) [K]}
\nomenclature[L]{$\boldsymbol{T_{\text{cell}}}$}{PV cell temperature [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{cw}}$}{Temperature of cold mixing water [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{\text{exp}}}$}{Most desirable tap water temperature [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{\text{High,mean}}}$}{Mean temp.\ of delivered hot water [K]}
\nomenclature[L]{$\boldsymbol{T_{in}}$}{Water temperature in EWH/TES tank [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{\text{inlet}}}$}{Temperature of cold water flowing into the tank [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{max}}$}{Maximum temperature EWH tank can withstand [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{ref}}$}{Reference temperature [\degree C]}
\nomenclature[L]{$\boldsymbol{T_{tap}}$}{Current tap water temperature [\degree C]}
\nomenclature[L]{$\boldsymbol{u(t)}$}{On/off status variable of EWH (binary) [-]}
\nomenclature[L]{$\boldsymbol{v_{\text{wind}}}$}{Wind velocity [m/s]}
\nomenclature[L]{$\boldsymbol{x(t)}$}{Switch-on signal of EWH (binary) [-]}
\nomenclature[L]{$\boldsymbol{y(t)}$}{Switch-off signal of EWH (binary) [-]}
\nomenclature[L]{$\boldsymbol{y_{HP,TES}}$}{Binary on/off status of HP for a given TES [-]}


\nomenclature[G]{$\boldsymbol{\alpha}$}{User preference factor (Weighting factor for cost) [-]}
\nomenclature[G]{$\boldsymbol{\beta}$}{Weighting factor for comfort $(\alpha+\beta=1)$ (EWH) [-]}
\nomenclature[G]{$\boldsymbol{\gamma}$}{Coefficient for TES lateral surface loss}
\nomenclature[G]{$\boldsymbol{\gamma_{\text{pdc}}}$}{Temperature coefficient of power (PV) [\%/\degree C]}
\nomenclature[G]{$\boldsymbol{\delta(t)}$}{Auxiliary variable for PWL approximation (binary) [-]}
\nomenclature[G]{$\boldsymbol{\Delta t}$}{Time step size for heat generation [s]}
\nomenclature[G]{$\boldsymbol{\Delta \tau}$}{Longer discretization period for TES/Supply [s]}
\nomenclature[G]{$\boldsymbol{\eta}$}{Efficiency (General) [-]}
\nomenclature[G]{$\boldsymbol{\eta_{\text{HP}}}$}{HP efficiency factor [-]}
\nomenclature[G]{$\boldsymbol{\kappa}$}{Electricity price [\euro/kWh]}
\nomenclature[G]{$\boldsymbol{\lambda}$}{Thermal conductivity of water [W/(m K)]}
\nomenclature[G]{$\boldsymbol{\mu}$}{Scale factor (EWH objective function) [\degree C/\euro]}
\nomenclature[G]{$\boldsymbol{\rho_{w}}$}{Water density [kg/m$^3$]}
\nomenclature[G]{$\boldsymbol{\theta}$}{Time step ratio $(\Delta \tau / \Delta t)$ [-]}

\printnomenclature
\chapter{Introduction}
\label{intro}




\section{The imperative for decarbonization in the building sector}
The global energy transition is increasingly focused on the urgent need to reduce greenhouse gas emissions and mitigate climate change.
A critical battleground in this transition is the building sector, which remains a significant contributor to energy use and carbon emissions.
For instance, in Belgium alone, the building sector\footnote{In this context, the building sector definition follows the European climate reporting categories, which encompass direct emissions from fossil fuel combustion in residential and tertiary (commercial/institutional) buildings, as well as energy-related emissions from the agriculture and fishery sectors.} is responsible for approximately 20\% of total carbon dioxide ($CO_2$) emissions in 2022 \cite{EU_Climate_Action_Belgium_2023}.
To meet ambitious climate goals, such as those outlined by the European Union, there is a pressing need to decarbonize the heat supply for buildings.
Under the \textit{European Climate Law}, the EU is legally committed to a progressive decarbonization pathway: a net greenhouse gas (GHG) emissions reduction of at least 55\% by 2030 (compared to 1990 levels), an intermediate target of 90\% by 2040, and the ultimate goal of climate neutrality by 2050 \cite{EU_Climate_Law_2021, EU_2040_Target_2026}.
For the building sector specifically, the revised \textit{Energy Performance of Buildings Directive} (EPBD) mandates that all new buildings must be Zero-Emission Buildings (ZEB) by 2030, with a complete phase-out of fossil-fuel-based heating systems required by 2040 \cite{EPBD_2024}.
The significance of the building sector in this context stems largely from the high carbon intensity of thermal energy demands.
Space heating (SH) and domestic hot water heating (DHW) account for approximately 77,6\% of the total energy consumption in residential buildings in Belgium in 2023 \cite{Eurostat_Energy_Consumption_2023}.
Because much of this demand is currently met through the direct combustion of fossil fuels, such as natural gas and heating oil, the thermal energy share is the primary driver of the sector's operational $CO_2$ emissions.
This transition relies heavily on shifting away from fossil-fuel-based systems, such as gas boilers, toward electrification using renewable energy sources (RES) \cite{EnergyVille_PATHS2050_Residential_2025} \cite{Esen2006}.

\section{The role of heat pumps and challenges in refurbishment}
Heat pump systems (HPSs) have emerged as a key technology for electrifying space heating (SH) and domestic hot water (DHW) production\cite{Esen2006}.
Their ability to generate thermal energy efficiently from electricity makes them a cornerstone of sustainable building energy systems.
However, the widespread adoption of heat pumps faces significant economic barriers, particularly in the refurbishment of existing buildings.
In these scenarios, heat pumps often compete with gas boilers, which traditionally have lower investment costs.
Furthermore, older buildings often utilize radiator heating systems requiring high flow temperatures (up to $70^{\circ}C$), unlike modern underfloor heating.
Consequently, heat pumps for elevated temperatures are essential for these applications to ensure comfort without extensive renovations.
To increase market penetration and motivate homeowners to switch to low-carbon solutions, HPSs must become more economically competitive. The Flemish government and the heat pump sector have recently established a 
collaborative framework to address these economic barriers and accelerate 
the deployment of HPSs \cite{VEKA2026}.
To shift the market from a 10\% adoption rate toward a 70\% target for 
renovations, the strategy focuses on three primary economic levers.
First, capital barriers are mitigated through substantial direct subsidies, 
where premiums can reach up to €8,000 per installation.
Furthermore, the provision of interest-free or low-interest renovation 
loans allows for the pre-financing of the high upfront costs, aligning 
the investment with long-term energy savings.
Second, a strategic energy tax shift is being implemented to reduce the 
price of electricity relative to fossil fuels, ensuring that HPSs remain 
the most cost-effective solution for daily operation.
Finally, the industry has committed to a "Heat Pump Charter" to enhance 
installation quality and provide standardized technical guidance, ensuring 
that systems are optimized for the specific flow requirements of 
refurbished buildings \cite{VEKA2026}.

\section{The Transition to Renewable Heat Supply: Challenges and Solutions}

The urgent need to reduce dependence on fossil fuels has accelerated the integration of Renewable Energy Sources (RES), such as Photovoltaic (PV) systems, into the built environment.
This shift is a cornerstone of sustainable development, yet the transition toward a fully renewable heat supply introduces a fundamental challenge: the intermittency of sources like solar and wind.
Unlike fossil fuels, where energy supply is constant and dispatchable, the generation of RES fluctuates based on weather conditions.
This stochastic nature poses a direct risk to grid stability and the security of energy supply.

\subsection{Flexibility through Electrification and Storage}
To mitigate these fluctuations and ensure reliability, the electrification of heat demand must be supported by intelligent control and storage technologies.
The following components are essential to framing this transition:

\begin{itemize}
    \item \textbf{Demand Side Management (DSM):} 
        This strategy steers electrical loads to match the availability of renewable generation, optimizing the timing of consumption.
            \item \textbf{Thermal Energy Storage (TES):} 
                By coupling TES units with heat pumps, heat generation can be decoupled from immediate demand. 
                    This provides the necessary flexibility to shift electrical loads to periods when renewable electricity is abundant or cheaper.
                        \item \textbf{Electrical Storage (Batteries):} 
                            Integrating battery systems alongside PV generation further stabilizes the grid by maximizing self-consumption and reducing peak loads on the distribution network.
                            \end{itemize}
                            
                            
                            
                            While the synergy between PV systems, heat pumps, and storage technologies offers a robust path toward a fossil-free future, it introduces significant layers of control complexity.
                            The primary challenge lies in balancing the maximization of energy savings and grid support with the non-negotiable requirement of maintaining user comfort, such as consistent domestic hot water temperatures and a stable indoor climate.
                            
                            
                    



\section{The design gap: From static rules to dynamic optimization}
Despite the clear benefits of hybrid energy systems, current design practices remain a bottleneck.
European normative standards for dimensioning HPSs (such as EN 15450 \cite{EN15450_2007}) typically rely on static rules and single operating points.
These procedures often determine the size of individual components sequentially, neglecting the complex interdependencies between the heat pump, storage sizing, and control strategies.
This static approach can lead to sub-optimal designs.
Furthermore, the optimal size of a system is heavily dependent on its operation; a system designed without considering its control strategy may be oversized and economically inefficient \cite{Dongellini2017}.
Therefore, to achieve a truly optimal and sustainable design, dimensioning and operation must be optimized simultaneously.  By optimizing
the system, we can reduce investment costs significantly about 45\% in comparison to normative standards,
while maintaining security of supply and not increasing electrical energy consumption\cite{Krutzfeldt2021}.



\section{Thesis Objective}
This thesis aims to bridge this gap by developing a comprehensive design tool in Python using MILP.
The tool will determine the optimal economic sizing of a hybrid energy system comprising a High-Temperature Heat Pump (HTHP), Photovoltaics (PV), Battery storage, and Thermal Energy Storage (TES).
By incorporating detailed modeling of system dynamics and component interdependencies, this work seeks to provide a solution that is not only economically viable but also environmentally sustainable, accelerating the decarbonization of the residential heating sector.

\subsection{Case Study Application: Transitioning M-accent from Gas to Hybrid Electrification}

The practical necessity for advanced optimization is exemplified by the M-accent case study, which currently relies on two 80~kW gas boilers for its primary heat supply. Decarbonizing such a system requires a transition to a hybrid energy architecture. To replace the existing 160~kW fossil-fuel capacity, this thesis proposes a system integrating a High-Temperature Heat Pump (HTHP), Photovoltaics (PV), Battery Energy Storage Systems (BESS), and Thermal Energy Storage (TES).

The integration of these technologies presents a complex sizing problem that exceeds the capabilities of static design standards like EN 15450 \cite{EN15450_2007}. Specifically, the design tool developed in this work addresses the following technical challenges identified in literature:

\begin{itemize}
    \item \textbf{HTHP Performance:} Unlike traditional boilers, the capacity and Coefficient of Performance (COP) of HTHPs are highly dependent on source and sink temperatures. Following the methodology of Krützfeldt et al. \cite{Krutzfeldt2021}, the tool utilizes MILP to account for these non-linearities through piecewise linear approximations or regressive modeling based on manufacturer data, such as the Daikin EWYE-CZ.
    
    \item \textbf{Storage Synergies:} The tool optimizes the trade-off between electrochemical (Battery) and thermal (TES) storage. While batteries facilitate PV self-consumption, TES allows for the decoupling of heat production from demand, which is crucial for managing the peak loads previously handled by the 80~kW boilers \cite{Steen2015, Krien2020oemofsolph}.
    
    \item \textbf{Economic Dispatch:} By utilizing MILP, the tool simultaneously determines the optimal investment (sizing) and the hourly operational strategy (dispatch) to minimize the Total Cost of Ownership (TCO), accounting for fluctuating electricity prices and Belgian climate targets \cite{EU_Climate_Action_Belgium_2023}.
\end{itemize}

Ultimately, this tool serves as a bridge between high-level decarbonization goals and the engineering reality of the M-accent project, providing a robust mathematical framework to ensure that the replacement of gas-fired assets results in both economic and environmental gains.

\chapter{Literature review}
\label{literature review}



\section{Modeling methods of complex energy systems}






\subsection{Non-Linear Programming}
These methods are used when the system's physical laws are mathematically non-linear and an exact representation is preferred over the linearization required by MILP.

\begin{itemize}
    \item \textbf{Mixed-Integer Non-Linear Programming (MINLP)}: The direct alternative to MILP. It allows for \textbf{non-linear expressions} (like squares or products of variables) while retaining binary variables to model discrete decisions (e.g., component sizing or on/off status). 
    \item \textbf{Non-Linear Programming (NLP)}: Similar to MINLP but excludes integer variables. It is faster than MINLP but cannot be used for sizing problems. Used for \textbf{operational optimization} after component sizing is fixed.
    \item \textbf{Dynamic Programming (DP)}: Breaks down a complex problem into a sequence of simpler subproblems. It is often used for energy storage dispatch problems (Battery, TES) to find the global optimum over a large time horizon.
\end{itemize}

\subsection{Heuristic and Meta-heuristic Algorithms}
These methods use iterative search techniques and do \textbf{not guarantee a globally optimal solution}, but can handle highly complex, non-linear, and non-convex problems that MILP and MINLP struggle with.

\begin{itemize}
    \item \textbf{Genetic Algorithms (GA)}: Inspired by natural selection, it evolves a population of solutions (designs) based on a fitness function (cost/emissions). Often used for simultaneous \textbf{sizing and scheduling} of hybrid renewable systems (PV-BESS).

    \textit{Simple analogy:} A GA works like a breeding programme for solutions. It starts with a random population of candidate designs (e.g.\ different combinations of PV capacity, battery size, and dispatch schedule). In each generation, the fittest individuals i.e. those with the lowest cost or emissions, are selected to ``reproduce'': their parameters are combined (\textit{crossover}) and slightly perturbed (\textit{mutation}) to produce offspring. Over many generations, the population converges toward near-optimal solutions, much like how selective breeding improves a trait in animals over successive generations.

    \textit{Example:} Mahesh and Sandhu~\cite{mahesh2017ga} applied a GA to optimally size a grid-connected PV--wind--battery hybrid system. Each individual in the population encodes the number of PV modules, wind turbines, and battery units. The fitness function minimises the total system cost while satisfying a reliability constraint (battery state of charge and power-balance). The GA converges to a Pareto-optimal sizing configuration that outperforms classical rule-based approaches in both cost and reliability.

    \item \textbf{Particle Swarm Optimization (PSO)}: Iteratively improves a candidate solution based on the movement of particles in the search space. Used for sizing optimization.

\textit{Simple analogy:} Imagine a flock of birds searching a field for the best patch of food. Each bird (particle) remembers the best location it has personally visited, and also knows the best location found by any bird in the flock. At each time step, every bird adjusts its velocity ,moving partly toward its own best-known spot and partly toward the swarm's global best, gradually converging on the optimal location. In an energy system context, each particle encodes a candidate sizing solution (e.g.\ PV capacity, battery size), and the ``food'' is the minimum cost or maximum reliability.

\textit{Example:} Mohamed et al.~\cite{mohamed2016pso} applied PSO to optimally size a grid-connected hybrid PV--wind--battery--diesel system for remote areas in Saudi Arabia. Each particle represents a combination of component sizes, and the fitness function minimises the \textit{Loss of Load Probability} (LOLP) and total cost simultaneously. A smart-grid load-shifting strategy was integrated to reshape the demand profile, further reducing required storage capacity.
    \item \textbf{Simulated Annealing (SA)}: A probabilistic technique used to approximate the global optimum of a non-convex objective function by iteratively exploring the solution space and occasionally accepting worse solutions to escape local optima.

\textit{Simple analogy:} SA is inspired by the physical process of slowly cooling molten metal. A blacksmith heats steel until the atoms move freely, then cools it gradually so they settle into a low-energy, stable crystalline structure. If cooled too quickly, defects are trapped; if cooled slowly, the atoms find near-perfect arrangements. In optimization, the ``temperature'' controls how willing the algorithm is to accept a worse solution. At high temperature, the algorithm explores broadly and accepts bad moves freely, avoiding entrapment in local optima. As the temperature is gradually reduced (\textit{cooling schedule}), the algorithm becomes increasingly selective, eventually converging on a near-global optimum. This makes SA particularly suited to the \textbf{non-linear, non-convex} sizing problems typical of hybrid renewable energy systems.

\textit{Example:} Ekren and Ekren~\cite{ekren2010sa} applied SA to size a stand-alone PV--wind--battery hybrid system for a campus in Turkey. The decision variables were PV array size, wind turbine rotor swept area, and battery capacity. The objective function minimised total system cost subject to a reliability constraint. 
\end{itemize}

\subsection{Stochastic and Hybrid Modeling Methods}
These methods address the inherent limitations of deterministic MILP related to computational load and parameter certainty.

\begin{itemize}
    \item \textbf{Stochastic Programming (SP)}: Integrates \textbf{uncertainty} (e.g., volatile electricity prices, uncertain PV output) into the optimization by using multiple scenarios with associated probabilities. Used to determine a system size that is optimal for the \textit{expected cost}.
    \item \textbf{Robust Design Optimization (RDO)}: A specific type of SP that aims to minimize the sensitivity of the solution to external parameters, ensuring the design performs well even under "worst-case" scenarios.
    \item \textbf{Hybrid Optimization Algorithm (HOA)}: Combines the strength of accurate MILP for sizing over \textit{representative days} with a faster heuristic or data-driven model for calculating the \textit{actual annual operating costs}. This offers a major speed advantage, addressing the core computational bottleneck of full-year MILP models.
    \item \textbf{Simulation-Based Optimization}: Couples an accurate black-box simulator (like TRNSYS) with an external meta-heuristic algorithm (GA or PSO) to iteratively search for the optimal design within the simulator’s bounds.
\end{itemize}



\subsection{Why MILP is the Preferred Method for This Study}

Several modelling paradigms exist for the analysis and design of energy systems. Ringkj{\o}b et al.
provide an extensive review of 75 modelling tools and classify 
them along two independent axes: \textit{purpose} (investment decision support, 
operation decision support, scenario analysis, power system analysis) and 
\textit{methodology} (simulation, linear programming, mixed-integer linear 
programming, non-linear programming, and heuristic optimisation) \cite{Ringkjob2018}. This 
classification underscores that the choice of modelling method is primarily 
driven by the research objective rather than by system complexity alone; complexity 
acts as a secondary constraint that determines which methods remain computationally 
tractable.


The preceding overview of optimization methods reveals a clear hierarchy of suitability when the objective is to simultaneously size and schedule a multi-technology energy system for a commercial building. This section argues why MILP is the most appropriate framework for the specific system under study, comprising a high-temperature heat pump (HTHP), photovoltaic (PV) system, battery energy storage system (BESS), and thermal energy storage (TES).

\subsubsection*{Global Optimality and Determinism}

The most fundamental advantage of MILP over heuristic and meta-heuristic methods (GA, PSO, SA) is its guarantee of a \textbf{globally optimal solution}. As established before, methods such as GA and PSO do not guarantee global optimality; they converge on near-optimal solutions whose quality depends on population size, tuning parameters, and random initialisation. MILP, by contrast, is a \textbf{deterministic} method: given the same input data, it always returns the same certified optimal solution within a defined optimality gap\footnote{The optimality gap is the relative difference between the best feasible integer solution found (upper bound) and the lower bound on the objective value, expressed as a percentage. The lower bound is obtained via \textit{LP relaxation}: binary variables are temporarily allowed to take fractional values, yielding an easier continuous problem whose objective is always at least as good as any integer-feasible solution. A gap of 0\% certifies global optimality; in practice, solvers such as Gurobi terminate when the gap falls below a user-defined tolerance (e.g.\ 0.01\%).}. This is a critical requirement for an investment decision tool, where the result must be defensible and reproducible~\cite{beck2021milp}.

\subsubsection*{Simultaneous Sizing and Scheduling}

The energy system in this study requires \textbf{coupled sizing and operational optimization}: the optimal capacity of the BESS and TES depends directly on how they are dispatched, and vice versa. MILP naturally handles this coupling in a single mathematical formulation. Binary decision variables represent discrete on/off states of the HTHP and charging/discharging modes of storage, while continuous variables represent energy flows and component capacities. This avoids the sequential, decoupled approach common in simulation-based methods (e.g., TRNSYS-coupled GA), which may yield suboptimal designs because sizing and scheduling are not optimized simultaneously~\cite{cardoso2014milp}.

\subsubsection*{Native Modeling of Mixed Continuous and Discrete Decisions}

Commercial building energy systems inherently involve both continuous variables (e.g., power flows, state of charge) and discrete decisions (e.g., whether the heat pump is operating, whether the battery is charging or discharging). MILP handles this mixed structure natively, whereas NLP cannot represent discrete decisions, and MINLP while theoretically capable remains computationally intractable for reasonably sized building energy models~\cite{kotzur2018milp}. This makes MILP the standard framework in the literature for building-level multi-energy system optimization~\cite{beck2021milp}.

\subsubsection*{Suitability for TES and HTHP Modeling}

TES introduces particular modeling challenges: the storage state of charge must be tracked across time steps, charging and discharging cannot occur simultaneously, and heat losses must be accounted for. All of these constraints are naturally expressed as linear equality and inequality constraints in MILP. \cite{cardoso2014milp} demonstrated that MILP can model TES with dual temperature sections, enabling the coupling of a heat pump as a charging technology directly analogous to the HTHP--TES configuration in this study. Furthermore, \cite{kotzur2018milp} showed that MILP-based optimization frameworks with reduced-complexity TES models solve significantly faster than dedicated simulation tools such as TRNSYS, while retaining sufficient accuracy for investment decisions.

\subsubsection*{Integration of Time-of-Use Electricity Pricing}

A key economic driver for the BESS and TES in a commercial building is \textbf{price arbitrage}: charging storage when electricity is cheap and discharging when it is expensive. MILP is inherently well-suited to this, as time-varying electricity prices enter linearly into the objective function. \cite{beck2021milp} explicitly note that MILP integrates seamlessly with demand response programs and variable electricity tariffs, making it the most effective framework for cost minimization in building energy systems with flexible loads and storage.

\subsubsection*{Limitations Acknowledged}

MILP requires \textbf{linearization} of any non-linear physical relationships. The coefficient of performance (COP) of the HTHP, for example, is in reality a non-linear function of source and sink temperatures. In this study, the COP is treated as a fixed parameter, representing a standard linearization assumption that is widely applied in the literature~\cite{cardoso2014milp, kotzur2018milp}. This is a deliberate trade-off: the loss of physical accuracy is accepted in exchange for the computational tractability and global optimality guarantees that MILP provides, which are essential for a design tool intended to evaluate investment decisions across a range of scenarios.


\subsection{MILP-based design optimisation framework.}
% ── FIGURE ────────────────────────────────────────────────────────
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/MILP_Methodology_Diagram.png}
    \caption{
        Overview of the proposed MILP-based design optimisation framework.
    }
    \label{fig:milp_methodology}
\end{figure}

The proposed design tool follows a four-stage workflow, illustrated in
\autoref{fig:milp_methodology}.
In the first stage, five hourly time-series datasets covering a full
calendar year (8\,760~time steps) are assembled:
day-ahead electricity spot prices~$\lambda(t)$~[\euro/kWh],
solar irradiation~$G(t)$~[W/m\textsuperscript{2}] and ambient
temperature~$T_\mathrm{amb}(t)$ derived from a typical meteorological
year~(TMY) file, the building's thermal heat demand~$Q_\mathrm{dem}(t)$~[kW],
its electrical demand~$P_\mathrm{dem}(t)$~[kW] (both representative of a
commercial building profile), and the applicable grid connection
constraints including the maximum import/export power~$P_\mathrm{max}$ and
network tariffs.

In the second stage the raw time series are normalised, validated, and
aligned to a common hourly resolution.
Representative operational periods (seasonal, peak, and off-peak) are
subsequently identified to characterise the building's energy-use pattern
throughout the year.

The third stage formulates a Mixed-Integer Linear Programme~(MILP).
The objective function minimises the total annualised system cost,
comprising capital expenditure~(CAPEX) and operational
expenditure~(OPEX):
%
\begin{equation}
    \min_{\mathbf{x},\,\mathbf{u}(t)}\;
    \mathrm{CAPEX}(\mathbf{x}) + \mathrm{OPEX}(\mathbf{u}(t))
    \label{eq:milp_obj}
\end{equation}
%
subject to the electricity and thermal energy balances,
state-of-charge~(SOC) bounds, component power limits,
binary on/off decisions, and grid capacity constraints,
enforced for every time step $t \in \{1,\ldots,8760\}$.
The programme is solved using an open-source Branch-and-Bound solver
(CBC) or the commercial solver Gurobi, implemented in Python via
PuLP~\cite{mitchell2011pulp} or Pyomo~\cite{hart2011pyomo},
with a relative optimality gap tolerance of~0.1\,\% and a time
limit of~3600\,s.

The fourth and final stage returns the cost-optimal installed
capacities of four storage and conversion technologies:
the BESS capacity~$E^*$~[kWh] and rated power~$P^*$~[kW],
the HTHP thermal output~$Q_\mathrm{hp}^*$~[kW],
the PV array area~$A_\mathrm{pv}^*$~[m\textsuperscript{2}],
and the TES tank volume~$V_\mathrm{tes}^*$~[m\textsuperscript{3}].

% ── ENERGY SYSTEM SCHEMATIC ──────────────────────────────────────
\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/Energy_System_Schematic_final.png}
    \caption{
        Energy system schematic of the studied commercial building configuration,
        showing the electricity and thermal energy flows between all components.
        Dashed arrows indicate technologies to be replaced; solid orange arrows
        denote electricity flows; solid red arrows denote thermal flows.
    }
    \label{fig:energy_system}
\end{figure}

The energy system under study is depicted in \autoref{fig:energy_system}.
The commercial building is characterised by a simultaneous electrical demand
$P_\mathrm{dem}(t)$ and thermal heat demand $Q_\mathrm{dem}(t)$, both
resolved at an hourly resolution over a full calendar year.
Thermal supply is currently provided by a gas boiler
(supplying $Q_\mathrm{boiler}(t)$), which is designated for replacement by the
optimised system.

Four technologies constitute the candidate system.
On the electrical side, a photovoltaic~(PV) array of panel area
$A_\mathrm{pv}^*$~[m\textsuperscript{2}] generates power from incident solar
irradiation $G(t)$ at ambient temperature $T_\mathrm{amb}(t)$.
A Battery Energy Storage System~(BESS) with capacity $E^*$~[kWh] and rated
power $P^*$~[kW] enables temporal shifting of electrical energy through
charge and discharge cycles.
Residual electrical demand is balanced by import from, or export to, the
electricity grid at power $P_\mathrm{grid}(t)$~[kW], subject to the network
connection limits.

On the thermal side, a High-Temperature Heat Pump~(HTHP) converts electrical
energy into heat at a rated thermal output $Q_\mathrm{hp}^*$~[kW], with
performance characterised by its coefficient of performance~(COP) as a
function of $T_\mathrm{amb}(t)$.
Thermal energy can be buffered in a Thermal Energy Storage~(TES) tank of
volume $V_\mathrm{tes}^*$~[m\textsuperscript{3}], decoupling heat generation
from instantaneous demand.
Together, these four technologies  BESS, HTHP, PV, and TES  form the
decision space of the MILP optimisation described in
\autoref{eq:milp_obj}, with their optimal capacities returned as the
primary output of the design tool

\section{Mixed-Integer Linear Programming (MILP)}
\label{sub:milp_advantages}

MILP is widely recognized in academic and industrial literature as the state-of-the-art methodology for optimizing the design and scheduling of energy systems\cite{Stinner2021ModelComplexity} \cite{Urbanucci2018Limits}.To understand the advantages of MILP, it is first necessary to understand the underlying computational mechanism used by modern solvers (e.g., Gurobi, CPLEX). 
\subsection{ MILP algorithm}




A \textbf{Mixed-Integer Linear Programming (MILP)} model is a mathematical optimization model where the objective function and all constraints are linear, but some, or all, of the decision variables are restricted to be integers (which can include binary, or 0-1, variables). The standard form of an MILP problem is:

\begin{equation}
\begin{aligned}
\text{Minimize (or Maximize)} \quad & \mathbf{c}^T\mathbf{x} + \mathbf{d}^T\mathbf{y} \\
\text{Subject to} \quad & \mathbf{A}\mathbf{x} + \mathbf{B}\mathbf{y} \le \mathbf{b} \\
& \mathbf{x} \ge 0, \quad \mathbf{x} \in \mathbb{R}^n \\
& \mathbf{y} \ge 0, \quad \mathbf{y} \in \mathbb{Z}^m
\end{aligned}
\end{equation}

Where:
\begin{itemize}
    \item $\mathbf{x}$ is a vector of \textbf{continuous} decision variables.
    \item $\mathbf{y}$ is a vector of \textbf{integer} decision variables (often binary variables are used to model yes/no decisions).
    \item $\mathbf{c}$ and $\mathbf{d}$ are vectors of coefficients for the objective function.
    \item $\mathbf{A}$ and $\mathbf{B}$ are matrices of coefficients for the constraints.
    \item $\mathbf{b}$ is a vector of right-hand side values for the constraints.
\end{itemize}

\section*{Key Components}

\begin{itemize}
    \item \textbf{Linear Objective Function:} The goal (e.g., minimizing cost or maximizing profit) is expressed as a linear combination of the variables. For example, \texttt{Minimize Cost = c1*x1 + c2*x2 + ...}.
    \item \textbf{Linear Constraints:} The limitations and requirements of the problem (e.g., resource availability, demand, capacity) are expressed as linear equations or inequalities. Non-linear expressions like \texttt{x1*x2} are not allowed, though some can be linearized using specific modeling techniques.
    \item \textbf{Mixed Variables:} The defining characteristic is the presence of both continuous variables (which can take any real value) and integer variables (which are restricted to whole numbers).
\end{itemize}


\section*{Solution Approach}

MILP problems are generally NP-hard and more difficult to solve than pure linear programming problems. Solvers (such as CPLEX, Gurobi, or SCIP) typically use algorithms like \textbf{Branch and Bound} or \textbf{Branch and Cut}. These methods work by:
\begin{enumerate}
    \item Solving a relaxed version of the problem (ignoring the integer constraints) to get a lower bound on the optimal solution.
    \item Systematically exploring a search tree (branching) and adding constraints (cutting planes) to find an optimal integer-feasible solution.
\end{enumerate}
\section*{Example}
\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/milp_pic.jpg}
    \caption{Schematic of the Solution approach of MILP \cite{du2025learning}}
    \label{fig:example_image}
    
\end{figure}

\subsubsection{The Branching Decision (Example: PV Investment)}

Consider the binary variable for investing in PV system capacity, denoted $y_{\text{PV}} \in \{0, 1\}$.

\begin{itemize}
    \item \textbf{Initial Problem Node (Root Node):} $\min C_{\text{total}}$ with $0 \le y_{\text{PV}} \le 1$.
    \item \textbf{Branching Operation:} The solver partitions the original problem into two mutually exclusive subproblems:
\end{itemize}

\begin{align*}
    \text{Subproblem 1 (Assigned to Core 1):} \quad & \min C_{\text{total}} \\
    & \text{subject to } y_{\text{PV}} = 1 \quad (\text{PV is Purchased})
\end{align*}

\begin{align*}
    \text{Subproblem 2 (Assigned to Core 2):} \quad & \min C_{\text{total}} \\
    & \text{subject to } y_{\text{PV}} = 0 \quad (\text{PV is Not Purchased})
\end{align*}

\subsection{Parallel Processing and Pruning}

\begin{itemize}
    \item \textbf{Parallel Execution:} Core 1 searches for the best design \textit{assuming} PV is bought, while Core 2 searches for the best design \textit{assuming} PV is not bought.
    \item \textbf{Global Update:} If Core 1 quickly finds a feasible solution with total cost $\boldsymbol{C_{\text{Upper}} = \$10,000}$, this value is immediately broadcasted as the new \textbf{Global Upper Bound} on the final cost.
    \item \textbf{Pruning:} If Core 2 finds that the theoretical minimum cost in its branch (its Lower Bound) is $\boldsymbol{L_{\text{Subproblem 2}} = \$10,500}$, Core 2 can immediately \textbf{prune} its entire branch because it cannot contain a solution better than the already known $\boldsymbol{C_{\text{Upper}} = \$10,000}$.
\end{itemize}



\subsection{MILP advantages}
\subsubsection{Guarantee of Global Optimality}

As a deterministic mathematical programming technique, MILP is fundamentally designed to find the exact best possible outcome for a given objective function.
It guarantees finding a globally optimal solution when the problem converges \cite{Urbanucci2018Limits}, a critical advantage over heuristic methods (such as Genetic Algorithms or Particle Swarm Optimization), which can only guarantee finding acceptable solutions or local optima.

\subsubsection{Integration of Discrete and Continuous Decisions}

MILP is essential for system design and sizing because it effectively integrates the two necessary classes of variables:
\begin{itemize}
    \item \textbf{Integer/Binary Variables:} These are necessary to model structural and commitment decisions, such as the selection of a technology (e.g., using a Heat Pump or not), the existence of a component, the integer number of units to install, or the binary on/off status of a component at each time step.
    \item \textbf{Continuous Variables:} These model the operational schedule and energy flow, such as the electrical power being dispatched or the continuous volume of a storage unit.
\end{itemize}
This dual capability allows MILP to perform the required co-optimization of investment and operation simultaneously \cite{Bordin2020MILP}.

\subsubsection{Handling Non-Linearity via Linearization}

While the presence of complex, high-order non-linear models (e.g., in thermodynamics or equipment performance curves) presents a challenge, MILP addresses this through robust techniques.
The primary method is \textbf{Piecewise Linear (PWL) approximation}.
This technique simplifies non-linear functions (like the thermal dynamics of an Electric Water Heater or the variable efficiency of an inverter) into a series of linear segments.
By converting the problem into a strictly linear form, the overall optimization process can be performed by commercially available solvers (e.g., CPLEX, Gurobi) efficiently, providing an "efficiently solvable model" for these complex problems.
MILP's use of this technique is why it remains the preferred approach over complex Mixed-Integer Non-Linear Programming (MINLP) problems, which often have higher computational requirements \cite{Urbanucci2018Limits, Fuso2025University}.



\subsection{Optimization Objective Functions in MILP}

\subsection*{Cost Minimization (Economic)}

The minimization of total cost is the most common objective, aiming for the economically optimal design and operation.

\begin{itemize}
    \item \textbf{Objective:} Minimize Total Annual Cost ($C_{\text{total}}$) or Lifetime Cost.
    \item \textbf{Key Components:} Total costs comprise Investment Costs ($C_{\text{invest}}$) and Operational Costs ($C_{\text{op}}$).
    \begin{enumerate}
        \item \textbf{Investment Costs ($C_{\text{invest}}$):} The annualized cost is spread over the component's lifetime using an Annuity Factor (ANF). Non-linear cost functions are typically addressed by using discrete equipment sizes or linear approximations.
        $$C_{\text{invest}} = \sum_{i} ANF^{-1} \cdot (C_{\text{inv\_fix},i} + c_{\text{inv\_cap},i} \cdot cap_{i}^{m_{i}})$$
        \item \textbf{Operational Costs ($C_{\text{op}}$):} Includes energy costs (based on constant or variable electricity prices, e.g., time-of-use tariffs) and maintenance costs.
        $$ \min C_{\text{total}} = C_{\text{op}} + ANF^{-1} \cdot C_{\text{invest}} $$
    \end{enumerate}
\end{itemize}

\subsection*{CO$_2$ Emissions Minimization (Environmental)}

This objective focuses on reducing the system's environmental impact.

\begin{itemize}
    \item \textbf{Objective:} Minimize Total $\mathbf{CO_2}$ Emissions.
    \item \textbf{Key Components:} Emissions are calculated by weighing the consumption of electricity and other fuels by their respective $\mathbf{CO_2}$ emission factors. This often aims to shift electricity consumption to periods when the grid's carbon intensity is lower. Primary Energy Consumption (PEC) is sometimes used as a related metric.
\end{itemize}

\subsection*{Mathematical Formulation}

The overall objective function is the sum of operational and amortized embodied $\text{CO}_2$ emissions:
$$
\min \text{C}_{\text{CO}_2}^{\text{Total}} = \text{C}_{\text{CO}_2}^{\text{Op}} + \text{C}_{\text{CO}_2}^{\text{Emb}}
$$

\textbf{1. Operational Emissions ($\text{C}_{\text{CO}_2}^{\text{Op}}$) - Minimizing Grid Carbon Intensity}

\begin{equation}
\min \text{C}_{\text{CO}_2}^{\text{Op}} = \sum_{t \in T} \left( \sum_{i \in I} \text{E}_{\text{CO}_2,i,t}^{\text{Op}} \cdot \text{P}_{i,t}^{\text{Cons}} \cdot \Delta t \right)
\end{equation}

\subsection*{Multi-Criteria Optimization}

To balance conflicting objectives like cost minimization and Co2 emmisions minimization, multi-criteria optimization is applied.

\begin{itemize}
    \item \textbf{Approach:} The most common method is the \textbf{weighted summation method}, which combines normalized objectives into a single linear objective function using weighting factors ($\alpha$, $\beta$).
    \item \textbf{Weighted Sum Objective:}
    $$\min f = \alpha (\mu C) + \beta D$$
    where $C$ is the cost, $D$ is a discomfort index (or other non-cost objective), and $\alpha$ and $\beta$ ($\alpha + \beta = 1$) are the user's preference factors. $\mu$ is a scaling factor for normalization.
\end{itemize}

\subsection{ epsilon constrained method to create pareto front}

The $\epsilon$-constraint method is highly effective for visualizing the \textbf{trade-off} between two conflicting objectives in a \textbf{Mixed-Integer Linear Programming (MILP)} problem, such as minimizing the \textbf{Total Annual Cost} ($f_{\text{Cost}}$) and minimizing the \textbf{Total Life Cycle $\text{CO}_2$ Emissions} ($f_{\text{CO}_2}$).

\subsection*{$\epsilon$-Constraint Method in MILP for Cost vs. $\text{CO}_2$}

In this specific case, the Multi-Objective Problem (MOP) is to simultaneously minimize both $f_{\text{Cost}}$ and $f_{\text{CO}_2}$:
$$
\min \left[ f_{\text{Cost}}(\mathbf{x}), f_{\text{CO}_2}(\mathbf{x}) \right] \\
\text{s.t. } \mathbf{x} \in \mathcal{X}
$$

The solution vector $\mathbf{x}$ includes both continuous variables (e.g., operational energy flows $\text{P}_{i,t}$) and integer/binary variables (e.g., component sizing $\text{Z}_j$, on/off status $u(t)$).
The feasible region $\mathcal{X}$ is defined by constraints, such as:
\begin{enumerate}
    \item \textbf{Investment Constraints:} Component capacities ($\text{Z}_j$) are non-negative and often discrete (MILP is applicable for incorporating discrete decisions).
    \item \textbf{Energy Balance:} Supply must meet demand in every time step.
    \item \textbf{Physical Limits:} Component models (e.g., Heat Pump COP, TES energy balance) must be satisfied.
\end{enumerate}

\subsubsection*{1. Transformation to Single-Objective Problem (SOP)}

The $\epsilon$-constraint method selects one objective as the primary optimization target.
If we choose to \textbf{minimize Total Annual Cost} ($f_{\text{Cost}}$), the $\mathbf{CO_2}$ objective ($f_{\text{CO}_2}$) is converted into a constraint bounded by a user-defined threshold, $\epsilon_{\text{CO}_2}$ (the Greek letter $\epsilon$ gives the method its name).

The resulting $\mathbf{SOP}$ solved by the $\mathbf{MILP}$ solver is:
$$
\min f_{\text{Cost}}(\mathbf{x}) \\
\text{s.t. } f_{\text{CO}_2}(\mathbf{x}) \leq \epsilon_{\text{CO}_2} \\
\mathbf{x} \in \mathcal{X}
$$

\subsubsection*{2. Generating the Pareto Front (Trade-off Curve)}

The \textbf{Pareto front} the set of non-dominated solutions where no objective can be improved without worsening the other is generated by systematically varying $\epsilon_{\text{CO}_2}$ and solving the MILP for each step.

\begin{enumerate}
    \item \textbf{Determine $\mathbf{CO_2}$ Range (Boundary Solutions):}
    \begin{itemize}
        \item \textbf{Max $\mathbf{CO_2}$ / Min Cost Point:} Solve the MILP to \textbf{only} minimize $f_{\text{Cost}}$.
        \item The resulting total $\text{CO}_2$ emissions, $f_{\text{CO}_2}^{\max}$, defines the largest $\epsilon_{\text{CO}_2}$ needed.
        \item This solution usually involves the cheapest, but most $\text{CO}_2$-intensive technologies (e.g., large Auxiliary Heater, small Heat Pump, minimal storage).
        \item \textbf{Min $\mathbf{CO_2}$ / Max Cost Point:} Solve the MILP to \textbf{only} minimize $f_{\text{CO}_2}$.
        \item The resulting total $\text{CO}_2$ emissions, $f_{\text{CO}_2}^{\min}$, defines the most restrictive $\epsilon_{\text{CO}_2}$ limit (which might require the largest or most expensive set of components, e.g., large $\mathbf{PV}$ and \textbf{Battery}).
    \end{itemize}
    \item \textbf{Discretization:} The range $\left[ f_{\text{CO}_2}^{\min}, f_{\text{CO}_2}^{\max} \right]$ is divided into a discrete number of steps $N$.
    \item Each step defines a new, tighter $\epsilon_{\text{CO}_2}$ value.
    \item \textbf{Iteration and Output:} For each $\epsilon_{\text{CO}_2}$ step, the SOP is solved.
    \item The optimal annual cost $f_{\text{Cost}}^{*}$ is recorded.
\end{enumerate}

This yields a set of points $\left( f_{\text{CO}_2}, f_{\text{Cost}}^{*} \right)$, which when plotted, visually represents the \textbf{cost-optimal system design} for every level of \textbf{$\text{CO}_2$ reduction} desired.
The steepness of the curve indicates the marginal cost of achieving further environmental improvement.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.8\textwidth]{images/pareto_Front.png}
    \caption{Schematic pareto Front\cite{PerezIribarren2023MILP}}
    \label{fig:example_image}
    
\end{figure}

\section{Component Modeling in Optimization Frameworks}
\label{sec:comp_modeling_methods}
\subsection{Component model requiremnts}
To be implemented within a Mixed-Integer Linear Programming (MILP) framework, a component model must fundamentally adhere to strict mathematical constraints regarding linearity to ensure the problem remains solvable by standard optimization algorithms~\cite{Krutzfeldt2021}.
Specifically, both the equality and inequality constraints defining the component's behavior, as well as the objective function, must be linear formulations involving real and integer decision variables~\cite{Wu2019}.
Since the actual thermodynamic dynamics of components such as the temperature-dependent Coefficient of Performance (COP) of heat pumps or the mixing processes in water heaters are inherently nonlinear and complex, they cannot be directly processed by MILP solvers~\cite{Wu2019, Krutzfeldt2021}.
Consequently, these models must undergo linearization or simplification, such as using piecewise linear approximation methods~\cite{Wu2019}, Taylor polynomial approximations~\cite{Krutzfeldt2021}, or simplifying thermal storage to a homogeneous capacity to avoid endogenous optimization problems~\cite{Steen2015}.
Furthermore, the physical system must be linearized temporally through time discretization, breaking continuous operations into distinct time steps to allow for the simultaneous optimization of design and operation~\cite{Krutzfeldt2021}.

\subsection{Lorentz Heat Pump Model Implementation} % Now a subsection
\label{sub:lorentz_model}

In energyPRO \cite{EMD_COP_HeatCapacity}, a simple \emph{Lorentz heat pump model} is implemented for calculating the theoretical Coefficient of Performance ($\text{COP}$). This model is based on the Carnot cycle, modified to account for the temperature variation during heat transfer by using mean temperatures.

\subsubsection{Theoretical COP Calculation} % Now a subsubsection

The theoretical $\text{COP}$ is calculated using the following thermodynamic relationship, where temperatures are expressed in Kelvin ($\text{K}$):

\[
\mathrm{COP} = \frac{T_{\mathrm{High,mean}}}{T_{\mathrm{High,mean}} - T_{\mathrm{Low,mean}}}
\]

\subsubsection{Mean Temperature of Delivered Hot Water ($T_{\mathrm{High,mean}}$)}

The mean temperature of the delivered hot water is computed using the logarithmic mean temperature over the condensor:

\[
T_{\mathrm{High,mean}} = 
\frac{T_{\mathrm{High,out}} - T_{\mathrm{High,in}}}
{\ln\!\left( \dfrac{T_{\mathrm{High,out}} + 273.15}{T_{\mathrm{High,in}} + 273.15} \right)}
\]

where:
\begin{itemize}
    \item $T_{\mathrm{High,out}}$ is the outlet water temperature (\textdegree C),
    \item $T_{\mathrm{High,in}}$ is the inlet water temperature (\textdegree C).
\end{itemize}

\subsubsection{Mean Temperature of the Heat Source ($T_{\mathrm{Low,mean}}$)}

\[
T_{\mathrm{Low,mean}} = 
\frac{T_{\mathrm{Low,out}} - T_{\mathrm{Low,in}}}
{\ln\!\left( \dfrac{T_{\mathrm{Low,out}} + 273.15}{T_{\mathrm{Low,in}} + 273.15} \right)}
\]

where:
\begin{itemize}
    \item $T_{\mathrm{Low,out}}$ is the initial temperature of the heat source (\textdegree C),
    \item $T_{\mathrm{Low,in}}$ is the cooled-down temperature of the heat source (\textdegree C).
\end{itemize}

\subsubsection{Actual COP and Heat Capacity Calculation}

The heat pump efficiency factor $\eta_{\mathrm{HP}}$ is computed from the stated COP and the theoretical COP under specification conditions (this efficiency can be derived from manufacturers data as in appendix \ref{appendix:datasheet_hthp}):

\[
\eta_{\mathrm{HP}} = 
\frac{\mathrm{COP}_{\mathrm{stated}}}{\mathrm{COP}_{\mathrm{theoretical,spec}}}
\]

At each timestep, the actual COP becomes:

\[
\mathrm{COP}_{\mathrm{actual}} = 
\mathrm{COP}_{\mathrm{theoretical,actual}} \cdot \eta_{\mathrm{HP}}
\]

The resulting heat capacity is:

\[
\dot{Q}_{\mathrm{heat}} = 
\mathrm{COP}_{\mathrm{actual}} \cdot \dot{E}_{\mathrm{elec}}
\]

\paragraph*{Limitation on Heat Capacity}




\subsection{Linearization for MILP:} 
When the $T_{\mathrm{High,out}}$ is considered a decision varible in the MILP optimization we cannot simply us the method desribed in \ref{sub:lorentz_model} we then need to linearize the equation
as done by Krutzefeldt et al. \cite{Germanbuiling}. The resulting non-linear energy relation $\left(P_{el} = Q_{HP} / COP\right)$ is transformed into a linear constraint by applying a multi-dimensional \textbf{Taylor polynomial approximation} around a fixed operation point (OP). The linear approximation $f(x) \approx f(a) + \nabla f(a) \cdot (x-a)$ for the electric power $P_{el, HP, t}$ (function $f$) with state vector $x = (\dot{Q}_{HP, t}, COP_{ t})$ and fixed operation point $a = (\dot{Q}_{OP}, COP_{OP})$ yields the constraint:
\begin{equation}
\begin{split}
\label{eq:taylor}
P_{el,HP,t} = &\frac{\dot{Q}_{OP}}{COP_{OP}} + \frac{1}{COP_{OP}} \cdot (\dot{Q}_{HP,t} - \dot{Q}_{OP}) \\
&- \frac{\dot{Q}_{OP}}{COP_{OP}^{2}} \cdot (COP_{t} - COP_{OP})
\end{split}
\end{equation}
This process enables the inclusion of realistic, state-dependent COP in the MILP model while maintaining linearity. Note that this approach is only needed when het COP contains a decision variable that is used in MILP, only then the equation $\left(P_{el} = Q_{HP} / COP\right)$ is non-linear( in the decision variables). In this master thesis we assume $T_{\mathrm{High,out}}$ as a given so we can use the method as described in \ref{sub:lorentz_model}.

\subsection{Thermal Energy Storage (TES) Modeling}

\subsubsection{Thermal Energy Storage (TES) Technology Selection}

Thermal Energy Storage (TES) systems can be implemented using several distinct technologies, each offering a different trade-off between volumetric energy density, cost, complexity, and operating temperature range. The three most relevant categories for building-scale applications are sensible heat storage (typically water tanks), latent heat storage (Phase Change Materials, PCM), and thermochemical storage.

\textbf{Sensible heat storage} using water tanks is the most mature and widely deployed technology. Water has a high specific heat capacity ($c_p \approx 4.18\,\text{kJ/kg·K}$), is inexpensive, non-toxic, and compatible with a broad range of heating and cooling systems. However, the volumetric energy density is relatively low: storing a meaningful quantity of thermal energy requires large tank volumes, which can be a limiting factor in space-constrained commercial building contexts \cite{Dincer2018}. Water tanks are typically operated between 40\,°C and 95\,°C for heating applications and are well-suited to coupling with high-temperature heat pumps (HTHPs).

\textbf{Latent heat storage} using Phase Change Materials (PCMs) exploits the energy absorbed or released during a phase transition (typically solid-to-liquid). PCMs can achieve volumetric energy densities 5--14 times higher than water for the same temperature lift, depending on the material selected \cite{Sharma2009}. This significantly reduced footprint makes LTES particularly attractive when available installation space is limited. The primary drawbacks are higher material and encapsulation costs, lower thermal conductivity (requiring enhanced heat exchanger designs), and sensitivity to the operating temperature window of the phase transition, which must be aligned with the supply temperature of the coupled heat source or sink \cite{Cabeza2014}. For HTHP-coupled systems, PCMs with melting points in the range of 55--80\,°C, such as fatty acids or paraffin waxes, are typically considered.

\textbf{Thermochemical storage} offers the highest theoretical energy density but remains largely at the research and pilot scale, with limited commercial availability and significant engineering complexity. It is therefore not considered further in this work.

A comparative summary of the relevant TES technologies is provided in Table~\ref{tab:tes_comparison}.

\begin{table}[h]
\centering
\caption{Comparative overview of TES technologies considered for HTHP-coupled building applications.}
\label{tab:tes_comparison}
\begin{tabular}{lccc}
\hline
\textbf{Property} & \textbf{Water tank} & \textbf{PCM (LTES)} & \textbf{Thermochemical} \\
\hline
Volumetric energy density & Low & Medium--High & Very High \\
Capital cost & Low & Medium--High & High \\
Technology readiness & High & Medium & Low \\
Required volume & High & Low--Medium & Very Low \\
Thermal conductivity & High & Low & Variable \\
Temperature flexibility & High & Narrow window & High \\
\hline
\end{tabular}
\end{table}

\subsubsection*{Selected Technology and Volume Constraint}

Given the space constraints typical of commercial building retrofits, this work models \textbf{Latent Thermal Energy Storage (LTES)} as the primary TES technology when there is limited space available. Its higher volumetric energy density allows more thermal capacity to be installed within the available footprint, making it well-suited for situations where installation space is a binding constraint.

To capture this spatial limitation within the optimization, the MILP model incorporates an optional maximum volume constraint on the TES unit:

\begin{equation}
    V_{\text{TES}} \leq V_{\text{TES,max}}
\end{equation}

\noindent where $V_{\text{TES}}$ [m$^3$] is the optimally determined storage volume and $V_{\text{TES,max}}$ [m$^3$] is a user-defined upper bound reflecting the physical space available at the installation site. When no space restriction applies, $V_{\text{TES,max}}$ can be set to an arbitrarily large value, leaving the optimizer free to select the cost-optimal storage capacity. Conversely, setting $V_{\text{TES,max}}$ to a finite value forces the MILP algorithm to respect the spatial constraint during sizing, potentially shifting the optimal solution toward more aggressive peak shaving via the HTHP or BESS rather than thermal storage.




The proposed optimization framework utilizes a linear model for thewater Thermal Energy Storage (TES) unit, representing a simplified or ideal stratified storage to maintain the tractability of the Mixed-Integer Linear Programming (MILP) problem \cite{Krien2020oemofsolph}. This approach balances accuracy with computational speed, as the detailed modeling of stratification, though more physically precise, significantly increases computation time. Studies show that the simplified model is often sufficiently accurate for operation optimization, as model overestimations and underestimations of temperature changes tend to compensate for each other, thus ensuring thermal comfort constraints are met \cite{Germanbuiling}.

The selection of a TES model for a Mixed-Integer Linear Programming (MILP) optimization tool involves a trade-off between \textbf{computational tractability} (simplicity) and \textbf{physical accuracy} (complexity) \cite{Krien2020oemofsolph}. The chosen model must accurately reflect real-world constraints, especially when coupled with temperature-sensitive technologies like Heat Pumps (HPs).



\subsubsection{Rule-of-Thumb Dimensioning}
In conventional planning, sizing the TES often relies on a \textbf{rule of thumb} to ensure reliability:
\begin{itemize}
    \item \textbf{Principle:} The storage should be able to provide its \textbf{peak thermal power} for a fixed duration, typically \textbf{6 to 7 hours} \cite{Krien2020oemofsolph}. This method is mainly used for preliminary heuristic sizing within the overall MILP design.
\end{itemize}

\subsubsection{Constant (Size-Independent) Losses}
For basic MILP operational models, thermal losses ($\dot{E}_{\text{loss}}$) can be simplified to be either neglected or represented by a \textbf{constant factor ($\delta$)} that is independent ofthe physical size ($Q_{\text{N}}$) \cite{Krien2020oemofsolph}.

\begin{equation}
Q_{t} = Q_{t-1}  - \delta + \dot{Q}_{\text{in},t} \eta_{\text{in}} \Delta t - \frac{\dot{Q}_{\text{out},t}}{\eta_{\text{out}}} \Delta t,
\label{eq:storage-simplified}
\end{equation}
with
\begin{align*}
\delta &= U (2 *\frac{\pi d^{2}}{4}+ \pi d h) \left(\Delta T_{\text{H0}} + \Delta T_{\text{C0}}\right)/2
\end{align*}

\begin{itemize}
    \item \textbf{Energy Balance:} This approach uses the core energy balance equation, but simplifies the loss terms to depend minimally on dynamic state or size \cite{1.5}.
    \item \textbf{Rationale:} This maintains strict linearity and computational speed, though it is the least physically accurate, as actual losses depend heavily on the temperature differential with the ambient environment.
\end{itemize}

\subsubsection{Stratified Thermal Storage (STS) 2-Zone Model}
\label{sub:sts_model}
The model implements a simplified \textbf{2-zone-model} of a large-scale sensible heat storage with ideal stratification for energy system optimization (see Figure 1 \cite{Krien2020oemofsolph}). This model assumes a cylindrical storage of diameter $d$ and height $h$ containing two perfectly separated bodies of water: a hot zone ($T_{\text{H}}$) and a cold zone ($T_{\text{C}}$). The temperatures are fixed to the feed-in/return temperatures of the heating system. When charging/discharging, the thermocline moves up or down. For investment problems, the diameter is typically fixed, and the height is adjusted to maintain linearity in the capacity calculation \cite{Krien2020oemofsolph}.


\begin{figure}[H]
    \centering
    \includegraphics[width=0.4\textwidth]{images/TES_picture.png}
    \caption{Schematic of the simplified 2-zone model of a stratified thermal storage with constant hot ($T_{\text{H}}$) and cold ($T_{\text{C}}$) temperature regions \cite{Krien2020oemofsolph}.}
    \label{fig:stratifiedtes}
\end{figure}


\subsubsection{Energy Balance}

The equation describing the storage content at time step $t$ is the following:

\begin{equation}
Q_{t} = Q_{t-1} (1 - \beta) - \gamma Q_{\text{N}} - \delta + \dot{Q}_{\text{in},t} \eta_{\text{in}} \Delta t - \frac{\dot{Q}_{\text{out},t}}{\eta_{\text{out}}} \Delta t,
\label{eq:storage-stratified}
\end{equation}
with
\begin{align*}
Q_{N} &= \rho V c \Delta T_{\text{HC}} \\
V &= \frac{\pi d^{2}}{4} h \\
\beta &= U \frac{4}{d \rho c} \Delta t \\
\gamma &= U \frac{4}{d \rho c \Delta T_{\text{HC}}} \Delta T_{\text{C0}} \\
\delta &= U \frac{\pi d^{2}}{4} \left(\Delta T_{\text{H0}} + \Delta T_{\text{C0}}\right) \\
U &= \left( \frac{1}{\alpha_{\text{in}}} + \frac{t_{\text{isolation}}}{\lambda_{\text{isolation}}} + \frac{1}{\alpha_{\text{out}}} \right)^{-1}
\end{align*}

The three terms represent:
\begin{itemize}
    \item $\delta$: Constant heat losses through the top and bottom surfaces.
    \item $\gamma \cdot Q_{\text{N}}$: Losses through the total lateral surface assuming the storage to be empty (storage is at $T_{\text{C}}$, and $\Delta T_{\text{C0}}$ is the driving temperature difference), dependent on the height of the storage.
    \item $\beta \cdot Q_{t-1}$: Additional losses through the lateral surface that belong to the hot part of the water body, dependent on the state of charge.
\end{itemize}



The diameter $d$ is given and the height can be adapted to adapt the nominal capacity of the storage. With this assumption,\textbf{ all relations stay linear}.

Because of the space that diffuser plates for charging/discharging take up, it is assumed that the storage can neither be fully charged nor discharged, which is parametrised as a minimal/maximal storage level (indicated by the dotted lines in \ref{fig:stratifiedtes}).



\subsubsection{Capacity and Boundary Constraints}
The model is constrained by the physical capacity of the storage and boundary conditions required for optimization:

\begin{itemize}
\item \textbf{Cycle Constraint:} To model continuous operation, such as in a rolling horizon or repeated operation schedule, the stored energy at the last time step ($t_{\text{last}}$) must equal the stored energy at the initial time step ($t_{\text{zero}}$) of the \textbf{full} simulation.
 \begin{equation}
 Q_{t-1} = Q_{t}
\tag{10}
 \end{equation}

\item \textbf{Capacity Limit:} The instantaneous stored energy is bound by the nominal capacity to ensure physical realism.
\begin{equation}
0 \le Q_{t} \le Q_N
 \tag{11}
 \end{equation}
\end{itemize}

\vspace{0.5cm}

The use of this simplified model is advantageous for design and operational optimization due to its robustness and linearity within the MILP framework.
\subsubsection{ Comparison simple thermal storage vs stratified thermal storage}
\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/TES_simpleVScomplicated.png}
    \caption{Comparison of storage content decay over time (left) and heat losses versus actual storage content (right) for a simple thermal storage model and stratified thermal storage models of different nominal capacities (30 MWh, 65 MWh, and 90 MWh) \cite{Krien2020oemofsolph}.}
    \label{fig:storage_comparison}
\end{figure}

\ref{fig:storage_comparison} shows that the simpler TES models neglects the dependence of thermal losses on storage tank size, justifying the choice of the (slightly more complicated) stratified approach for our work.
The evident difference in both the absolute loss calculation and the decay of stored energy confirms that the \textbf{simple model is inadequate} for accurately simulating the TES unit's performance, especially for applications like heat pump coupling where temperature and heat loss dependency are critical for economic optimization \cite{Germanbuiling}. Therefore, the \textbf{stratified model is chosen} to ensure the MILP predictions are grounded in realistic thermal dynamics \cite{Germanbuiling}.


\subsection{Photovoltaic (PV) System modeling}

The electrical energy generated by the photovoltaic (PV) system is a crucial input for the overall Mixed-Integer Linear Programming (MILP) optimization framework. For the sake of generality, the PV generation is modeled as a function of environmental conditions and component characteristics, rather than relying exclusively on a specific measured dataset.

\subsubsection{Choice of PV Modeling Approach}

For a geographically generic design tool, two primary methods for obtaining PV data are considered:

\begin{enumerate}
    \item \textbf{Measured Data:} Utilize historical, on-site PV generation data and corresponding weather conditions collected at a specific location (e.g., the M-Accent site in Eeklo). This method provides highly accurate, site-specific input.
    \item \textbf{Model-Based Data:} Employ a publicly validated physical model, such as the widely accepted PVWatts model \cite{Dobos2014PVWatts}, to calculate the DC power output based on standard meteorological inputs (e.g., solar irradiance, ambient temperature, and wind speed). This approach is highly flexible and enables the tool to be applied to any arbitrary location for which standard weather data is available.
\end{enumerate}

\noindent
\textbf{To ensure the developed optimization tool remains \emph{generic} and \emph{widely applicable} to residential buildings in Belgium, the model-based approach is adopted for the core PV simulation.} This decouples the core optimization framework from the physical constraints of a single measurement site.

\subsubsection{PV Model Implementation}

The PVWatts model converts the Plane-of-Array (POA) irradiance into DC power ($P_{\text{DC}}$) by accounting for cell temperature effects. The fundamental equation, derived from the PVWatts framework, relates the instantaneous DC power output to instantaneous POA irradiance ($G_{\text{POA}}$) and cell temperature ($T_{\text{cell}}$):

\label{sub:pv_modeling}
\begin{equation}
\label{pv}
P_{\text{DC}} \left[ \frac{\text{W}}{\text{m}^2} \right] = \frac{G_{\text{POA}}}{1,000 \left[ \frac{\text{W}}{\text{m}^2} \right]} P_{\text{PV}} \left( 1 + \frac{\gamma_{\text{pdc}}}{100} \left( T_{\text{cell}} - T_{\text{ref}} \right) \right) \\
\end{equation}

with
\begin{align*}
\quad P_{\text{DC, PV}} [\frac{\text{W}}{\text{m}^2}] &= \frac{A_{\text{PV}}}{A_{\text{ref.}}} P_{\text{STC}} \\ T_{\text{cell}} [\text{}^\circ\text{C}] &= T_{\text{module}} + \frac{G_{\text{POA}}}{1,000 \left[ \frac{\text{W}}{\text{m}^2} \right]} \Delta T \\
 \quad T_{\text{module}} [\text{}^\circ\text{C}] &= G_{\text{POA}} e^{a + b \cdot v_{\text{wind}}(10\text{m})} + T_{\text{ambient}} 
\end{align*}
\noindent
The instantaneous calculation of DC power output is dependent on the following variables and module specifications:

\begin{table}[h]
    \centering
    \caption{PV Model Variables and Base Values}
    \begin{tabular}{|l|c|l|c|}
        \hline
        \textbf{Variable} & \textbf{Unit} & \textbf{Description} & \textbf{Standard Condition} \\
        \hline
        $A_{\text{ref.}}$ & $\text{m}^2$ & Surface area of one PV module. & N/A \\
        \hline
        $P_{\text{STC}}$ & $\text{W}$ & \textbf{Nominal DC power} for one module. & $1,000\,\text{W/m}^2$  \\
        \hline
        $\gamma_{\text{pdc}}$ & $\%/\text{}^\circ\text{C}$ & \textbf{Temperature coefficient of power}. & -0,5  \\
        \hline
        $v_{\text{wind(10m)}}$ & $\text{m/s}$ & \textbf{Wind velocity} measured at a height of $10\,\text{m}$. & N/A  \\
        \hline
        $G_{\text{POA}}$ & $\text{W/m}^2$ & \textbf{Solar irradiance} in the plane of the array. & N/A  \\
        \hline
        $T_{\text{ambient}}$ & $\text{}^\circ\text{C}$ & \textbf{Ambient outside air temperature}. & 25  \\
        \hline
    \end{tabular}
\end{table}

\noindent
The characteristic parameters $a$, $b$, and $\Delta T$ are empirical and depend on the physical setup. These properties can be retrieved from publicly available data sets, such as those provided by the California Energy Commission \cite{Dobos2012ImprovedCEC}.

\begin{table}[h]
    \centering
    \caption{Empirically determined coefficients used to predict PV module back surface temperature (Extracted from source data) \cite{Dobos2012ImprovedCEC}.}
    \label{tab:pv_coefficients_streamlined}
    \begin{tabular}{|l|l|c|c|c|}
        \hline
        \textbf{Module Type} & \textbf{Mount} & $\mathbf{a}$ & $\mathbf{b}$ & $\mathbf{\Delta T}$ ($\mathbf{^\circ C}$) \\
        \hline
        Glass/cell/glass & Open rack & $-3.47$ & $-.0594$ & $3$ \\
        \hline
        Glass/cell/glass & Close roof mount & $-2.98$ & $-.0471$ & $1$ \\
        \hline
        Glass/cell/polymer sheet & Open rack & $-3.56$ & $-.0750$ & $3$ \\
        \hline
        Glass/cell/polymer sheet & Insulated back & $-2.81$ & $-.0455$ & $0$ \\
        \hline
        Polymer/thin-film/steel & Open rack & $-3.58$ & $-.113$ & $3$ \\
        \hline
        22X Linear Concentrator & Tracker & $-3.23$ & $-.130$ & $13$ \\
        \hline
    \end{tabular}
\end{table}
\subsection{Battery Energy Storage Systems (BESS)}
\label{sub:battery_modeling}

Battery Energy Storage Systems (BESS) are critical components that, when coupled with generation (like PV), provide flexibility to shift electrical loads and enhance a system's self-sufficiency.\cite{Germanbuiling}

\begin{itemize}
    \item \textbf{Role in MILP:} BESS are considered alongside other DER technologies (PV, Combined Heat and Power - CHP) within Mixed-Integer Linear Programming (MILP) optimization frameworks.
    \item \textbf{Application:} MILP models are used for the \textbf{optimal operation, configuration, and sizing} of BESS alongside residential heat pump and PV systems, focusing on economic and self-consumption goals.
    \item \textbf{Modeling:} Modeling a BESS in an optimization framework typically involves formulating constraints for:
    \begin{enumerate}
        \item \textbf{Energy balance} and State-of-Charge (SoC) tracking over time.
        \item \textbf{Capacity limits} (maximum and minimum allowable SoC).
        \item \textbf{Power flow limits} and \textbf{efficiency} (charging/discharging losses).
    \end{enumerate}
\end{itemize}

\subsubsection*{Mathematical Formulation}

The BESS model within the MILP framework uses the following variables and constraints across a set of discrete time periods $t \in T$:

\textbf{Variables:}
\begin{itemize}
    \item $SOC_t$: State of Charge (energy stored) at the end of time period $t$ [kWh] (continuous variable).
    \item $P^{ch}_t$: Power charged into the battery during period $t$ [kW] (continuous variable).
    \item $P^{dis}_t$: Power discharged from the battery during period $t$ [kW] (continuous variable).
    \item $U_t$: Binary variable, equals 1 if the battery is charging in period $t$, and 0 otherwise. This ensures the battery cannot charge and discharge simultaneously.
\end{itemize}

\textbf{Constraints:}

\paragraph{1. Energy Balance and SoC Dynamics:}
The SoC at time $t$ is determined by the previous period's SoC, adjusted by the energy charged and discharged, factoring in charging ($\eta^{ch}$) and discharging ($\eta^{dis}$) efficiencies, and the time step duration ($\Delta t$).
\begin{equation}
    SOC_t = SOC_{t-1} + P^{ch}_t \cdot \eta^{ch} \cdot \Delta t - \frac{P^{dis}_t}{\eta^{dis}} \cdot \Delta t \quad \forall t \in T
\end{equation}

\paragraph{2. SoC Capacity Limits:}
The SoC must remain within a defined minimum ($SOC_{min}$) and maximum ($SOC_{max}$) capacity to ensure operational limits are respected.
\begin{equation}
    SOC_{min} \le SOC_t \le SOC_{max} \quad \forall t \in T
\end{equation}

\paragraph{3. Mutual Exclusion:}

The definition of \textbf{binary variables} that specify whether the battery is charging or discharging isn't needed here, resulting in a consequent saving of \textbf{computational time} \cite{Gabrielli2018Electrochemical}. This simplification is valid because the battery's \textbf{charging/discharging efficiency is lower than 1}. Consequently, the optimization process is able to automatically select the most appropriate operating mode of the battery at each time step, since the energy lost from simultaneously charging and discharging would have a greater influence than the chosen MIP gap \cite{Gabrielli2018Electrochemical}.

The solver first solves the linear programming (LP) relaxation of the problem by removing the integer constraints. This provides a theoretical "best possible" bound on the objective value (a lower bound for minimization, an upper bound for maximization).
The solver then finds the best feasible integer solution it can, which is called the incumbent.
The MIP gap is calculated from the difference between these two values.

\paragraph{4. Power Flow Limits}

The charging and discharging power flows are limited by the nominal power capacity of the battery ($P^{Nom}$) at each time step $t$:

\begin{align}
    \dot{P}_{t}^{c} &\leq P^{Nom} \\
    \dot{P}_{t}^{d} &\leq P^{Nom}
\end{align}



\paragraph{5. Initial Conditions:}
The initial state of charge ($SOC_0$) is set as a known parameter at the beginning of the optimization horizon.
\begin{equation}
    SOC_0 = \text{Initial SoC Value}
\end{equation}

\noindent These linear constraints and mixed-integer variables allow the optimization framework to dispatch the battery effectively to meet the objective function (e.g., minimizing operational cost or maximizing self-consumption).

\paragraph{6. Cycle Aging Proxy:}
Cycle degradation can be implemented in the following manner:
\begin{equation}
    SOC_t = SOC_{t-1} \cdot (1 - \eta^{SD}) + P^{ch}_t \cdot \eta^{ch} \cdot \Delta t - \frac{P^{dis}_t}{\eta^{dis}} \cdot \Delta t \quad \forall t \in T
\end{equation}


\noindent Implication for Lifetime Analysis: While this simplification is valid for short-term operational scheduling, it should be noted that neglecting self-discharge (and detailed capacity degradation) will result in an overestimation of the battery's true performance when extrapolating the operational strategy over the entire lifetime of the installation. Consequently, this simple model may lead to an underdimensioning of the required BESS capacity in a lifetime economic analysis.
\subsection{Inverter modeling}
Modeling the \textbf{inverter efficiency} in a \textbf{Mixed-Integer Linear Programming ($\text{MILP}$) model} is crucial for accurately calculating energy losses and finding the true economic optimum, especially for systems including Photovoltaic ($\text{PV}$) or Battery Energy Storage Systems ($\text{BESS}$). Since inverter efficiency is non-linear—it varies significantly with the input power load—you must use \textbf{linearization techniques} to keep the problem solvable with $\text{MILP}$.The most common and effective method is \textbf{Piecewise Linear ($\text{PWL}$) Approximation}.
\subsection*{PWL Approximation for Inverters}
The core idea is to approximate the smooth, non-linear efficiency curve of the inverter with a series of straight-line segments. This converts the overall non-linear problem into a solvable $\text{MILP}$ formulation by introducing auxiliary variables and constraints.
\subsubsection*{1. The Need for Linearization}
\begin{itemize}
\item \textbf{The Problem:} Inverter output power ($P_{ac}$) is the result of input DC power ($P_{dc}$) multiplied by efficiency ($\eta$): $P_{ac} = P_{dc} \cdot \eta(P_{dc})$. 
Since $\eta$ itself is a function of $P_{dc}$. Consider for means of example the PVwatt model \cite{Dobos2014PVWatts}.

\begin{equation}
\eta = \frac{\eta_{\text{nom}}}{\eta_{\text{ref}}} - 0.0162 \cdot \zeta - \frac{0.0059}{\zeta} + 0.9858 \quad \text{where} \quad \zeta = \frac{P_{\text{dc}}}{P_{\text{dc}0}} \quad \text{and} \quad P_{\text{dc}0} = \frac{P_{\text{ac}0}}{\eta_{\text{nom}}} 
\end{equation}

 this equation is \textbf{non-linear} and incompatible with linear programming ($\text{LP}$) and standard $\text{MILP}$ solvers.
 \begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{images/inverter effiency.png}
    \caption{Inverter Part-Load Efficiency. This graph illustrates how inverter efficiency varies with the load fraction, showing different nominal efficiencies and a comparison to PVWatts V1.}
    \label{fig:inverter_efficiency}
\end{figure}
 
 \item \textbf{The Trade-off:} Assuming a constant efficiency (a simple linear model) is fast but leads to modeling inaccuracies and suboptimal economic results. $\text{PWL}$ modeling offers a better balance between \textbf{accuracy} and \textbf{computational complexity}.\end{itemize}\subsubsection*{2. Implementation Steps}The \textbf{Piecewise Linear Approximation ($\text{PWL}$)} transforms the curve into a set of linear functions, typically using the \textbf{Multiple Choice/Convex Combination approach} (also known as the $\lambda$-method or Special Ordered Sets of Type 2, $\text{SOS}2$):\begin{enumerate}\item \textbf{Define Segments/Breakpoints:} The non-linear curve is divided into $N$ linear segments defined by $N+1$ known points (breakpoints) along the input power axis. The accuracy increases with the number of segments, but so does the computational time.\item \textbf{Introduce Binary Variables (for segments):} You assign auxiliary $\text{binary variables}$ ($\delta_s$) to identify which segment is active at any given time step.\item \textbf{Use Convex Combination ($\lambda$-method):} The actual power flow point on the curve is represented as a convex combination of the segment breakpoints. This uses additional continuous variables ($\lambda_i$) whose sum must equal 1.
 \begin{itemize}
 \item $P_{dc} = \sum_{i} \lambda_i P_{dc, i}$ (The actual power is a weighted average of the breakpoint powers).
 \item $\eta(P_{dc}) = \sum_{i} \lambda_i \eta_i$ (The calculated efficiency is a weighted average of the breakpoint efficiencies).
 \end{itemize}
 Constraints ensure that only two adjacent $\lambda$ values (or their corresponding segment binary variables) can be non-zero simultaneously, forcing the solution to lie on a defined segment.
 \end{enumerate}

\subsection{Part-Load Operation HTHP}
\label{sub:part_load_modeling}
\subsubsection{Inverter modulated model}
The performance of a heat pump at a given temperature depends on its operating regime, which in turn alters the effective coefficient of performance. The effective COP, denoted as \textbf{$\mathrm{COP}^{\star}(\Delta T)$}, is defined by \ref{eq:cop_star_delta_t_new} based on different operating conditions \cite{Rogeau2024HPPL}:

\begin{equation}
\label{eq:cop_star_delta_t_new}
\mathrm{COP}^{\star}(\Delta T) =
\begin{cases}
\mathrm{COP}^{\mathrm{FL}}(\Delta T_{\mathrm{HP}}), & \text{if } T_{\mathrm{out}} \le T_{\mathrm{biv}} \\
\mathrm{COP}^{\mathrm{PL}}(\Delta T_{\mathrm{HP}}), & \text{if } T_{\mathrm{biv}} \le T_{\mathrm{out}} \le T_{\mathrm{biv}, 2} \\
\mathrm{COP}^{0/1}(\Delta T), & \text{if } T_{\mathrm{out}} \ge T_{\mathrm{biv}, 2}
\end{cases}
\end{equation}

The core operational principle of modern high-temperature heat pumps (HTHPs), such as the Daikin EWYE-CZ, relies on inverter technology (Variable Frequency Drive or VFD) to modulate power and maintain efficiency at partial load conditions. While this modulating behavior leads to higher realistic performance ($\mathrm{COP}^{\mathrm{PL}}$) by minimizing efficiency losses associated with frequent on/off cycling, its full integration into a design-and-operation Mixed-Integer Linear Program (MILP) introduces a significant modeling challenge. Precisely modeling regimes such as $\mathrm{COP}^{\star}(\Delta T)$ which transition from full load ($\mathrm{COP}^{\mathrm{FL}}$) to inverter-controlled ($\mathrm{COP}^{\mathrm{PL}}$) and eventually to on/off cycles ($\mathrm{COP}^{0/1}$) requires defining the non-linear performance curve using Piecewise Linear (PWL) approximations or introducing a matrix of auxiliary binary decision variables to select the active operating segment at every time step of the simulation. This drastic increase in discrete variables would expand the combinatorial search space of the MILP exponentially, making the computational problem intractable and violating the requirement for a functional, fast optimization tool. Therefore, for the sake of model simplicity and computational feasibility, the HP operation for all regimes, including the inverter-modulated segment, is conservatively simplified to a single on/off (full-load) switching mechanism, acknowledging that this design choice inherently leads to an underestimation of the HP's true seasonal performance (SCOP).
\subsubsection{Simplified model}
The estimation of Part Load (PL) performance for the heat pump assumes adherence to the EN 14511: 2022 standard, as detailed in relevant literature \cite{Grassi2018HP}.
\begin{equation}\label{eq:pl}\mathrm{COP}{\mathrm{PL}}[-] = (1 - C{\text{d}}(1 - \text{PLR}))\mathrm{COP} \quad \text{with} \quad \mathrm{PLR}[-] = \frac{\min(\dot{Q}{\text{demand}}, \dot{Q}{\text{duty}})}{\dot{Q}_{\text{duty}}}\end{equation}In \cref{eq:pl} , $C_{\text{d}} (-)$ represents the degradation coefficient, assigned a value of $0.25$ for air-to-water HP systems \cite{Grassi2018HP}.The $\text{PLR} (-)$ variable quantifies the part load ratio by comparing the required heating output against the maximum possible theoretical output under prevailing weather conditions.This maximum output is termed the duty $\dot{Q}_{\text{duty}} (\text{W})$, while the load actually required by the building is the heat demand $\dot{Q}_{\text{demand}} (\text{Wh/h or W})$, which may also be the effective heat to be provided.The duty is derived from the Lorenz $\text{COP}$ and the nominal power use, as defined by the sizing and setpoint temperature in \cref{eq: duty}.\begin{equation}\label{eq: duty}\dot{Q}{\text{duty}}[\mathrm{W}] = \mathrm{COP}{\mathrm{Lorenz}}\frac{\dot{Q}{\mathrm{HP,nom}}}{\mathrm{COP}{\mathrm{nom}}}\end{equation}In \cref{eq: duty}, $\dot{Q}_{\text{HP,nom}} (\text{W})$ is the sizing parameter used for HPs and $\text{COP}_{\text{nom}} (-)$ represents the nominal $\text{COP}$ linked to the system's target setpoint temperature. Finally, the electrical power drawn by the HP and the net heat provided to the load are calculated using the duty cycle, as shown in \ref{eq: duty_final}.
\begin{equation}
\label{eq: duty_final}
P_{\mathrm{HP}}[\mathrm{W}] = \min\left(\frac{\dot{Q}{\mathrm{demand}}}{\mathrm{COP}{\mathrm{PL}}}, \frac{\dot{Q}{\mathrm{duty}}}{\mathrm{COP}{\mathrm{PL}}}\right) \quad \text{and} \quad \dot{Q}{\mathrm{HP}}[\mathrm{W}] = \mathrm{COP}{\mathrm{PL}}P_{\mathrm{HP}}
\end{equation}

\section{Conclusion}
The literature reviewed in this chapter establishes a robust methodological foundation for the design and operation of complex, hybrid energy systems.
As the building sector shifts toward decarbonization, it is evident that static, rule-of-thumb dimensioning standards (such as EN 15450) are insufficient for systems integrating High-Temperature Heat Pumps (HTHPs), Photovoltaics (PV), Battery Energy Storage Systems (BESS), and Thermal Energy Storage (TES).
Achieving both economic viability and grid stability requires the simultaneous optimization of component sizing and operational scheduling.

Several key takeaways emerge from the evaluation of energy system modeling methodologies:

\begin{itemize}
    \item \textbf{The Suitability of MILP:} Mixed-Integer Linear Programming (MILP) stands out as the most appropriate mathematical framework for this application.
    Unlike heuristic methods (e.g., Genetic Algorithms or Particle Swarm Optimization) that only offer near-optimal approximations, MILP guarantees a deterministic, globally optimal solution.
    It natively handles the combination of continuous variables (e.g., energy flows, state of charge) and discrete binary decisions (e.g., investment choices, on/off operational states) required for co-optimizing system design and dispatch.
    
    \item \textbf{Balancing Accuracy and Tractability:} The primary limitation of MILP is its strict requirement for linear constraints.
    However, the literature demonstrates that complex, non-linear thermodynamic behaviors can be successfully integrated without causing computational bottlenecks.
    Techniques such as Piecewise Linear (PWL) approximations for inverter efficiencies, stratified 2-zone models for TES, and Lorentz-based or simplified part-load COP models for HTHPs provide the necessary balance between physical realism and solver efficiency.
    
    \item \textbf{Evaluating Trade-offs:} Multi-objective optimization, specifically utilizing the $\epsilon$-constraint method, provides a vital mechanism for evaluating competing goals.
    It allows for the clear visualization of trade-offs between minimizing the Total Cost of Ownership (TCO) and reducing life-cycle CO$_2$ emissions via Pareto fronts.
\end{itemize}

Ultimately, these modeling strategies validate the approach taken in this thesis.
By utilizing a MILP-based framework with carefully selected component linearizations, the proposed design tool is well-equipped to navigate the complex interdependencies of hybrid energy systems.
This methodology will directly enable the cost-optimal and environmentally sound transition from gas boilers to electrified heating for commercial applications like the M-accent case study.





%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------

%---------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------
%----------------------------------------------------------------------------------

\chapter{Methodology}
\label{chapt3}

The preceding chapters established the problem context and the theoretical foundation for this work.
Chapter~\ref{intro} identified the core challenge: replacing the two 80\,kW gas boilers at the M-accent commercial building with an electrified hybrid system comprising a High-Temperature Heat Pump (HTHP), Photovoltaic (PV) array, Battery Energy Storage System (BESS), and Thermal Energy Storage (TES), while simultaneously minimising the Total Cost of Ownership (TCO).
Chapter~\ref{literature review} reviewed the available optimisation paradigms and concluded that Mixed-Integer Linear Programming (MILP) is the most appropriate framework for this application: it guarantees a globally optimal solution, natively handles the mixed continuous-and-discrete decision structure inherent to energy systems, and integrates seamlessly with the time-varying electricity tariffs that drive economic dispatch decisions.

Building on those foundations, this chapter presents the complete mathematical formulation of the MILP design tool developed in this thesis.
The model co-optimises two interrelated decision layers: the \textit{investment layer}, which determines the optimal installed capacities of the HTHP, PV array, BESS, and TES; and the \textit{operational layer}, which determines the cost-optimal hourly dispatch of every technology over a representative full calendar year (8\,760 time steps).
These two layers cannot be solved independently: the economic value of storage, for instance, depends on how it is operated, and the optimal operating strategy in turn depends on the installed capacity.
The MILP formulation resolves this coupling in a single unified optimisation problem.

The chapter is structured as follows.
Section~\ref{sec:data_inputs} describes the five input time-series that drive the model day-ahead electricity prices, solar irradiance, thermal heat demand, electrical load, and ambient temperature, together with the HTHP-specific pre-processing steps that convert the ambient temperature record into the hourly COP and capacity-fraction arrays used as fixed parameters by the solver.
Sections~\ref{sec:component_models} develops the linearised component models for each technology, detailing the physical constraints and the linearisation choices required for MILP compatibility.
Section~\ref{sec:energy_balance} states the system-level energy balance constraints that couple all components at every time step: an electrical balance ensuring that generation and storage always match the AC bus demand, and a thermal balance ensuring that heat pump output and TES discharge together satisfy the building heat demand.
Section~\ref{sec:econ_params} establishes the economic framework: the Capital Recovery Factor used to annualise investment costs, the linear-affine CAPEX model fitted to market data, and the full set of techno-economic parameters.
Section~\ref{sec:objective_function} defines the objective function, which minimises the annualised total system cost comprising CAPEX and OPEX.
Finally, Section~\ref{sec:solver_settings} documents the solver configuration and solution strategy.

% ============================================================
\section{Input Data and Pre-Processing}
\label{sec:data_inputs}
% ============================================================

The MILP model operates over a full calendar year discretised into $T = 8\,760$ hourly time steps, indexed $t \in \{1, \ldots, T\}$.
Five time-series datasets are required: the thermal heat demand $Q_\mathrm{dem}(t)$, the electrical demand $P_\mathrm{dem}(t)$, the in-plane solar irradiance $G(t)$ and ambient temperature $T_\mathrm{amb}(t)$, and the day-ahead electricity spot price $\lambda(t)$.
This section describes the origin, processing, and validation of each dataset.

% ────────────────────────────────────────────────────────────
\subsection{Thermal Heat Demand}
\label{subsec:thermal_demand}
% ────────────────────────────────────────────────────────────

\subsubsection*{Initial measurement approach and its limitations}

The building's thermal demand was first estimated by instrumenting the existing hydronic
heating circuit.
As shown in Figure~\ref{fig:pid_schematic}, two gas boilers feed a hydraulic separator
that decouples the primary boiler circuit from the secondary distribution loop.
Three signals were logged at sub-hourly resolution from the building management system:
supply temperature $T_\mathrm{sup}$ (sensor~3U2), return temperature $T_\mathrm{ret}$
(sensor~3U4), and the run status of secondary pump~26M1.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    thick, >=latex, font=\small,
    comp/.style    = {draw, rounded corners=4pt, fill=gray!12,
                      minimum width=2.6cm, minimum height=1.8cm, align=center},
    sensorT/.style = {draw, circle, fill=orange!25, minimum size=0.72cm,
                      inner sep=0pt, font=\footnotesize\bfseries},
    pumpM/.style   = {draw, circle, fill=cyan!20, minimum size=0.82cm,
                      inner sep=0pt, font=\footnotesize\bfseries},
    hot/.style     = {red!75!black,  line width=1.6pt},
    cold/.style    = {blue!70!black, line width=1.6pt},
    annot/.style   = {font=\footnotesize, align=center},
]

% ── y-levels ──────────────────────────────────────────────────
\def\ysup{1.2}    % supply pipe
\def\yret{-1.2}   % return pipe

% ── x-positions ───────────────────────────────────────────────
% boilers: 0  |  sep: 4.0  |  pump: 5.5  |  sensors: 7.5  |  buffer: 9.5  |  loads: 12.5

% ── BOILERS ───────────────────────────────────────────────────
\node[comp, minimum height=2.8cm] (boilers) at (0,0)
      {Gas Boilers\\$2\!\times\!80$\,kW};

% ── HYDRAULIC SEPARATOR ───────────────────────────────────────
\node[draw, fill=gray!12, minimum width=0.75cm, minimum height=3.2cm]
      (sep) at (4.0, 0) {};
\node[annot, below=6pt] at (sep.south) {Hydraulic\\separator};

% ── BUFFER VESSEL ─────────────────────────────────────────────
\node[draw, fill=gray!12, minimum width=0.75cm, minimum height=3.2cm]
      (buf) at (9.5, 0) {};
\node[annot, below=6pt] at (buf.south) {Buffer\\vessel};

% ── BUILDING LOADS ────────────────────────────────────────────
\node[comp, minimum width=3.0cm, minimum height=2.8cm] (loads) at (12.5,0)
      {Building\\heating zones};

% ── SUPPLY LINE (red, top) ────────────────────────────────────
% boiler → separator
\draw[hot, ->] (boilers.east |- 0,\ysup) -- (sep.center |- 0,\ysup);
% separator → sensor gap → buffer vessel
\draw[hot]     (sep.center |- 0,\ysup)   -- (7.1, \ysup);
\draw[hot]     (7.9, \ysup)              -- (buf.west |- 0,\ysup);
% buffer vessel → loads
\draw[hot, ->] (buf.east |- 0,\ysup)    -- (loads.west |- 0,\ysup);
\node[annot, above=3pt] at (2.0,\ysup) {\color{red!75!black}supply};

% ── RETURN LINE (blue, bottom) ────────────────────────────────
% loads → buffer vessel
\draw[cold]    (loads.west |- 0,\yret)  -- (buf.east |- 0,\yret);
% buffer vessel → sensor gap → separator
\draw[cold]    (buf.west |- 0,\yret)    -- (7.9, \yret);
\draw[cold, ->](7.1, \yret)             -- (sep.center |- 0,\yret);
% separator → boiler
\draw[cold, ->](sep.center |- 0,\yret) -- (boilers.east |- 0,\yret);
\node[annot, below=3pt] at (2.0,\yret) {\color{blue!70!black}return};

% ── PUMP 26M1  (supply side, just after separator) ────────────
\node[pumpM] (pump) at (5.5, \ysup) {\textbf{M}};
\draw (pump.north) -- ++(0, 0.55)
      node[above, annot, font=\footnotesize\bfseries] {26M1};

% ── SENSOR 3U2  (T_sup, supply, after pump) ───────────────────
\node[sensorT] (U2) at (7.5, \ysup) {T};
\draw (U2.north) -- ++(0, 0.45)
      node[above, annot, font=\footnotesize\bfseries] {3U2 \;$T_\mathrm{sup}$};

% ── SENSOR 3U4  (T_ret, return side) ─────────────────────────
\node[sensorT] (U4) at (7.5, \yret) {T};
\draw (U4.south) -- ++(0,-0.45)
      node[below, annot, font=\footnotesize\bfseries] {3U4 \;$T_\mathrm{ret}$};

% ── LEGEND  (2 × 2 grid below the diagram) ────────────────────
\begin{scope}[shift={(0,-3.6)}]
  % row 1
  \draw[hot,  ->] (0, 0.4) -- (0.7, 0.4)
        node[right, annot] {supply (hot)};
  \draw[cold, ->] (5.5, 0.4) -- (6.2, 0.4)
        node[right, annot] {return (cold)};
  % row 2
  \node[sensorT, scale=0.75] (Tleg) at (0.35, -0.35) {T};
  \node[right, annot] at (0.75, -0.35) {temperature sensor};
  \node[pumpM,  scale=0.75] (Mleg) at (5.85, -0.35) {M};
  \node[right, annot] at (6.25, -0.35) {circulation pump};
\end{scope}

\end{tikzpicture}
\caption{Simplified schematic of the M-accent Eeklo heating circuit (derived from the installation P\&ID).
         Pump~26M1 drives the secondary circuit; sensor~3U2 measures supply temperature $T_\mathrm{sup}$ downstream of the pump, and sensor~3U4 measures return temperature $T_\mathrm{ret}$ on the return pipe, both located between the hydraulic separator and the buffer vessel.
         Thermal power is computed as $\dot{Q} = \dot{m}\,c_p\,(T_\mathrm{sup}-T_\mathrm{ret})\cdot u_\mathrm{pump}$.}
\label{fig:pid_schematic}
\end{figure}

From these signals the instantaneous thermal power was computed as
\begin{equation}
    \dot{Q}(t) = \dot{m} \, c_p \, \bigl(T_\mathrm{sup}(t) - T_\mathrm{ret}(t)\bigr) \cdot u_\mathrm{pump}(t),
    \label{eq:Q_measured}
\end{equation}
where $\dot{m} = 2.22$\,kg/s is the nominal flow rate of 8\,m$^3$/h, $c_p = 4.186$\,kJ/(kg\,K),
and $u_\mathrm{pump} \in \{0,1\}$ is the pump state.
The temperature sensors were calibrated in a calibration oven across the full operating range,
and the flow rate was verified independently by the HVAC contractor Climanova and by computing
the theoretical flow from the measured pressure drop across a balancing valve; all methods
agreed with the 8\,m$^3$/h specification.

Despite this confirmed sensor accuracy, the resulting power series recovered only
21--36\% of the monthly energy expected from the historical gas consumption records
(seasons 2018/2019 and 2021/2022, $\approx 260$\,MWh/yr), as illustrated in
Figure~\ref{fig:heat_demand_monthly}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\linewidth]{images/thesis_heat_demand_monthly.png}
    \caption{Monthly heat demand comparison for the 2025/26 heating season at M-accent Eeklo:
             BDEW model (red) versus calorimetric measurements (blue).
             The measured values recover only 21--36\% of the model-estimated demand each month.}
    \label{fig:heat_demand_monthly}
\end{figure}

The 1\,000\,L buffer vessel located downstream of the sensors
(Figure~\ref{fig:pid_schematic}) could in principle cause a systematic error if the
sensors measure heat flowing into or out of storage rather than heat delivered to the
zones.
However, this effect is bounded: the buffer's maximum storage capacity is
$E_\mathrm{buf} = 1000 \times 4.186 \times 29\,\mathrm{kJ} \approx 33.7\,\mathrm{kWh}$,
compared to a typical winter day demand of $\approx 480\,\mathrm{kWh/day}$, giving a
worst-case daily error of $33.7/480 \approx 7\,\%$.
In practice, the buffer temperature varies by only a few kelvin per day, and over a
full 24-hour period the tank returns to approximately the same thermal state, so any
morning overestimation is offset by evening recovery.
A systematic 21--36\% deficit across full months cannot be explained by this mechanism.

A model-based approach anchored to the annual gas consumption records was therefore
adopted instead.
Figure~\ref{fig:heat_measurement_dec9} shows a representative day of raw measurements
and illustrates the unphysical power spikes caused by rapid pump on/off cycling.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.88\linewidth]{images/heat_measurement_dec9.png}
    \caption{Raw hydronic circuit measurements for 9 December 2025.
             \textit{Top:} supply and return temperatures ($T_\mathrm{sup}$, $T_\mathrm{ret}$).
             \textit{Middle:} binary pump run signal.
             \textit{Bottom:} calorimetric power via Eq.~\eqref{eq:Q_measured}.
             Unphysical spikes and negative values arise from rapid pump cycling and
             transient temperature mixing.}
    \label{fig:heat_measurement_dec9}
\end{figure}

\subsubsection*{BDEW synthetic profile methodology}

Given the absence of reliable on-site metering, the thermal heat demand profile was instead constructed using the standardised BDEW (Bundesverband der Energie- und Wasserwirtschaft) standard load profile methodology for non-residential buildings, implemented via the Python \texttt{demandlib} library \cite{demandlib}.
This approach is routinely applied in energy system studies when sub-hourly measurement data are unavailable or unreliable, and has been extended to Belgium through the \textit{when2heat} project \cite{Ruhnau2019}.

The methodology proceeds in three stages: (i) computation of a smoothed \textit{reference temperature} from local weather data, (ii) mapping of that reference temperature to a daily demand factor through the BDEW sigmoid function, and (iii) disaggregation of the daily factor to hourly resolution using a BGW lookup table that accounts for weekday/weekend and temperature-bin variations.

\paragraph{Reference temperature.}
To account for thermal inertia in the building fabric, the BDEW standard does not use the instantaneous daily mean temperature directly.
Instead, a weighted reference temperature $T_\mathrm{ref}$ is formed as
\begin{equation}
    T_\mathrm{ref}(d) = 0.50\,T_\mathrm{daily}(d)
                      + 0.25\,T_\mathrm{daily}(d-1)
                      + 0.125\,T_\mathrm{daily}(d-2)
                      + 0.125\,T_\mathrm{daily}(d-3),
    \label{eq:T_ref}
\end{equation}
where $T_\mathrm{daily}(d)$ is the 24-hour mean outdoor temperature on day $d$.
This four-day exponentially-weighted average damps the demand response to short temperature excursions and better reflects the delayed thermal response of commercial buildings.

Hourly outdoor temperatures were obtained from the Open-Meteo historical archive API ($T_\mathrm{2m}$ field) at the site coordinates (Eeklo, Belgium: $51.18^{\circ}$N, $3.55^{\circ}$E).

\paragraph{Belgium heating threshold correction.}
The BDEW sigmoid parameters were originally calibrated for Germany, where the space-heating activation threshold is approximately 13.98°C.
Belgium's threshold, as tabulated in Kozarcanin et al.\ \cite{Kozarcanin2019} and adopted by when2heat, is 15.2°C.
To remove this systematic bias, the reference temperature series is uniformly shifted by
\begin{equation}
    \Delta T_\mathrm{BE} = 15.2 - 13.98 = +1.22\,\text{K}
    \label{eq:T_offset}
\end{equation}
before entering the sigmoid function.

\paragraph{Daily demand factor.}
The adjusted reference temperature is mapped to a dimensionless daily demand factor $h(d)$ through the BDEW sigmoid:
\begin{equation}
    h(d) = \frac{A}{1 + \left(\dfrac{B}{T_\mathrm{ref}(d) - 40}\right)^C} + D,
    \label{eq:bdew_sigmoid}
\end{equation}
where the parameters $A$, $B$, $C$, and $D$ are tabulated by the BDEW for each building type, windiness class, and building energy standard.
For this study the building type \textit{GKO} (Kaufhaus, i.e.\ retail/commercial shop) and wind class~1 (exposed, representative for the Belgian coastal climate) were selected, with building class~0 as required for non-residential building types.
The sigmoid saturates at low temperatures (high demand) and approaches zero for $T_\mathrm{ref} \gtrsim 15^\circ$C, reflecting the near-zero space-heating demand during summer.

\paragraph{Hourly disaggregation.}
Each day's demand factor $h(d)$ is distributed across 24 hours using the BGW hourly profile table.
This table provides dimensionless shape factors indexed by weekday (Monday through Sunday), hour of day (0--23), and temperature bin ($-15, -10, \ldots, 25, 30$°C).
Linear interpolation between the two nearest temperature bins is applied for each hour.
The product $h(d) \times f_\mathrm{BGW}(\text{weekday}, \text{hour}, T_\mathrm{ref}) \times 24$ yields a raw hourly profile that, when summed over the year, is normalised to unity and subsequently scaled to the known annual thermal energy demand.

\paragraph{Annual demand scaling.}
The M-accent building historically consumed approximately 260\,MWh/year for space heating, based on gas billing records.
This figure serves as the annual scaling constant; each normalised hourly profile value is multiplied by 260\,MWh to obtain the final hourly demand series $Q_\mathrm{dem}(t)$ in kWh.

\subsubsection*{Validation}

The generated BDEW profile was cross-validated against the historical gas consumption records for the seasons 2018/2019 and 2021/2022, and against the partial calorimetric measurement series for December 2025 -- March 2026 (used as a qualitative reference only, given the limitations above).
Monthly aggregates from the BDEW profile were found to be consistent with the gas records, confirming that the synthetic profile correctly reproduces the seasonal distribution of heating demand for this building type and climate.


% ────────────────────────────────────────────────────────────
\subsection{Day-Ahead Electricity Prices}
\label{subsec:price_data}
% ────────────────────────────────────────────────────────────

The hourly day-ahead electricity price series $\lambda(t)$ [\euro/kWh] is retrieved from the
\textbf{ENTSO-E Transparency Platform} via the \texttt{entsoe-py} Python client
\cite{entsoe-py}, which provides a REST API wrapper around the official ENTSO-E data feed.

\paragraph{Retrieval procedure.}
Prices are queried for the Belgian bidding zone (area code \texttt{BE}), with all
timestamps anchored to the \texttt{Europe/Brussels} timezone to correctly handle
daylight-saving transitions.
To avoid API timeouts the full year is fetched in \textbf{monthly chunks}; each chunk is
retried up to three times with an exponential back-off before a \texttt{NaN} placeholder
is inserted to keep the index continuous.
The ENTSO-E platform publishes prices in \euro/MWh at up to 15-minute resolution;
these are resampled to an hourly mean and converted to \euro/kWh by dividing by
1\,000, yielding a 8\,760-element array aligned with the model's hourly time steps.
Any residual gaps (DST clock changes, occasional missing quarter-hours) are
forward-filled and then back-filled with a limit of three hours.

\paragraph{Year selection and price scaling.}
The baseline optimisation uses the 2023 price series.
For the sensitivity analysis a \texttt{price\_multiplier} parameter scales the entire
series uniformly, allowing the full range of plausible future price levels to be
explored without re-fetching data (see Table~\ref{tab:sens_params}).

\paragraph{Consumer tariff structure.}
The ENTSO-E spot prices are converted to consumer buy and sell prices using the
Ecopower \textit{Dynamische burgerstroom} tariff (valid from 1 January 2026,
\cite{Ecopower2026tariff}; reproduced in Appendix~\ref{appendix:tariefkaart}) and
the Fluvius Imewo distribution tariffs applicable to the Eeklo grid area.

\paragraph{Capacity network tariff.}
The Fluvius Imewo capacity tariff is published as 54.20\,\euro/kW/year, billed monthly
on the highest 15-minute average grid-import peak recorded each calendar month.
Dividing by twelve gives 4.52\,\euro/kW/month, which is rounded to
$c_\mathrm{cap} = 5.0$\,\euro/kW/month in the model.
This tariff creates a strong incentive for the MILP to smooth and reduce peak grid
imports, directly motivating investment in BESS and TES for peak shaving.

\paragraph{Fixed annual connection cost.}
The 200\,\euro/yr fixed cost is the sum of three standing charges that apply regardless
of consumption: the Fluvius Imewo data-management fee (17.85\,\euro/yr), the Ecopower
subscription fee (5.00\,\euro/month $\times$ 12 = 60\,\euro/yr), and the Flemish
Energy Fund contribution applicable to commercial connections
(10.07\,\euro/month $\times$ 12 = 120.84\,\euro/yr), totalling
$17.85 + 60 + 120.84 \approx 200$\,\euro/yr.

\paragraph{Electricity buy price and injection revenue.}
The Ecopower dynamic product links the consumer buy price directly to the
EPEX\,DA quarter-hourly spot price:
\begin{equation}
    \lambda_\mathrm{buy}(t) = 0.00102\,\lambda_\mathrm{EPEX}(t) + c_\mathrm{markup},
    \label{eq:buy_price}
\end{equation}
where $c_\mathrm{markup}$ aggregates the fixed per-kWh surcharges: the Ecopower
service margin (0.004\,\euro/kWh), the green-certificate levy (GSC, 0.011\,\euro/kWh),
the combined-heat-and-power levy (WKK, 0.00392\,\euro/kWh), the Fluvius Imewo
distribution tariff (0.05229\,\euro/kWh), the federal energy contribution
(0.00193\,\euro/kWh), and the special excise duty applicable to commercial consumers
in the 50\,000--1\,000\,000\,kWh band (0.01139\,\euro/kWh).
For PV export the feed-in compensation follows
$\lambda_\mathrm{sell}(t) = 0.00098\,\lambda_\mathrm{EPEX}(t) - 0.015$\,\euro/kWh,
reduced by the Fluvius Imewo injection tariff of 0.00175\,\euro/kWh levied on
inverter systems with rated power above 10\,kVA.
In the MILP, $\lambda_\mathrm{EPEX}(t)$ is the ENTSO-E Belgian day-ahead price
series retrieved as described above, and 21\,\% VAT is applied to all operational
cost terms.

\paragraph{Cross-validation of the all-in price.}
The ENTSO-E API returns only the wholesale energy component; the full consumer price
is obtained by stacking all per-kWh surcharges on top.
Using the mean 2023 EPEX\,DA price of 97.27\,\euro/MWh derived from the fetched
price series, the all-in buy price is reconstructed as follows:

\begin{equation}
\lambda_\mathrm{all{-}in} =
  \underbrace{0.00102 \times 97.27 + 0.004}_{\text{Ecopower energy: }0.1032}
+ \underbrace{0.011 + 0.00392 + 0.05229 + 0.00193 + 0.01139}_{\text{fixed surcharges: }0.0805}
= 0.1837\;\text{\euro/kWh (excl.\ VAT)},
\label{eq:allin_check}
\end{equation}

\noindent which gives $0.1837 \times 1.21 = 0.222$\,\euro/kWh including 21\,\% VAT\@.
Eurostat reports the average non-household electricity price for Belgium in H1\,2023
at approximately 0.253\,\euro/kWh for the small-commercial consumer band
(20--500\,MWh/yr, band\,IB)~\cite{Eurostat_nrg_pc_205}, which corresponds to
the M-accent building's annual consumption of approximately 200\,MWh/yr.
The reconstructed all-in price of 0.222\,\euro/kWh lies about 12\,\% below this
reference, which is consistent with the nature of the Ecopower dynamic product:
the Eurostat band\,IB figure averages across all Belgian suppliers and tariff types
including fixed-price contracts, which in 2023 still reflected the elevated 2022
energy-crisis prices. The dynamic Ecopower tariff, by contrast, tracks real-time
EPEX\,DA prices, which fell sharply in 2023; a lower all-in price relative to the
Eurostat average is therefore expected.

% ────────────────────────────────────────────────────────────

% ────────────────────────────────────────────────────────────
\subsection{Solar Irradiance Pre-Processing}
\label{subsec:pv_irradiance}
% ────────────────────────────────────────────────────────────

The pre-processing step produces a normalised hourly specific yield:
\begin{equation}
    I_\mathrm{sol}(t) \quad [\mathrm{kW/kW_p}], \qquad t = 1, \ldots, 8760,
    \label{eq:pv_sol_output}
\end{equation}
which is stored as a fixed parameter array before the optimisation begins,
keeping every MILP constraint in which it appears linear.

\subsubsection*{Why not PVGIS}

The European Commission's PVGIS API~\cite{Huld2012PVGIS} was the natural first
candidate: it provides a free, well-validated hourly AC power series from satellite
climatology and has been shown to achieve the lowest annual error among 14 widely used
PV simulation tools~\cite{Milosavljevic2022PVtools}.
However, PVGIS currently covers only up to 2023, whereas the scenario analysis
in this thesis requires solar profiles for 2024 and 2025.
This alignment is essential: because large-scale PV deployment across Europe has
substantially increased the frequency of near-zero and negative day-ahead electricity
prices in recent years, using a mismatched solar-year would distort the co-optimisation
of PV sizing and price-driven dispatch.
PVGIS was therefore set aside in favour of a pipeline that can deliver up-to-date data
for any target year.
Figure~\ref{fig:low_price_hours} illustrates the trend: the number of hours per year
with a Belgian day-ahead spot price below 30\,\euro/MWh has risen sharply since 2021,
confirming that year-specific price alignment is material to the optimisation results.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/low_price_hours.png}
    \caption{Number of hours per year with a Belgian day-ahead spot price below
             30\,\euro/MWh (ENTSO-E transparency platform, Belgium bidding zone).
             The pronounced increase from 2021 onwards reflects the rapid growth in
             renewable generation capacity across the Central Western Europe price zone.}
    \label{fig:low_price_hours}
\end{figure}

\subsubsection*{Open-Meteo + pvlib pipeline}

Hourly irradiance components (GHI, DNI, DHI), ambient temperature, and 10\,m wind speed
are retrieved from the Open-Meteo historical archive API for the building site
($51.18^{\circ}$N, $3.55^{\circ}$E).
Open-Meteo is a free, open-source reanalysis product that combines ERA5 and
high-resolution numerical weather prediction (NWP) model output and requires no API key,
making it straightforward to obtain consistent multi-year time series.
The archive provides hourly data at roughly 5--11\,km spatial resolution and is
available for any historical year up to the present.

Plane-of-array (POA) irradiance is then computed locally with \texttt{pvlib} using the
Hay--Davies transposition model~\cite{Hay1980} for a fixed tilt of $35^{\circ}$
(the widely cited energy-yield optimum for the Belgian latitude of $51^{\circ}$\,N~\cite{Jacobson2018tilt},
feasible because the building has a flat roof) and an azimuth of $190^{\circ}$
(10$^{\circ}$ west of south), read from a Google Maps satellite view of the M-accent roof.
The Hay--Davies model computes the total plane-of-array irradiance as the sum of three
components:
\begin{equation}
    G_\mathrm{POA}(t) = E_\mathrm{beam}(t) + E_\mathrm{diff}(t) + E_\mathrm{refl}(t).
    \label{eq:poa_total}
\end{equation}
The beam component is projected onto the tilted surface using the ratio $R_b$ of the
cosine of the angle of incidence $\theta_\mathrm{AOI}$ to the cosine of the solar
zenith angle $Z$:
\begin{equation}
    E_\mathrm{beam}(t) = \mathrm{DNI}(t)\cdot R_b(t),
    \qquad R_b = \frac{\cos\theta_\mathrm{AOI}}{\cos Z}.
\end{equation}
The sky diffuse component is split into a circumsolar fraction (treated as originating
from the solar disk) and an isotropic background integrated over the visible sky dome,
weighted by the anisotropy index $A_i = \mathrm{DNI}/E_a$, where $E_a$ is the
extraterrestrial irradiance, i.e.\ the solar irradiance incident on a surface
perpendicular to the sun's rays at the top of the atmosphere, which varies
seasonally as the Earth--Sun distance changes (from roughly
$1\,322\,\mathrm{W\,m}^{-2}$ in July to $1\,413\,\mathrm{W\,m}^{-2}$ in January)
and is computed by \texttt{pvlib} following Spencer~(1971):
\begin{equation}
    E_\mathrm{diff}(t) = \mathrm{DHI}(t)\left[
        A_i(t)\,R_b(t)
        + \bigl(1 - A_i(t)\bigr)\frac{1+\cos\beta}{2}
    \right].
    \label{eq:haydavies_diff}
\end{equation}
Ground-reflected irradiance is added using a fixed albedo $\rho$:
\begin{equation}
    E_\mathrm{refl}(t) = \mathrm{GHI}(t)\cdot\rho\cdot\frac{1-\cos\beta}{2}.
\end{equation}
When $A_i\to0$ (overcast sky, no direct beam) the diffuse term reduces to the
isotropic model; when $A_i\to1$ all diffuse is concentrated around the sun.
This anisotropic treatment improves accuracy over the pure isotropic model at the cost
of one additional input (DNI), which Open-Meteo provides directly.
Solar positions are evaluated at the midpoint of each hour to reduce numerical
integration error.
The normalised specific yield is obtained from:
\begin{equation}
    I_\mathrm{sol}(t) = \frac{G_\mathrm{POA}(t)}{1000} \cdot \eta_\mathrm{temp}(t) \cdot (1 - \ell),
\end{equation}
where $G_\mathrm{POA}(t)$ is the plane-of-array irradiance in W\,m$^{-2}$,
$\eta_\mathrm{temp}(t)$ is the PVWatts temperature correction factor, and
$\ell$ is a fixed loss factor covering wiring, shading, soiling, and DC-to-AC
conversion.
The temperature correction follows the PVWatts model:
\begin{equation}
    \eta_\mathrm{temp}(t) = 1 + \gamma_\mathrm{pdc}\,\bigl(T_\mathrm{cell}(t) - T_\mathrm{ref}\bigr),
    \quad \gamma_\mathrm{pdc} = -0.004\,^{\circ}\mathrm{C}^{-1},
    \quad T_\mathrm{ref} = 25\,^{\circ}\mathrm{C},
    \label{eq:pv_temp_correction}
\end{equation}
where the cell temperature $T_\mathrm{cell}(t)$ is pre-computed in the
pre-processing pipeline using the Faiman~\cite{Faiman2008} model:
\begin{equation}
    T_\mathrm{cell}(t) = T_\mathrm{amb}(t)
        + \frac{G_\mathrm{POA}(t)}{u_0 + u_1\,v_\mathrm{wind}(t)},
    \quad u_0 = 25\,\mathrm{W\,m^{-2}\,K^{-1}},\;
           u_1 = 6.84\,\mathrm{W\,s\,m^{-3}\,K^{-1}},
    \label{eq:faiman}
\end{equation}
with $T_\mathrm{amb}(t)$ and $v_\mathrm{wind}(t)$ (10\,m wind speed) both supplied
by the Open-Meteo fetch.
Because $T_\mathrm{cell}(t)$ is computed entirely from known climate inputs before
the optimisation runs, $\eta_\mathrm{temp}(t)$ is a fixed scalar at each time step,
keeping the constraint $P_\mathrm{PV}(t) = C_\mathrm{PV}\cdot I_\mathrm{sol}(t)$
linear in the sizing variable $C_\mathrm{PV}$.

\subsubsection*{Loss calibration against 2025 measured data}

The loss factor $\ell$ was calibrated against the 15-minute sub-meter production
data of the existing 34.63\,kW$_\mathrm{p}$ rooftop system for the full year 2025.
With the updated PVWatts\,+\,Faiman model (Eqs.~\eqref{eq:pv_temp_correction}
and~\eqref{eq:faiman}), temperature derating is now captured explicitly by
$\eta_\mathrm{temp}(t)$, so $\ell$ covers only the remaining fixed losses:
wiring resistance, inter-row shading, soiling, and DC-to-AC inverter losses.
Irradiance components (GHI, DNI, DHI), ambient temperature, and 10\,m wind speed
were obtained from the Open-Meteo historical archive for the site coordinates.
The factor was found by bisection so that the simulated annual total matched
the measured 2025 total of 37\,747\,kWh, yielding $\ell = 28.14\,\%$.
The NREL PVWatts\,v5 documentation~\cite{Dobos2014PVWatts} identifies the
principal fixed-loss categories for grid-connected rooftop systems and quotes
the following typical ranges: DC wiring resistance ($1$--$3\,\%$), soiling
($0$--$5\,\%$, lower in climates with frequent rainfall such as Belgium), inter-row
shading ($0$--$5\,\%$), and DC-to-AC inverter losses ($2$--$4\,\%$), giving an
aggregate physical loss budget of roughly $5$--$17\,\%$.
The calibrated $\ell = 28.14\,\%$ exceeds this physical range primarily because the
fixed inter-row shading term in the model does not capture the strongly seasonal
nature of near-horizon shading at this latitude.
At $51^{\circ}$\,N the solar elevation at solar noon falls below $20^{\circ}$ in
December, meaning that surrounding buildings and rooftop obstacles that cast no shadow
in summer can block a significant fraction of the incoming beam in winter.
This effect manifests as a pronounced positive bias in the out-of-sample validation
(up to $+80\,\%$ in December; see Section below), and $\ell$ absorbs its
annual-average impact so that the total energy balance remains correct over the full year.

\subsubsection*{Out-of-sample validation against 2024 measured data}

To confirm that the calibrated pipeline generalises beyond the year used for fitting,
the loss-corrected model is applied to 2024, a fully independent year, and
compared against the corresponding 15-minute sub-meter measurements
(Figures~\ref{fig:openmeteo_daily_2024} and~\ref{fig:openmeteo_monthly_2024}).

Over the full year 2024 the model simulates 32.31\,MWh against a measured total of
31.78\,MWh, an annual overestimation of $+1.7\,\%$.
The goodness of fit is quantified by two standard metrics computed from the $N = 8782$
aligned hourly observations $(\hat{P}_t,\, P_t^\mathrm{meas})$:

\begin{equation}
    \mathrm{RMSE} = \sqrt{\frac{1}{N}\sum_{t=1}^{N}\!\left(\hat{P}_t - P_t^\mathrm{meas}\right)^2},
    \label{eq:rmse}
\end{equation}

\begin{equation}
    R^2 = 1 - \frac{\displaystyle\sum_{t=1}^{N}\!\left(\hat{P}_t - P_t^\mathrm{meas}\right)^2}
                   {\displaystyle\sum_{t=1}^{N}\!\left(P_t^\mathrm{meas} - \bar{P}^\mathrm{meas}\right)^2},
    \label{eq:r2}
\end{equation}

\noindent where $\hat{P}_t$ is the simulated hourly power, $P_t^\mathrm{meas}$ the
measured hourly power, and $\bar{P}^\mathrm{meas}$ their mean.
Applying these to the 2024 data yields $R^2 = 0.789$ and
$\mathrm{RMSE} = 2.86\,\mathrm{kW}$.
$R^2$ measures how much of the variance in the measured hourly output is explained by
the model: a value close to one means the simulated and measured profiles rise and fall
together, the model captures the right days as sunny or overcast, and the right hours
as peak or off-peak.
The RMSE quantifies the typical hour-by-hour deviation in absolute power terms: at
$2.86\,\mathrm{kW}$ on a $34.63\,\mathrm{kW_p}$ system this corresponds to roughly
$8\,\%$ of peak capacity, reflecting the coarser spatial resolution of the Open-Meteo
reanalysis relative to site-specific satellite products.

Monthly breakdown (Figure~\ref{fig:openmeteo_monthly_2024}) reveals a pronounced
seasonal bias pattern: summer months (April--August) carry a negative bias of
$1$--$12\,\%$ (model underestimates), while winter months show a strong positive
overestimation of up to $+80\,\%$ in December and November.
This seasonal asymmetry is a known characteristic of ERA5-based reanalysis products at
northern latitudes, where low solar elevation angles and frequent cloud and fog events
are difficult to reproduce at coarse spatial resolution.
Because the winter months contribute only a small fraction of the annual yield, the
annual error remains low ($+1.7\,\%$), and the bias has a limited effect on the
optimisation outcomes.

Figure~\ref{fig:openmeteo_daily_profiles_2024} overlays the simulated and measured intraday
power profiles for one representative day per month.
In the summer months the hourly Open-Meteo profile tracks the smooth bell-shaped generation
curve reasonably well.
In the winter months the model correctly identifies the midday generation window but
overestimates the magnitude, consistent with the positive winter bias noted above.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/openmeteo_2024_daily_timeseries.pdf}
    \caption{Full-year daily PV energy comparison for 2024: Open-Meteo + pvlib simulated (orange)
             versus 15-minute sub-meter measurements (blue) for the
             34.63\,kW$_\mathrm{p}$ rooftop system.
             The inset reports the annual totals, overall bias, and goodness-of-fit
             statistics ($R^2 = 0.789$, RMSE $= 2.86\,\mathrm{kW}$).}
    \label{fig:openmeteo_daily_2024}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/openmeteo_2024_monthly_bar.pdf}
    \caption{Monthly PV energy totals for 2024: Open-Meteo + pvlib simulated (orange) versus
             measured (blue).
             Percentage labels show the monthly bias (simulated $-$ measured).
             The model was calibrated on 2025 data ($\ell = 28.14\,\%$); this comparison
             constitutes an out-of-sample validation on the preceding year.}
    \label{fig:openmeteo_monthly_2024}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/daily_openmeteo_vs_measured_2024.png}
    \caption{Intraday power profiles for one representative day per month (2024).
             Blue: measured 15-minute power (kW); orange: Open-Meteo + pvlib hourly simulation.
             Daily energy totals are annotated in each panel.
             Summer days show good shape agreement; winter days reflect the coarser
             spatial resolution of the reanalysis product relative to site conditions.}
    \label{fig:openmeteo_daily_profiles_2024}
\end{figure}

% ────────────────────────────────────────────────────────────
\subsection{HTHP Time-Series Pre-Processing}
\label{sub:hthp_preprocess}
% ────────────────────────────────────────────────────────────

The HTHP model requires two ambient-temperature-dependent parameter arrays that must
be computed from climate data before the optimisation begins: the hourly COP time
series and the hourly capacity-fraction time series.
Both are derived from the same ambient temperature vector $T_\mathrm{amb}(t)$ supplied
by the Open-Meteo\,+\,pvlib pipeline (Section~\ref{subsec:pv_irradiance}) and are passed
to the model as fixed scalar parameters, keeping every MILP constraint linear.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Pre-Processing Step 1: Lorentz COP Time Series}
\label{sub:hthp_cop_preprocess}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

As established in Section~\ref{sub:lorentz_model}, the Lorentz model expresses the ideal COP
of a heat pump in terms of the log-mean temperatures on the condenser (sink) side and the
evaporator (source) side.
With a fixed supply temperature $T_\mathrm{sup}$ [$^{\circ}$C] and fixed return temperature
$T_\mathrm{ret}$ [$^{\circ}$C], the log-mean condenser temperature is a constant.
For the M-accent system, $T_\mathrm{sup} = 60\,^{\circ}$C is taken directly from the
measured heat demand data (Section~\ref{subsec:thermal_demand}), which shows a consistent
supply temperature of 60\,°C.
The return temperature is then $T_\mathrm{ret} = T_\mathrm{sup} - \Delta T_\mathrm{HC}
= 60 - 20 = 40\,^{\circ}$C, where $\Delta T_\mathrm{HC} = 20\,\mathrm{K}$ is the
design temperature drop across the radiators as specified in the M-accent P\&ID.
These values yield $T_\mathrm{h,lm} \approx 323\,\mathrm{K}$:

\begin{equation}
    T_{\mathrm{h,lm}} =
    \frac{T_\mathrm{sup} - T_\mathrm{ret}}
         {\ln\!\left(\dfrac{T_\mathrm{sup}+273.15}{T_\mathrm{ret}+273.15}\right)}
    \quad [\mathrm{K}].
    \label{eq:T_hlm}
\end{equation}

The heat source is outdoor air at the hourly ambient temperature $T_\mathrm{amb}(t)$
[$^{\circ}$C], converted to Kelvin as $T_\mathrm{src}(t) = T_\mathrm{amb}(t)+273.15$~[K].
The ideal Lorentz COP at each hour is therefore:

\begin{equation}
    \mathrm{COP}_\mathrm{Lorenz}(t)
    = \frac{T_{\mathrm{h,lm}}}{T_{\mathrm{h,lm}} - T_\mathrm{src}(t)}.
    \label{eq:cop_lorenz_t}
\end{equation}

The actual COP is obtained by scaling with the Lorentz efficiency factor
$\eta_\mathrm{HP}$, calibrated from manufacturer performance data
(Appendix~\ref{appendix:datasheet_hthp}) at the standard EN\,14511 reference operating
point A7/W40--45: ambient air at $T_\mathrm{amb} = 7\,^{\circ}$C, condenser return at
$T_\mathrm{ret} = 40\,^{\circ}$C, and condenser supply at $T_\mathrm{sup} = 45\,^{\circ}$C:

\begin{equation}
    \mathrm{COP}(t) = \eta_\mathrm{HP} \cdot \mathrm{COP}_\mathrm{Lorenz}(t),
    \qquad \eta_\mathrm{HP} = 0.361.
    \label{eq:cop_actual_t}
\end{equation}

\noindent Because both $T_\mathrm{h,lm}$ and $\eta_\mathrm{HP}$ are constants, and
$T_\mathrm{amb}(t)$ is a known input time series, $\mathrm{COP}(t)$ is fully determined
before the optimisation begins.
It is passed to the model as a \emph{scalar parameter} at each time step, making every
constraint in which it appears linear.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Pre-Processing Step 2: Capacity-Fraction Linear Regression}
\label{sub:hthp_capfrac}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

At low ambient temperatures an air-source heat pump cannot deliver its rated thermal output:
the refrigerant mass-flow rate decreases as the evaporator pressure drops.
This effect is \emph{separate} from the COP variation and must be modelled independently as
a reduction in the \emph{available} capacity.

A \textbf{linear regression} is fitted to the heating-capacity data extracted from the
Daikin EWYE-CZ manufacturer simulation at full load and a fixed condenser leaving water
temperature of $55\,^{\circ}$C (eight points from $-15\,^{\circ}$C to $+7\,^{\circ}$C
plus one high-temperature point at $+20\,^{\circ}$C).
The regression is forced through the reference point $(T_\mathrm{ref} = 7\,^{\circ}\mathrm{C},\;
\kappa = 1.0)$, yielding:

\begin{equation}
    \kappa(t) = 1 + k \bigl(T_\mathrm{amb}(t) - T_\mathrm{ref}\bigr),
    \quad k = 0.02106\,^{\circ}\mathrm{C}^{-1},\quad T_\mathrm{ref} = 7\,^{\circ}\mathrm{C},
    \label{eq:capfrac}
\end{equation}

\noindent clipped to the manufacturer operating envelope:
$\kappa(t) \in [0.537,\; 1.274]$ (corresponding to
$T_\mathrm{amb} \in [-15\,^{\circ}\mathrm{C},\; +20\,^{\circ}\mathrm{C}]$).
The fit achieves $R^2 = 0.997$, meaning the linear model explains 99.7\,\% of the
variance in the manufacturer capacity data across the operating temperature range,
confirming that the capacity derating is well described by a linear function of ambient
temperature over the relevant operating envelope.
Like $\mathrm{COP}(t)$, the array $\kappa(t)$ is pre-computed and enters the MILP
as a fixed coefficient.

\noindent \textbf{Physical interpretation.}
A value $\kappa < 1$ means the unit can deliver less than its nameplate output at cold
conditions; a value $\kappa > 1$ means it can slightly exceed nameplate at mild conditions.
The effective weather-derated capacity at any hour is therefore
$Q_\mathrm{hp}^{*} \cdot \kappa(t)$ [kW].
\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\linewidth]{images/heat_pump_performance.png}
    \caption{Heat pump performance characteristics used for the HTHP model. Left: fitted capacity-fraction vs ambient temperature from manufacturer simulation data, which yields the slope $k = 0.02106\,^{\circ}\mathrm{C}^{-1}$ and the clipped operating envelope. Right: part-load performance illustrating the degradation coefficient $C_d$ used to represent fixed auxiliary electrical overhead at part-load.}
    \label{fig:hthp_performance}
\end{figure}

% ============================================================

\section{Component Models}
\label{sec:component_models}
% ============================================================

This section presents the linearised mathematical models for each technology in the candidate system.
All models are formulated as linear equality and inequality constraints suitable for direct embedding in the MILP problem.

% ------------------------------------------------------------
\subsection{High-Temperature Heat Pump (HTHP)}
\label{sec:hthp_model}
% ------------------------------------------------------------

The High-Temperature Heat Pump (HTHP) is the primary source of thermal energy in the system,
converting electrical power into heat at supply temperatures up to $75\,^{\circ}\mathrm{C}$,
making it suitable for the existing radiator circuit of the M-accent building without requiring
hydronic renovation.
The specific unit modelled is the Daikin EWYE-CZ air-to-water heat pump; its full manufacturer
datasheet is shown in Appendix~\ref{appendix:datasheet_hthp}.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Governing Model Equations}
\label{sub:hthp_equations}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Three equations govern the HTHP model.
The first two capture ambient-temperature-dependent effects and are fully derived in
Section~\ref{sub:hthp_preprocess}; they are reproduced here for reference.
The thermodynamic efficiency is described by the Lorentz-calibrated COP
(Eq.~\eqref{eq:cop_actual_t}):

\begin{equation}
    \mathrm{COP}(t) = \eta_\mathrm{HP} \cdot \mathrm{COP}_\mathrm{Lorenz}(t),
    \qquad \eta_\mathrm{HP} = 0.361. \tag{\ref{eq:cop_actual_t}}
\end{equation}

\noindent The weather-dependent capacity derating is described by the linear regression
fit to manufacturer data (Eq.~\eqref{eq:capfrac}):

\begin{equation}
    \kappa(t) = 1 + k\bigl(T_\mathrm{amb}(t) - T_\mathrm{ref}\bigr),
    \quad k = 0.02106\,^{\circ}\mathrm{C}^{-1}, \tag{\ref{eq:capfrac}}
\end{equation}

\noindent so that the available thermal capacity at time step $t$ is
$Q_\mathrm{hp}^{*}\cdot\kappa(t)$ [kW$_\mathrm{th}$].
The third equation links electrical power consumption to thermal output.
The heat pump is inverter-driven, so fixed auxiliary consumers (circulation pump,
controls, fans) are active whenever it runs, independently of compressor modulation.
Following the EN~14511 part-load degradation formalism
(Section~\ref{sub:part_load_modeling}), the electrical draw is split into a fixed
fraction and a variable fraction:

\begin{equation}
    P_\mathrm{el,HP}(t)\cdot\mathrm{COP}(t)
    = C_\mathrm{d}\,\tilde{Q}_\mathrm{hp}(t)\cdot\kappa(t)
      + \bigl(1-C_\mathrm{d}\bigr)\,Q_\mathrm{HP}(t),
    \quad C_\mathrm{d} = 0.10.
    \label{eq:hthp_power}
\end{equation}

\noindent At full load this reduces to the ideal relation
$P_\mathrm{el,HP} = Q_\mathrm{HP}/\mathrm{COP}$; at part load the $C_\mathrm{d}$
term adds a modest overhead that slightly reduces the effective part-load COP.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Fixed Parameter Arrays and Inputs}
\label{sub:hthp_params}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

All fixed coefficients embedded in the HTHP constraints are listed in
Table~\ref{tab:hthp_inputs}.
Because $\mathrm{COP}(t)$ and $\kappa(t)$ are pre-computed from known climate data
before the optimisation begins, every constraint in which they appear is linear in the
decision variables.

\begin{table}[H]
\centering
\caption{Fixed parameters and pre-computed input arrays for the HTHP MILP model.}
\label{tab:hthp_inputs}
\begin{tabular}{lllp{5cm}}
\hline
\textbf{Symbol} & \textbf{Value / Type} & \textbf{Source} & \textbf{Role in model} \\
\hline
$\mathrm{COP}(t)$ & hourly array & Eq.~\eqref{eq:cop_actual_t}, Section~\ref{sub:hthp_cop_preprocess}
    & Thermodynamic efficiency \\
$\kappa(t)$ & hourly array & Eq.~\eqref{eq:capfrac}, Section~\ref{sub:hthp_capfrac}
    & Weather-capacity derating factor \\
$f_\mathrm{min}$ & 0.222 & Manufacturer data
    & Minimum allowed load fraction \\
$C_\mathrm{d}$ & 0.10 & EN\,14511 formalism
    & Fixed auxiliary overhead coefficient \\
$Q_\mathrm{hp,max}^{*}$ & design envelope & Section~\ref{sec:econ_params}
    & Upper bound on sizing variable \\
\hline
\end{tabular}
\end{table}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Decision Variables}
\label{sub:hthp_vars}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

\begin{itemize}

    \item $y_\mathrm{HP} \in \{0,1\}$: binary installation decision (1 if the heat pump
          is included in the system, 0 otherwise).
          This variable is required to correctly model the fixed-cost component of the
          annualised capital expenditure: the total HP cost is split into a fixed term
          incurred only if the technology is installed and a capacity-proportional term,
          \begin{equation}
              C_\mathrm{HP,annual} = \alpha_\mathrm{HP,fixed}\,y_\mathrm{HP}
                                   + \alpha_\mathrm{HP,var}\,Q_\mathrm{hp}^{*},
              \label{eq:hthp_capex}
          \end{equation}
          where $\alpha_\mathrm{HP,fixed}$ and $\alpha_\mathrm{HP,var}$ are derived
          from the linear-affine CAPEX fit of Section~\ref{sub:capex_model},
          Eq.~\eqref{eq:capex_affine}.
          Without $y_\mathrm{HP}$, the fixed cost would either always be charged or
          always be omitted regardless of whether the solver installs the unit.

    \item $Q_\mathrm{hp}^{*} \in [0,\; Q_\mathrm{hp,max}^{*}]$ [kW$_\mathrm{th}$]:
          continuous sizing variable (rated thermal output at reference conditions).

    \item $u_\mathrm{HP}(t) \in \{0,1\}$, $t=1,\ldots,8\,760$: binary on/off
          commitment.
          Without this variable the minimum-load constraint would read
          $Q_\mathrm{HP}(t) \geq f_\mathrm{min}\,Q_\mathrm{hp}^{*}\,\kappa(t)$,
          a strictly positive lower bound for any non-zero sized unit that forces
          the heat pump on at every time step.
          Likewise, the fixed auxiliary overhead in Eq.~\eqref{eq:hthp_power} would
          remain active at idle, spuriously drawing electricity.
          The binary resolves both issues: when $u_\mathrm{HP}(t)=0$ the effective
          capacity is zero, making all bounds non-binding.
          Introducing 8\,760 per-timestep binaries carries a significant computational
          price, unlike the five technology-selection binaries
          ($y_\mathrm{PV}$, $y_\mathrm{bat}$, $y_\mathrm{HP}$, $y_\mathrm{ltes}$,
          $y_\mathrm{wtes}$), which add negligible combinatorial complexity, each
          additional operational binary can exponentially increase the branch-and-bound
          search tree and this block dominates solver cost.

    \item $\tilde{Q}_\mathrm{hp}(t) \geq 0$ [kW$_\mathrm{th}$]: auxiliary continuous
          variable that replaces the bilinear product
          $Q_\mathrm{hp}^{*}\cdot u_\mathrm{HP}(t)$.
          Because $Q_\mathrm{hp}^{*}$ is continuous and $u_\mathrm{HP}(t)$ is binary,
          their product cannot appear directly in an MILP.
          The big-$M$ constraints of Section~\ref{sub:hthp_bigm} force
          $\tilde{Q}_\mathrm{hp}(t) = Q_\mathrm{hp}^{*}$ when on and
          $\tilde{Q}_\mathrm{hp}(t) = 0$ when off, replacing the product exactly.

    \item $Q_\mathrm{HP}(t) \geq 0$ [kW$_\mathrm{th}$]: actual thermal output
          delivered at time step $t$.

    \item $P_\mathrm{el,HP}(t) \geq 0$ [kW$_\mathrm{el}$]: electrical power consumed
          at time step $t$.

\end{itemize}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{On/Off Linearisation (Big-$M$)}
\label{sub:hthp_bigm}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The three linear constraints that force $\tilde{Q}_\mathrm{hp}(t)$ to equal
$Q_\mathrm{hp}^{*}$ when $u_\mathrm{HP}(t)=1$ and zero when $u_\mathrm{HP}(t)=0$
are:

\begin{align}
    \tilde{Q}_\mathrm{hp}(t) &\leq Q_\mathrm{hp,max}^{*}\, u_\mathrm{HP}(t)
        \label{eq:hthp_bigm1} \\
    \tilde{Q}_\mathrm{hp}(t) &\leq Q_\mathrm{hp}^{*}
        \label{eq:hthp_bigm2} \\
    \tilde{Q}_\mathrm{hp}(t) &\geq Q_\mathrm{hp}^{*} - Q_\mathrm{hp,max}^{*}\,
        \bigl(1 - u_\mathrm{HP}(t)\bigr).
        \label{eq:hthp_bigm3}
\end{align}

\noindent When $u_\mathrm{HP}(t)=0$, constraint \eqref{eq:hthp_bigm1} gives
$\tilde{Q}_\mathrm{hp}(t) \leq 0$, which combined with the non-negativity declaration
$\tilde{Q}_\mathrm{hp}(t) \geq 0$ forces $\tilde{Q}_\mathrm{hp}(t)=0$ exactly.
Without the non-negativity bound, equation~\eqref{eq:hthp_bigm1} alone would only
provide an upper bound and could not prevent $\tilde{Q}_\mathrm{hp}(t)$ from becoming
negative.
When $u_\mathrm{HP}(t)=1$, constraints \eqref{eq:hthp_bigm2} and \eqref{eq:hthp_bigm3}
together force $\tilde{Q}_\mathrm{hp}(t)=Q_\mathrm{hp}^{*}$.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{MILP Constraints}
\label{sub:hthp_constraints}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Three operational constraints bound $Q_\mathrm{HP}(t)$ and $P_\mathrm{el,HP}(t)$.
The thermal output is bounded from above by the weather-derated capacity and from
below by the minimum load fraction $f_\mathrm{min}$ (set to $0.222$ from manufacturer
data):

\begin{align}
    Q_\mathrm{HP}(t) &\leq \tilde{Q}_\mathrm{hp}(t)\cdot\kappa(t)
        \label{eq:hthp_qmax} \\
    Q_\mathrm{HP}(t) &\geq f_\mathrm{min}\,\tilde{Q}_\mathrm{hp}(t)\cdot\kappa(t).
        \label{eq:hthp_qmin}
\end{align}

\noindent The lower bound prevents unrealistically shallow part-load operation.
When the unit is off, $\tilde{Q}_\mathrm{hp}(t)=0$ collapses both bounds to
$Q_\mathrm{HP}(t)=0$ exactly.
The electrical power consumption is governed by Eq.~\eqref{eq:hthp_power},
repeated here as a constraint:

\begin{equation}
    P_\mathrm{el,HP}(t)\cdot\mathrm{COP}(t)
    = C_\mathrm{d}\,\tilde{Q}_\mathrm{hp}(t)\cdot\kappa(t)
      + \bigl(1-C_\mathrm{d}\bigr)\,Q_\mathrm{HP}(t). \tag{\ref{eq:hthp_power}}
\end{equation}

\noindent Since $\mathrm{COP}(t)$ and $\kappa(t)$ are fixed scalars, all three
constraints are linear in the decision variables.
Using $\tilde{Q}_\mathrm{hp}(t)$ rather than $Q_\mathrm{hp}^{*}$ in the overhead
term ensures that $P_\mathrm{el,HP}(t)=0$ when the unit is off; had $Q_\mathrm{hp}^{*}$
appeared instead, the fixed term $C_\mathrm{d}\,Q_\mathrm{hp}^{*}\cdot\kappa(t)$
would draw electricity at every idle step.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Summary of HTHP Constraints}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Table~\ref{tab:hthp_constraints} collects all HTHP constraints, the pre-computed
parameters they use, and their role in the MILP.

\begin{table}[H]
\centering
\caption{Complete HTHP constraint set embedded in the MILP.
         $\kappa(t)$ and $\mathrm{COP}(t)$ are pre-computed parameter arrays;
         all other symbols are MILP variables.}
\label{tab:hthp_constraints}
\begin{tabular}{llp{5.5cm}}
\hline
\textbf{Label} & \textbf{Constraint} & \textbf{Role} \\
\hline
\eqref{eq:hthp_bigm1}--\eqref{eq:hthp_bigm3}
    & Big-$M$ on $\tilde{Q}_\mathrm{hp}(t)$
    & Links sizing variable to on/off binary \\
\eqref{eq:hthp_qmax}
    & $Q_\mathrm{HP}(t) \leq \tilde{Q}_\mathrm{hp}(t)\,\kappa(t)$
    & Weather-derated upper capacity \\
\eqref{eq:hthp_qmin}
    & $Q_\mathrm{HP}(t) \geq f_\mathrm{min}\,\tilde{Q}_\mathrm{hp}(t)\,\kappa(t)$
    & Minimum load fraction \\
\eqref{eq:hthp_power}
    & $P_\mathrm{el}\,\mathrm{COP}(t) = C_\mathrm{d}\tilde{Q}\kappa + (1-C_\mathrm{d})Q_\mathrm{HP}$
    & Electrical draw with cycling penalty \\
\hline
\end{tabular}
\end{table}

% ------------------------------------------------------------
\subsection{Photovoltaic (PV) Array}
\label{sec:pv_model}
% ------------------------------------------------------------

The PV array provides on-site renewable electricity, directly offsetting grid imports and
supplying the HTHP, BESS, and building loads.
Its MILP representation is intentionally simple: all complex photovoltaic physics
(plane-of-array irradiance, cell temperature derating, DC-to-AC inverter losses,
soiling, and wiring losses) are handled entirely in a pre-processing step using the
Open-Meteo\,+\,pvlib pipeline (see Section~\ref{subsec:pv_irradiance}), so only a single
linear constraint is needed inside the optimiser.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Governing Model Equations}
\label{sub:pv_equations}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

One equation governs the PV model.
The AC power injected by the array at each time step is the product of the
pre-computed normalised specific yield $I_\mathrm{sol}(t)$ [kW/kW$_\mathrm{p}$]
and the installed peak capacity $C_\mathrm{PV}$ [kW$_\mathrm{p}$]:

\begin{equation}
    P_\mathrm{PV}(t) = I_\mathrm{sol}(t)\cdot C_\mathrm{PV},
    \qquad \forall\, t.
    \label{eq:pv_power}
\end{equation}

\noindent Because $I_\mathrm{sol}(t)$ is a fixed scalar at every time step,
the constraint is linear in the single sizing variable $C_\mathrm{PV}$.
$P_\mathrm{PV}(t)$ enters the electrical energy balance
(Section~\ref{sec:energy_balance}) as a generation term.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Fixed Parameter Arrays and Inputs}
\label{sub:pv_params}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

All fixed coefficients embedded in the PV constraints are listed in
Table~\ref{tab:pv_inputs}.
Because $I_\mathrm{sol}(t)$ is pre-computed from known climate data before the
optimisation begins, every constraint in which it appears is linear in the decision
variables.

\begin{table}[H]
\centering
\caption{Fixed parameters and pre-computed input arrays for the PV MILP model.}
\label{tab:pv_inputs}
\begin{tabular}{lllp{4.8cm}}
\hline
\textbf{Symbol} & \textbf{Value / Type} & \textbf{Source} & \textbf{Role in model} \\
\hline
$I_\mathrm{sol}(t)$ & hourly array [kW/kW$_\mathrm{p}$]
    & Eq.~\eqref{eq:pv_sol_output}, Section~\ref{subsec:pv_irradiance}
    & Normalised specific yield \\
$C_\mathrm{PV,max}$ & 50\,kW$_\mathrm{p}$
    & Available roof area (M-accent)
    & Upper bound on sizing variable \\

\hline
\end{tabular}
\end{table}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Decision Variables}
\label{sub:pv_vars}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

\begin{itemize}

    \item $y_\mathrm{PV} \in \{0,1\}$: binary installation decision (1 if PV is
          included in the system, 0 otherwise).
          This variable is required to correctly model the fixed-cost component of the
          annualised capital expenditure: the total PV cost is split into a fixed term
          incurred only if the technology is installed and a capacity-proportional term,
          \begin{equation}
              C_\mathrm{PV,annual} = \alpha_\mathrm{PV,fixed}\,y_\mathrm{PV}
                                   + \alpha_\mathrm{PV,var}\,C_\mathrm{PV},
              \label{eq:pv_capex_lin}
          \end{equation}
          where $\alpha_\mathrm{PV,fixed}$ and $\alpha_\mathrm{PV,var}$ are derived
          from the linear-affine CAPEX fit of Section~\ref{sub:pv_capex}.
          Without $y_\mathrm{PV}$, a solver choosing $C_\mathrm{PV}=0$ would still
          incur the fixed cost term, incorrectly penalising the no-PV outcome.

    \item $C_\mathrm{PV} \in [0,\; C_\mathrm{PV,max}]$ [kW$_\mathrm{p}$]:
          continuous sizing variable (installed peak capacity).

    \item $P_\mathrm{PV}(t) \geq 0$ [kW]: hourly AC power output, determined at each
          time step by Eq.~\eqref{eq:pv_power}.

\end{itemize}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{MILP Constraints}
\label{sub:pv_constraints}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Three constraints govern the PV block.
The technology-selection constraint enforces consistency between the binary and the
sizing variable, ensuring no capacity is installed when $y_\mathrm{PV}=0$:
\begin{equation}
    C_\mathrm{PV} \leq C_\mathrm{PV,max}\, y_\mathrm{PV}.
    \label{eq:pv_select}
\end{equation}

\noindent The hourly generation constraint (Eq.~\eqref{eq:pv_power}) links the
sizing decision to the power output at each time step and is linear because
$I_\mathrm{sol}(t)$ is a pre-computed scalar.

To prevent the model from selling grid-purchased electricity at a profit (a physically
impossible arbitrage), the power sold to the grid is capped at the sum of local
generation and battery discharge:
\begin{equation}
    P_\mathrm{sell}(t) \leq P_\mathrm{PV}(t) + P_\mathrm{bat,dis}(t),
    \qquad \forall\, t.
    \label{eq:pv_sellcap}
\end{equation}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Summary of PV Constraints}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Table~\ref{tab:pv_constraints} collects all PV decision variables, parameters,
and constraints embedded in the MILP.

\begin{table}[H]
\centering
\caption{Complete PV  constraint set embedded in the MILP.
         $I_\mathrm{sol}(t)$ is a pre-computed parameter array from the Open-Meteo\,+\,pvlib
         pipeline (Section~\ref{subsec:pv_irradiance});
         all other symbols are MILP variables.}
\label{tab:pv_constraints}
\begin{tabular}{llp{5.5cm}}
\hline
\textbf{Label} & \textbf{Expression} & \textbf{Role} \\
\hline
\eqref{eq:pv_select}
    & $C_\mathrm{PV} \leq C_\mathrm{PV,max}\,y_\mathrm{PV}$
    & Forces zero capacity if PV not selected \\
\eqref{eq:pv_power}
    & $P_\mathrm{PV}(t) = I_\mathrm{sol}(t)\cdot C_\mathrm{PV}$
    & Hourly AC generation (linear in $C_\mathrm{PV}$) \\
\eqref{eq:pv_sellcap}
    & $P_\mathrm{sell}(t) \leq P_\mathrm{PV}(t) + P_\mathrm{bat,dis}(t)$
    & Prevents arbitrage: injection from local sources only \\
\hline
\end{tabular}
\end{table}

% ------------------------------------------------------------
\subsection{Battery Energy Storage System (BESS)}
\label{sec:bess_model}
% ------------------------------------------------------------

The BESS stores surplus PV generation for later use and performs price-arbitrage by
charging during cheap off-peak hours and discharging during expensive peak hours.
It also provides peak-shaving services that directly reduce the monthly capacity
network tariff $c_\mathrm{cap}$.
The electrochemical physics (cell voltage, temperature derating, cycle ageing) are
abstracted into two one-way efficiencies and a fixed C-rate, keeping the formulation
fully linear.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Governing Model Equations}
\label{sub:bess_equations}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The central equation of the BESS model is the state-of-charge (SoC) energy
balance, which links the storage level at each hour to the charge and discharge
power flows:
\begin{equation}
    E_\mathrm{bat}(t) = E_\mathrm{bat}(t-1)
        + \eta_\mathrm{ch}\,P_\mathrm{bat,ch}(t)\,\Delta t
        - \frac{1}{\eta_\mathrm{dis}}\,P_\mathrm{bat,dis}(t)\,\Delta t,
    \qquad \forall\,t.
    \label{eq:bess_soc_dyn}
\end{equation}
The one-way losses are applied asymmetrically: the charging efficiency
$\eta_\mathrm{ch}$ reduces the energy actually stored per kWh injected, while
the discharge efficiency $\eta_\mathrm{dis}$ increases the energy that must be
drawn from storage to deliver one kWh to the bus.
Both coefficients are fixed scalars, so every term in
Eq.~\eqref{eq:bess_soc_dyn} is linear in the MILP variables.

The annualised BESS investment cost follows the same linear-affine structure
used for all other technologies (Section~\ref{sub:capex_model}):
\begin{equation}
    C_\mathrm{bat,annual}
        = \alpha_\mathrm{bat,fixed}\,y_\mathrm{bat}
        + \alpha_\mathrm{bat,var}\,C_\mathrm{bat},
    \label{eq:bess_capex}
\end{equation}
where $y_\mathrm{bat}$ is the installation binary introduced below.
Without $y_\mathrm{bat}$, a solver that sets $C_\mathrm{bat} = 0$ would still
incur the fixed cost term, incorrectly penalising the no-BESS outcome.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Fixed Parameters and Inputs}
\label{sub:bess_params}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

All scalar coefficients embedded in the BESS constraints are listed in
Table~\ref{tab:bess_inputs}.
Because every coefficient is a fixed number known before optimisation, all
constraints remain linear in the decision variables.

\begin{table}[H]
\centering
\caption{Fixed parameters for the BESS MILP model.}
\label{tab:bess_inputs}
\begin{tabular}{lllp{4.8cm}}
\hline
\textbf{Symbol} & \textbf{Value} & \textbf{Source} & \textbf{Role in model} \\
\hline
$\eta_\mathrm{ch}$ & 0.95 & BYD/SolarEdge datasheets & Charging efficiency \\
$\eta_\mathrm{dis}$ & 0.95 & BYD/SolarEdge datasheets & Discharge efficiency \\
$r_\mathrm{C}$ & 0.5\,kW/kWh & Manufacturer C-rate spec & Max charge/discharge rate \\
$C_\mathrm{bat,max}$ & 200\,kWh & Available installation space & Upper bound on sizing \\
\hline
\end{tabular}
\end{table}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Decision Variables}
\label{sub:bess_vars}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

\begin{itemize}

    \item $y_\mathrm{bat} \in \{0,1\}$: binary installation decision (1 if a BESS
          is included in the system, 0 otherwise).
          Required to correctly model the fixed-cost component of the annualised
          CAPEX in Eq.~\eqref{eq:bess_capex}: without it, a zero-capacity solution
          would still incur the fixed installation cost.

    \item $C_\mathrm{bat} \in [0,\; C_\mathrm{bat,max}]$ [kWh]: continuous sizing
          variable representing the usable energy capacity.
          No separate rated-power variable is introduced; charge and discharge
          powers are instead bounded by a fixed C-rate relative to this capacity.

    \item $P_\mathrm{bat,ch}(t) \geq 0$ [kW]: charge power drawn from the AC bus
          at each hour.

    \item $P_\mathrm{bat,dis}(t) \geq 0$ [kW]: discharge power delivered to the
          AC bus at each hour.

    \item $E_\mathrm{bat}(t) \geq 0$ [kWh]: state of charge at each hour,
          determined by the SoC dynamics Eq.~\eqref{eq:bess_soc_dyn}.

\end{itemize}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{MILP Constraints}
\label{sub:bess_constraints}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The technology-selection constraint couples the binary installation decision to
the sizing variable, forcing zero capacity when the BESS is not selected:
\begin{equation}
    C_\mathrm{bat} \leq C_\mathrm{bat,max}\,y_\mathrm{bat}.
    \label{eq:bat_binary}
\end{equation}

The SoC dynamics (Eq.~\eqref{eq:bess_soc_dyn}) enforce energy conservation at every time step.

Charge and discharge powers are each limited to the installed C-rate, bounding
the peak power relative to the energy capacity:
\begin{align}
    P_\mathrm{bat,ch}(t)  &\leq r_\mathrm{C}\,C_\mathrm{bat}, \label{eq:bess_ch_lim} \\
    P_\mathrm{bat,dis}(t) &\leq r_\mathrm{C}\,C_\mathrm{bat}. \label{eq:bess_dis_lim}
\end{align}
With $r_\mathrm{C} = 0.5\,\mathrm{kW/kWh}$, the battery can be fully charged or
discharged in two hours.

The SoC is bounded between zero and the installed capacity at every hour:
\begin{equation}
    0 \leq E_\mathrm{bat}(t) \leq C_\mathrm{bat},
    \qquad \forall\,t.
    \label{eq:bess_soc_bounds}
\end{equation}
No explicit depth-of-discharge limit is imposed; the full 0--100\,\% usable
window is available to the optimiser.

Simultaneous charging and discharging is not excluded by an additional binary
variable: because both flows incur efficiency losses, operating them
simultaneously is never economically optimal and the solver avoids it
naturally.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Summary of BESS Constraints}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Table~\ref{tab:bess_summary} collects all BESS decision variables and
constraints embedded in the MILP.

\begin{table}[H]
\centering
\caption{Complete BESS constraint set embedded in the MILP.}
\label{tab:bess_summary}
\begin{tabular}{llp{5.5cm}}
\hline
\textbf{Label} & \textbf{Expression} & \textbf{Role} \\
\hline
\eqref{eq:bat_binary}
    & $C_\mathrm{bat} \leq C_\mathrm{bat,max}\,y_\mathrm{bat}$
    & Forces zero capacity if BESS not selected \\
\eqref{eq:bess_soc_dyn}
    & $E_\mathrm{bat}(t) = E_\mathrm{bat}(t-1) + \eta_\mathrm{ch}P_\mathrm{ch}\Delta t - \frac{P_\mathrm{dis}\Delta t}{\eta_\mathrm{dis}}$
    & SoC energy balance \\
\eqref{eq:bess_ch_lim}--\eqref{eq:bess_dis_lim}
    & $P_\mathrm{bat,ch/dis}(t) \leq r_\mathrm{C}\,C_\mathrm{bat}$
    & C-rate power limits \\
\eqref{eq:bess_soc_bounds}
    & $0 \leq E_\mathrm{bat}(t) \leq C_\mathrm{bat}$
    & SoC feasibility bounds \\
\hline
\end{tabular}
\end{table}

% ------------------------------------------------------------
\subsection{Thermal Energy Storage (TES)}
\label{sec:tes_model}
% ------------------------------------------------------------

The MILP framework offers two competing TES technologies, between which the
optimiser selects via a binary variable.
Binary variables $y_\mathrm{ltes},\,y_\mathrm{wtes} \in \{0,1\}$ encode this
choice, with the mutual exclusivity constraint:
\begin{equation}
    y_\mathrm{ltes} + y_\mathrm{wtes} = 1.
    \label{eq:tes_select}
\end{equation}
When a technology is not selected its sizing variable is forced to zero by:
\begin{align}
    C_\mathrm{ltes} &\leq C_\mathrm{ltes,max}\,y_\mathrm{ltes}, \label{eq:ltes_select} \\
    h_\mathrm{wtes} &\leq h_\mathrm{wtes,max}\,y_\mathrm{wtes}. \label{eq:wtes_select}
\end{align}
The two technologies are described in the following subsubsections.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Latent Thermal Energy Storage (LTES)}
\label{sub:ltes_model}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The LTES uses a Phase-Change Material (PCM) that stores thermal energy as latent
heat during melting and releases it during solidification, at an essentially
\emph{constant} temperature $T_\mathrm{pcm}$.
Because the driving temperature difference between the tank wall and its
surroundings does not change as the tank charges or discharges, the standby-loss
model differs fundamentally from the SoC-proportional loss of the sensible-heat
WTES: a dedicated isothermal loss model is required.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Governing Model Equations}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
The thermal capacity is linked to the physical installation volume through the
effective volumetric energy density:
\begin{equation}
    C_\mathrm{ltes} = \rho_\mathrm{L,pcm}\,V_\mathrm{ltes},
    \qquad
    \rho_\mathrm{L,pcm} \approx 47.6\;\text{kWh}_\mathrm{th}/\text{m}^3.
    \label{eq:ltes_capacity}
\end{equation}
This value is estimated from the four Sunamp Thermino~e models
(Appendix~\ref{appendix:thermino_datasheet}) by dividing each unit's rated thermal
capacity by its outer installation volume (Dim$_1 \times$ Dim$_2 \times$ Dim$_3$),
which yields 38--64\,kWh$_\mathrm{th}$/m$^3$ across the range.
Using outer dimensions as the installation footprint is conservative (the PCM core
occupies only part of the enclosure; the remainder is insulation, heat exchanger,
and electronics), so $47.6\,\text{kWh}_\mathrm{th}/\text{m}^3$ represents a
lower-bound estimate that avoids over-reliance on thermal storage in the optimiser.

Because the PCM operates isothermally, the hourly standby loss is a linear function
of installed capacity only, with no SoC-dependent term:
\begin{equation}
    \ell_\mathrm{ltes} = a\,y_\mathrm{ltes} + b\,C_\mathrm{ltes},
    \label{eq:ltes_loss}
\end{equation}
where $a$ [kWh/h] is a fixed overhead loss incurred whenever the unit is installed
and $b$ [$\mathrm{h}^{-1}$] is the capacity-proportional loss rate.
The coefficients were obtained by ordinary least-squares regression against
manufacturer standby-loss data from the Sunamp Thermino~e product range
(Appendix~\ref{appendix:thermino_datasheet}), spanning 3.5--14\,kWh$_\mathrm{th}$:
\begin{equation}
    a = 0.01667\;\text{kWh/h},
    \qquad
    b = 1.393\times10^{-3}\;\text{h}^{-1},
    \qquad
    R^2 = 0.935.
    \label{eq:ltes_loss_fit}
\end{equation}
Figure~\ref{fig:ltes_loss_fit} shows the data and fitted line.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.65\textwidth]{images/ltes_loss_fit.png}
    \caption{Hourly standby loss versus installed capacity for the Sunamp Thermino~e
             product range. Data from the manufacturer datasheet
             (Appendix~\ref{appendix:thermino_datasheet}).
             The linear fit (Eq.~\eqref{eq:ltes_loss_fit}, $R^2 = 0.935$) decomposes
             total loss into a fixed overhead $a$ and a capacity-proportional
             term $b\,C_\mathrm{ltes}$, reflecting the isothermal operating principle
             of the PCM unit.}
    \label{fig:ltes_loss_fit}
\end{figure}

The stored thermal energy at each hour evolves according to the energy balance:
\begin{equation}
    Q_\mathrm{ltes}(t) = Q_\mathrm{ltes}(t-1)
        - \ell_\mathrm{ltes}\,y_\mathrm{ltes}
        + \eta_\mathrm{ch}\,Q_\mathrm{ltes,in}(t)
        - \frac{Q_\mathrm{ltes,out}(t)}{\eta_\mathrm{dis}},
    \qquad \forall\,t.
    \label{eq:ltes_dyn}
\end{equation}
The loss term $\ell_\mathrm{ltes}\,y_\mathrm{ltes}$ is automatically deactivated
when the LTES is not selected ($y_\mathrm{ltes} = 0$).

The annualised LTES investment cost follows the same linear-affine structure used
for all technologies (Section~\ref{sub:capex_model}):
\begin{equation}
    C_\mathrm{ltes,annual}
        = \alpha_\mathrm{ltes,fixed}\,y_\mathrm{ltes}
        + \alpha_\mathrm{ltes,var}\,C_\mathrm{ltes},
    \label{eq:ltes_capex}
\end{equation}
with $\alpha_\mathrm{ltes,fixed}$ and $\alpha_\mathrm{ltes,var}$ from
Section~\ref{sub:capex_model}.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Fixed Parameters and Inputs.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

All fixed coefficients embedded in the LTES constraints are listed in
Table~\ref{tab:ltes_inputs}.

\begin{table}[H]
\centering
\caption{Fixed parameters for the LTES MILP model.}
\label{tab:ltes_inputs}
\begin{tabular}{lllp{4.5cm}}
\hline
\textbf{Symbol} & \textbf{Value} & \textbf{Source} & \textbf{Role in model} \\
\hline
$\rho_\mathrm{L,pcm}$ & $47.6\,\text{kWh}_\mathrm{th}/\text{m}^3$
    & PCM material data & Volumetric latent-heat density \\
$a$ & $0.01667\,\text{kWh/h}$
    & Sunamp Thermino~e fit & Fixed standby-loss overhead \\
$b$ & $1.393\times10^{-3}\,\text{h}^{-1}$
    & Sunamp Thermino~e fit & Capacity-proportional loss rate \\
$V_\mathrm{max}$ & $20\,\text{m}^3$
    & Available plant room & Shared volume limit (LTES/WTES) \\
$C_\mathrm{ltes,max}$ & $\approx 952\,\text{kWh}_\mathrm{th}$
    & $\rho_\mathrm{L,pcm}\times V_\mathrm{max}$ & Upper bound on thermal capacity \\
$\eta_\mathrm{ch}$, $\eta_\mathrm{dis}$ & $0.95$ each
    & Engineering assumption & Charge/discharge efficiency \\
$r_\mathrm{C}$ & $0.5\,\text{kW}_\mathrm{th}/\text{kWh}_\mathrm{th}$
    & Heat-exchanger sizing & Max charge/discharge rate \\
\hline
\end{tabular}
\end{table}

The high volumetric energy density of the LTES ($\rho_\mathrm{L,pcm} \approx
47.6\,\text{kWh}_\mathrm{th}/\text{m}^3$) is roughly \textbf{twice} that of the
WTES ($\rho_\mathrm{w} \approx 22.6\,\text{kWh}_\mathrm{th}/\text{m}^3$), so the
shared $V_\mathrm{max}$ constraint gives the LTES access to nearly twice the
thermal capacity in the same footprint.
This is the core economic rationale for including the more expensive LTES: when
available volume is binding, the solver may endogenously select LTES despite its
higher unit cost.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Decision Variables.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

\begin{itemize}

    \item $y_\mathrm{ltes} \in \{0,1\}$: binary installation decision.
          Required to correctly model the fixed-cost component of the CAPEX
          (Eq.~\eqref{eq:ltes_capex}) and to deactivate the standby-loss term
          in Eq.~\eqref{eq:ltes_loss} when the LTES is not selected.

    \item $C_\mathrm{ltes} \in [0,\; C_\mathrm{ltes,max}]$
          [kWh$_\mathrm{th}$]: continuous sizing variable (installed thermal
          capacity), linked to the physical volume by Eq.~\eqref{eq:ltes_capacity}.

    \item $Q_\mathrm{ltes}(t) \geq 0$ [kWh$_\mathrm{th}$]: stored thermal energy
          (state of charge) at each hour, governed by Eq.~\eqref{eq:ltes_dyn}.

    \item $Q_\mathrm{ltes,in}(t) \geq 0$ [kWh$_\mathrm{th}$/h]: charging heat flow
          at each hour.

    \item $Q_\mathrm{ltes,out}(t) \geq 0$ [kWh$_\mathrm{th}$/h]: discharging heat
          flow at each hour.

\end{itemize}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{MILP Constraints.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The technology-selection constraint (already introduced in the TES subsection
intro, Eq.~\eqref{eq:ltes_select}) couples the binary to the sizing variable,
forcing zero capacity when the LTES is not chosen.

Both TES technologies compete for the same physical footprint; the shared volume
limit bounds the maximum installable LTES capacity:
\begin{equation}
    C_\mathrm{ltes} \leq \rho_\mathrm{L,pcm}\,V_\mathrm{max}.
    \label{eq:ltes_maxvol}
\end{equation}

The SoC energy balance (Eq.~\eqref{eq:ltes_dyn}) enforces thermal energy
conservation at every hour, incorporating the isothermal standby loss.

Charge and discharge heat flows are bounded by the C-rate limit:
\begin{align}
    Q_\mathrm{ltes,in}(t)  &\leq r_\mathrm{C}\,C_\mathrm{ltes}, \label{eq:ltes_ch} \\
    Q_\mathrm{ltes,out}(t) &\leq r_\mathrm{C}\,C_\mathrm{ltes}. \label{eq:ltes_dis}
\end{align}

The state of charge is bounded between 5\,\% and 95\,\% of full capacity to
prevent complete depletion or overflow:
\begin{equation}
    0.05\,C_\mathrm{ltes} \;\leq\; Q_\mathrm{ltes}(t) \;\leq\; 0.95\,C_\mathrm{ltes},
    \qquad \forall\,t.
    \label{eq:ltes_soc}
\end{equation}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Summary of LTES Constraints.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Table~\ref{tab:ltes_summary} collects all LTES constraints embedded in the MILP.

\begin{table}[H]
\centering
\caption{Complete LTES constraint set embedded in the MILP.}
\label{tab:ltes_summary}
\begin{tabular}{llp{5.5cm}}
\hline
\textbf{Label} & \textbf{Expression} & \textbf{Role} \\
\hline
\eqref{eq:ltes_maxvol}
    & $C_\mathrm{ltes} \leq \rho_\mathrm{L,pcm}\,V_\mathrm{max}$
    & Shared volume upper bound \\
\eqref{eq:ltes_dyn}
    & $Q_\mathrm{ltes}(t) = Q_\mathrm{ltes}(t-1) - \ell_\mathrm{ltes}\,y_\mathrm{ltes} + \eta_\mathrm{ch}Q_\mathrm{in} - Q_\mathrm{out}/\eta_\mathrm{dis}$
    & SoC energy balance (isothermal loss) \\
\eqref{eq:ltes_ch}--\eqref{eq:ltes_dis}
    & $Q_\mathrm{ltes,in/out}(t) \leq r_\mathrm{C}\,C_\mathrm{ltes}$
    & C-rate charge/discharge limits \\
\eqref{eq:ltes_soc}
    & $0.05\,C_\mathrm{ltes} \leq Q_\mathrm{ltes}(t) \leq 0.95\,C_\mathrm{ltes}$
    & SoC feasibility bounds \\
\hline
\end{tabular}
\end{table}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\subsubsection{Water Thermal Energy Storage (WTES)}
\label{sub:wtes_model}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The WTES is modelled as a vertical, insulated, cylindrical water tank using the
stratified two-zone model described in Section~\ref{sub:sts_model} and
illustrated in Figure~\ref{fig:stratifiedtes}.
A fixed inner diameter $d = 1\,\text{m}$ and a variable height $h_\mathrm{wtes}$
are adopted, keeping all capacity-related expressions linear in the sizing variable.
The tank operates between supply temperature $T_\mathrm{H} = 60\,^{\circ}\mathrm{C}$
and return temperature $T_\mathrm{C} = 40\,^{\circ}\mathrm{C}$
($\Delta T_\mathrm{HC} = 20\,\mathrm{K}$), with a representative indoor ambient
of $T_\mathrm{env} = 10\,^{\circ}\mathrm{C}$.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Governing Model Equations.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The nominal thermal capacity is a linear function of tank height:
\begin{equation}
    C_\mathrm{WTES} = \kappa_m \, h_\mathrm{wtes},
    \qquad
    \kappa_m = \frac{\tfrac{\pi d^2}{4}\,\rho_w\,c_w\,\Delta T_\mathrm{HC}}{3.6\times10^6}
    \approx 17.77\;\text{kWh}_\mathrm{th}/\text{m},
    \label{eq:wtes_capacity}
\end{equation}
where $\rho_w = 971.8\,\text{kg/m}^3$ (water at $55\,^{\circ}\mathrm{C}$)
and $c_w = 4{,}190\,\text{J/(kg\,K)}$.

The three-component standby-loss model~\cite{Krien2020oemofsolph} captures the
SoC-dependent lateral wall loss, a baseline capacity-proportional loss, and a
constant lid loss:
\begin{equation}
    \ell_\mathrm{wtes}(t) = \beta\,Q_\mathrm{wtes}(t-1)
                           + \gamma\,C_\mathrm{WTES}
                           + \delta,
    \label{eq:wtes_loss}
\end{equation}
with coefficients derived from first principles.
The lateral wall area of the cylinder is $A_\mathrm{wall} = 4V/d$, giving an
instantaneous wall heat loss:
\begin{equation}
    \dot{Q}_\mathrm{wall} = U_w \cdot \frac{4V}{d} \cdot (T_\mathrm{tank} - T_\mathrm{env}) \cdot \Delta t.
    \label{eq:Qwall_raw}
\end{equation}
In the two-zone model, the bulk temperature depends linearly on SoC,
$T_\mathrm{tank} = T_\mathrm{C} + (Q/C)\,\Delta T_\mathrm{HC}$, so the
driving temperature difference splits into a constant baseline and a
SoC-dependent part:
\begin{equation}
    T_\mathrm{tank} - T_\mathrm{env}
        = \underbrace{(T_\mathrm{C} - T_\mathrm{env})}_{\Delta T_\mathrm{C0}\,=\,30\,\mathrm{K}}
        + \underbrace{\frac{Q}{C}\,\Delta T_\mathrm{HC}}_{\text{SoC-dependent}}.
    \label{eq:dT_split}
\end{equation}
Substituting into Eq.~\eqref{eq:Qwall_raw} and using $C = \rho_w V c_w \Delta T_\mathrm{HC}$:
\begin{align}
    \dot{Q}_\mathrm{wall}
        &= \underbrace{\frac{U_w \cdot 4 \cdot \Delta t}{\rho_w \cdot c_w \cdot d}}_{\displaystyle\beta} \cdot Q
          \;+\;
           \underbrace{\frac{U_w \cdot 4 \cdot \Delta t}{\rho_w \cdot c_w \cdot d} \cdot \frac{\Delta T_\mathrm{C0}}{\Delta T_\mathrm{HC}}}_{\displaystyle\gamma} \cdot C.
    \label{eq:betagamma_derived}
\end{align}
The coefficient $\beta$ is the fractional lateral loss per unit stored energy
(higher SoC means hotter water, larger gradient);
$\gamma = \beta\,\Delta T_\mathrm{C0}/\Delta T_\mathrm{HC}$ is the baseline lateral
loss per unit installed capacity, present even when the tank is empty;
and $\delta$ accounts for the constant top and bottom lid losses.

The SoC energy balance at each hour combines the loss model with charge and
discharge flows:
\begin{equation}
    Q_\mathrm{wtes}(t) = Q_\mathrm{wtes}(t-1)
        - \ell_\mathrm{wtes}(t)\,y_\mathrm{wtes}
        + \eta_\mathrm{ch}\,Q_\mathrm{wtes,in}(t)
        - \frac{Q_\mathrm{wtes,out}(t)}{\eta_\mathrm{dis}},
    \qquad \forall\,t.
    \label{eq:wtes_dyn}
\end{equation}
The factor $y_\mathrm{wtes}$ deactivates the loss term when WTES is not selected.

The annualised WTES investment cost follows the same linear-affine structure used
for all technologies (Section~\ref{sub:capex_model}):
\begin{equation}
    C_\mathrm{wtes,annual}
        = \alpha_\mathrm{wtes,fixed}\,y_\mathrm{wtes}
        + \alpha_\mathrm{wtes,var}\,C_\mathrm{WTES},
    \label{eq:wtes_capex_annual}
\end{equation}
where $y_\mathrm{wtes}$ is the installation binary introduced below.
Without $y_\mathrm{wtes}$, a solver that sets $h_\mathrm{wtes} = 0$ would still
incur the fixed cost term, incorrectly penalising the no-WTES outcome.

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Fixed Parameters and Inputs.}
\label{par:wtes_uval}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

All fixed coefficients embedded in the WTES constraints are listed in
Table~\ref{tab:wtes_inputs}.
The wall U-value $U_w$ is the single most influential parameter in the loss model.
Manufacturer datasheets report standby energy consumption [kWh/24\,h] under
EN~12977-3 conditions (tank at $65\,^{\circ}\mathrm{C}$, ambient at
$20\,^{\circ}\mathrm{C}$, $\Delta T = 45\,\mathrm{K}$), from which the implied
$U$-value is back-calculated as:
\begin{equation}
    U_\mathrm{impl} = \frac{Q_\mathrm{standby} \times 1000}{A_\mathrm{tot}\,\Delta T\,24},
    \label{eq:uimpl}
\end{equation}
where $A_\mathrm{tot} = A_\mathrm{lat} + 2A_\mathrm{cross}$ [m$^2$].
Applied to the full Vaillant uniSTOR VPS range
(Appendix~\ref{appendix:vps_datasheet}), the implied values range from
$\approx 0.54$ (VPS~300/3) down to $\approx 0.30\,\text{W/(m}^2\text{K)}$
(VPS~2000/3); Figure~\ref{fig:wtes_validation} shows the results.
The adopted value $U_w = 0.4\,\text{W/(m}^2\text{K)}$ is conservative for the
large-tank range most relevant here, ensuring the optimiser does not over-rely
on thermal storage.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/wtes_validation_vps.png}
    \caption{WTES wall U-value validation against the Vaillant uniSTOR VPS
             product range (Appendix~\ref{appendix:vps_datasheet}).
             \textit{Left}: absolute standby loss [kWh/24\,h] per tank model
             with candidate $U$-value curves overlaid.
             \textit{Right}: implied $U$-value back-calculated from
             Eq.~\eqref{eq:uimpl} for each VPS model; the chosen value
             $U_w = 0.4\,\text{W/(m}^2\text{K)}$ is shown as a horizontal line.}
    \label{fig:wtes_validation}
\end{figure}

\begin{table}[H]
\centering
\caption{Fixed parameters for the WTES MILP model.}
\label{tab:wtes_inputs}
\begin{tabular}{lllp{4.5cm}}
\hline
\textbf{Symbol} & \textbf{Value} & \textbf{Source} & \textbf{Role in model} \\
\hline
$d$ & $1.0\,\text{m}$ & Design choice & Fixed inner diameter \\
$U_w$ & $0.4\,\text{W/(m}^2\text{K)}$ & VPS datasheet fit (Fig.~\ref{fig:wtes_validation}) & Wall heat-loss coefficient \\
$\Delta T_\mathrm{HC}$ & $20\,\text{K}$ & Operating temps & Usable temperature swing \\
$\kappa_m$ & $17.77\,\text{kWh}_\mathrm{th}/\text{m}$ & Eq.~\eqref{eq:wtes_capacity} & Capacity-per-unit-height \\
$\beta$ & $1.42\times10^{-3}$ & Eq.~\eqref{eq:beta_wtes} & SoC-proportional loss coefficient \\
$\gamma$ & $2.12\times10^{-3}$ & Eq.~\eqref{eq:gamma_wtes} & Capacity-proportional loss coefficient \\
$\delta$ & $0.025\,\text{kWh}_\mathrm{th}/\text{h}$ & Eq.~\eqref{eq:delta_wtes} & Constant lid loss \\
$h_\mathrm{wtes,max}$ & $25.5\,\text{m}$ & $V_\mathrm{max}/A_\mathrm{cross}$ & Upper height bound \\
$\eta_\mathrm{ch}$, $\eta_\mathrm{dis}$ & $0.95$ each & Engineering assumption & Charge/discharge efficiency \\
$r_\mathrm{C}$ & $0.5\,\text{kW}_\mathrm{th}/\text{kWh}_\mathrm{th}$ & HX sizing & Max charge/discharge rate \\
$\alpha_\mathrm{wtes,fixed}$ & $73.9\,\text{\euro/yr}$ & Section~\ref{sub:capex_model} & Fixed annual CAPEX term \\
$\alpha_\mathrm{wtes,var}$ & $4.0\,\text{\euro/(yr\,kWh}_\mathrm{th})$ & Section~\ref{sub:capex_model} & Capacity-proportional CAPEX \\
$n_\mathrm{wtes}$ & $20\,\text{yr}$ & Market data & Economic lifetime \\
\hline
\end{tabular}
\end{table}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Decision Variables.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

\begin{itemize}

    \item $y_\mathrm{wtes} \in \{0,1\}$: binary installation decision.
          Required to correctly model the fixed CAPEX term in
          Eq.~\eqref{eq:wtes_capex_annual} and to deactivate the standby-loss
          term in Eq.~\eqref{eq:wtes_dyn} when WTES is not selected.

    \item $h_\mathrm{wtes} \in [0,\; h_\mathrm{wtes,max}]$ [m]: continuous
          sizing variable (tank height).
          All capacity-related quantities follow linearly from this single variable
          via Eq.~\eqref{eq:wtes_capacity}.

    \item $Q_\mathrm{wtes}(t) \geq 0$ [kWh$_\mathrm{th}$]: stored thermal energy
          at each hour, governed by Eq.~\eqref{eq:wtes_dyn}.

    \item $Q_\mathrm{wtes,in}(t) \geq 0$ [kWh$_\mathrm{th}$/h]: charging heat
          flow at each hour.

    \item $Q_\mathrm{wtes,out}(t) \geq 0$ [kWh$_\mathrm{th}$/h]: discharging
          heat flow at each hour.

\end{itemize}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{MILP Constraints.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

The technology-selection constraint (Eq.~\eqref{eq:wtes_select}, introduced in
the TES subsection intro) couples the binary to the sizing variable, forcing
$h_\mathrm{wtes} = 0$ when WTES is not chosen.

The shared volume limit bounds the maximum installable tank height:
\begin{equation}
    h_\mathrm{wtes} \leq \frac{V_\mathrm{max}}{A_\mathrm{cross}} \approx 25.5\;\text{m}.
    \label{eq:wtes_hmax}
\end{equation}

The SoC energy balance (Eq.~\eqref{eq:wtes_dyn}) enforces thermal energy
conservation at every hour, incorporating the three-component standby loss.

Charge and discharge heat flows are bounded by the C-rate limit:
\begin{align}
    Q_\mathrm{wtes,in}(t)  &\leq r_\mathrm{C}\,C_\mathrm{WTES}, \label{eq:wtes_ch} \\
    Q_\mathrm{wtes,out}(t) &\leq r_\mathrm{C}\,C_\mathrm{WTES}. \label{eq:wtes_dis}
\end{align}

To prevent the diffuser plates from being submerged at the tank extremes, the
state of charge is bounded between 5\,\% and 95\,\% of full capacity:
\begin{equation}
    0.05\,C_\mathrm{WTES} \;\leq\; Q_\mathrm{wtes}(t) \;\leq\; 0.95\,C_\mathrm{WTES},
    \qquad \forall\,t.
    \label{eq:wtes_soc}
\end{equation}

% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -
\paragraph{Summary of WTES Constraints.}
% - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

Table~\ref{tab:wtes_summary} collects all WTES constraints embedded in the MILP.

\begin{table}[H]
\centering
\caption{Complete WTES constraint set embedded in the MILP.}
\label{tab:wtes_summary}
\begin{tabular}{llp{5.5cm}}
\hline
\textbf{Label} & \textbf{Expression} & \textbf{Role} \\
\hline
\eqref{eq:wtes_capex_annual}
    & $C_\mathrm{wtes,annual} = \alpha_\mathrm{wtes,fixed}\,y_\mathrm{wtes} + \alpha_\mathrm{wtes,var}\,C_\mathrm{WTES}$
    & Linear-affine annualised CAPEX \\
\eqref{eq:tes_select}
    & $y_\mathrm{ltes} + y_\mathrm{wtes} = 1$
    & Mutual exclusivity with LTES \\
\eqref{eq:wtes_select}
    & $h_\mathrm{wtes} \leq h_\mathrm{wtes,max}\,y_\mathrm{wtes}$
    & Forces zero height if WTES not selected \\
\eqref{eq:wtes_hmax}
    & $h_\mathrm{wtes} \leq 25.5\,\text{m}$
    & Shared volume upper bound \\
\eqref{eq:wtes_dyn}
    & $Q_\mathrm{wtes}(t) = Q_\mathrm{wtes}(t-1) - \ell_\mathrm{wtes}\,y_\mathrm{wtes} + \eta_\mathrm{ch}Q_\mathrm{in} - Q_\mathrm{out}/\eta_\mathrm{dis}$
    & SoC energy balance (three-term loss) \\
\eqref{eq:wtes_ch}--\eqref{eq:wtes_dis}
    & $Q_\mathrm{wtes,in/out}(t) \leq r_\mathrm{C}\,C_\mathrm{WTES}$
    & C-rate charge/discharge limits \\
\eqref{eq:wtes_soc}
    & $0.05\,C_\mathrm{WTES} \leq Q_\mathrm{wtes}(t) \leq 0.95\,C_\mathrm{WTES}$
    & SoC feasibility bounds \\
\hline
\end{tabular}
\end{table}

% ============================================================
\section{System Energy Balance Constraints}
\label{sec:energy_balance}
% ============================================================

The component models of Section~\ref{sec:component_models} are coupled through two
nodal energy balances that must hold at every hour $t \in \{1,\ldots,T\}$: an
\textit{electrical} balance on the AC bus and a \textit{thermal} balance at the
building heat-demand node.
Figure~\ref{fig:energy_flows} illustrates all power and heat flows connecting the
system components.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    >=Latex, font=\small, thick,
    box/.style  = {draw, rounded corners=3pt, minimum width=1.9cm,
                   minimum height=0.72cm, align=center},
    lbl/.style  = {font=\footnotesize, fill=white, inner sep=1.5pt},
    earr/.style = {->, line width=1.2pt},
    tarr/.style = {->, line width=1.2pt, red!65!black},
]

%% All left-column nodes have minimum width=1.9 cm centred at x=0,
%% so their east edge is at x=0.95. The AC bus is at x=2.9.
%% HP at x=5.6 → west edge at x=4.65.

%% ─── Nodes ──────────────────────────────────────────────────
\node[box, fill=orange!20]  (pv)    at (0,   4.2) {PV};
\node[box, fill=green!12]   (grid)  at (0,   3.0) {Grid};
\node[box, fill=teal!15]    (bess)  at (0,   1.5) {BESS};
\node[box, fill=gray!15]    (eload) at (0,   0.2) {Elec.\ load};
\node[box, fill=purple!15]  (hp)    at (5.6, 3.0) {HP};
\node[box, fill=yellow!15]  (tes)   at (9.4, 4.5) {TES};
\node[box, fill=red!12]     (tload) at (12.0,3.0) {Heat demand\\$Q_\mathrm{dem}(t)$};

%% ─── AC bus bar (vertical, x=2.9) ──────────────────────────
\draw[line width=5pt, blue!40, line cap=round] (2.9,-0.4) -- (2.9, 4.7);
\node[rotate=90, font=\small\bfseries, blue!60!black] at (3.25, 2.3) {AC Bus};

%% ─── Thermal junction dot ───────────────────────────────────
\coordinate (junc) at (7.8, 3.0);
\fill[red!60!black] (junc) circle (3.2pt);

%% ─── Electrical arrows (all perfectly horizontal) ───────────
% Both endpoints of every arrow share the same y-coordinate.
% Parallel pairs (import/export, charge/discharge) are offset ±0.15 cm.

% PV → bus  (y = 4.2)
\draw[earr, orange!80!black]
    (0.95, 4.2) -- node[lbl, above]{$P_\mathrm{PV}(t)$} (2.9, 4.2);

% Grid → bus: import  (y = 3.15)
\draw[earr, green!65!black]
    (0.95, 3.15) -- node[lbl, above]{$P_\mathrm{buy}(t)$} (2.9, 3.15);

% Bus → grid: export  (y = 2.85)
\draw[earr, green!45!black]
    (2.9, 2.85) -- node[lbl, below]{$P_\mathrm{sell}(t)$} (0.95, 2.85);

% BESS → bus: discharge  (y = 1.65)
\draw[earr, teal!70!black]
    (0.95, 1.65) -- node[lbl, above]{$P_\mathrm{bat,dis}(t)$} (2.9, 1.65);

% Bus → BESS: charge  (y = 1.35)
\draw[earr, teal!45!black]
    (2.9, 1.35) -- node[lbl, below]{$P_\mathrm{bat,ch}(t)$} (0.95, 1.35);

% Bus → elec load  (y = 0.2)
\draw[earr, gray!65!black]
    (2.9, 0.2) -- node[lbl, above]{$P_\mathrm{dem}^\mathrm{el}(t)$} (0.95, 0.2);

% Bus → HP  (y = 3.0)
\draw[earr, purple!70!black]
    (2.9, 3.0) -- node[lbl, above]{$P_\mathrm{hp}^\mathrm{el}(t)$} (4.65, 3.0);

%% ─── Thermal arrows ─────────────────────────────────────────
% HP → junction  (horizontal, y=3.0)
\draw[tarr]
    (hp.east) -- node[lbl, above]{$q_\mathrm{hp}^\mathrm{th}(t)$} (junc);

% Junction → TES: L-shape (vertical up to TES y, then horizontal right)
% Label sits on the short horizontal segment (pos≈0.75 of total path length)
\draw[tarr, orange!70!black]
    (junc) -| node[lbl, above, pos=0.70]{$Q_\mathrm{tes,in}(t)$} (tes.west);

% Junction → heat demand: direct bypass  (horizontal, y=3.0)
\draw[tarr]
    (junc) -- node[lbl, below]{$Q_\mathrm{load,in}(t)$} (tload.west);

% TES → heat demand: L-shape (horizontal right to tload x, then vertical down)
% Label sits on the horizontal segment (pos≈0.25 of total path length)
\draw[tarr, orange!70!black]
    (tes.east) |- node[lbl, above, pos=0.45]{$Q_\mathrm{tes,out}(t)$} (tload.north);

%% ─── Domain outlines ─────────────────────────────────────────
\draw[dashed, blue!40, rounded corners=7pt]  (-1.3,-0.9) rectangle (4.0, 5.1);
\node[blue!55!black, font=\footnotesize\itshape] at (1.3, -0.75) {electrical domain};

\draw[dashed, red!40, rounded corners=7pt]   (4.1,-0.9) rectangle (13.1, 5.1);
\node[red!55!black, font=\footnotesize\itshape] at (8.6, -0.75) {thermal domain};

\end{tikzpicture}
\caption{System power-flow diagram.
         \textit{Left (electrical domain):} PV generation, grid import/export, and BESS
         charge/discharge all connect to the AC bus, which supplies the building electrical
         load and the HP.
         \textit{Right (thermal domain):} HP thermal output splits at the junction node
         into TES charging ($Q_\mathrm{tes,in}$) and direct supply to the load
         ($Q_\mathrm{load,in}$); TES discharge ($Q_\mathrm{tes,out}$) and the direct
         path together satisfy the hourly heat demand $Q_\mathrm{dem}(t)$.}
\label{fig:energy_flows}
\end{figure}

% ------------------------------------------------------------
\subsection{Electrical Energy Balance}
\label{sub:elec_balance}
% ------------------------------------------------------------

At every hour the total power injected into the AC bus must equal the total power
withdrawn:
\begin{equation}
    P_\mathrm{PV}(t) + P_\mathrm{bat,dis}(t) + P_\mathrm{buy}(t)
    \;=\;
    P_\mathrm{dem}^\mathrm{el}(t) + P_\mathrm{bat,ch}(t)
    + P_\mathrm{hp}^\mathrm{el}(t) + P_\mathrm{sell}(t),
    \label{eq:elec_balance}
\end{equation}
where $P_\mathrm{PV}(t) = I_\mathrm{sol}(t)\,C_\mathrm{PV}$ is the PV generation
(Section~\ref{subsec:pv_irradiance}), $P_\mathrm{dem}^\mathrm{el}(t)$ is the
measured hourly building electrical load, and $P_\mathrm{buy}(t),\,P_\mathrm{sell}(t)
\geq 0$ are the grid import and export flows respectively.

To prevent the optimiser from buying grid electricity and immediately re-selling it
at a profit, grid export is capped to on-site generation:
\begin{equation}
    P_\mathrm{sell}(t) \leq P_\mathrm{PV}(t) + P_\mathrm{bat,dis}(t).
    \label{eq:sell_limit}
\end{equation}
This ensures that any exported energy originates exclusively from the PV array or
BESS, not from grid-purchased power.

% ------------------------------------------------------------
\subsection{Thermal Energy Balance}
\label{sub:therm_balance}
% ------------------------------------------------------------

The thermal domain is governed by two coupled equality constraints.
First, all heat output by the HP must be accounted for — it either charges the TES
or is delivered directly to the building heating circuit:
\begin{equation}
    q_\mathrm{hp}^\mathrm{th}(t) = Q_\mathrm{tes,in}(t) + Q_\mathrm{load,in}(t),
    \label{eq:hp_split}
\end{equation}
where $Q_\mathrm{tes,in}(t) = Q_\mathrm{ltes,in}(t) + Q_\mathrm{wtes,in}(t)$
(exactly one term is non-zero, determined by the TES type selection binaries
$y_\mathrm{ltes}$ and $y_\mathrm{wtes}$) and $Q_\mathrm{load,in}(t) \geq 0$
is the heat bypassing storage.

Second, the hourly heat demand must be fully met by the combination of direct HP
output and TES discharge:
\begin{equation}
    Q_\mathrm{load,in}(t) + Q_\mathrm{tes,out}(t) = Q_\mathrm{dem}(t),
    \label{eq:therm_balance}
\end{equation}
where $Q_\mathrm{tes,out}(t) = Q_\mathrm{ltes,out}(t) + Q_\mathrm{wtes,out}(t)$
is TES discharge and $Q_\mathrm{dem}(t)$ is the BDEW hourly heat demand profile
(Section~\ref{subsec:thermal_demand}).

Together, Eqs.~\eqref{eq:hp_split} and~\eqref{eq:therm_balance} imply that the
HP is the sole thermal source in the system: all heat delivered to the building
originates from HP operation, with the TES acting only as a temporal buffer.
The optimiser can therefore shift HP operation away from expensive peak-price
hours by pre-charging the TES during cheap off-peak periods.





% ============================================================


\section{Techno-Economic Parameters}
\label{sec:econ_params}
% ============================================================

Before presenting the component models it is necessary to establish the economic framework
used to translate one-time capital expenditures into annual costs comparable with the
annual operational expenditures optimised by the MILP.
This section describes the Capital Recovery Factor, the economies-of-scale cost model,
and lists all techno-economic parameters used in the study.

% ------------------------------------------------------------
\subsection{Capital Recovery Factor (CRF)}
\label{sub:crf}
% ------------------------------------------------------------

Investment costs for all technologies are converted to an equivalent \textbf{annualised
CAPEX} using the Capital Recovery Factor (CRF), also known as the annuity factor.
The CRF spreads a one-time investment over the asset's economic lifetime $n$ [yr]
at a fixed discount rate $r$ [-], representing the weighted average cost of capital (WACC):

\begin{equation}
    \mathrm{CRF}(r,\,n) = \frac{r\,(1+r)^{n}}{(1+r)^{n}-1}.
    \label{eq:crf}
\end{equation}

\noindent A single discount rate of $r = 5\,\%$ is applied uniformly to all technologies,
reflecting a typical WACC for commercial building energy investments in Belgium~\cite{VEKA2026}.
Each technology has its own economic lifetime $n$, so a separate CRF value is computed per
technology.
The annualised CAPEX for a technology with unit investment cost $c$ [\euro/unit] and
installed capacity $X$ [unit] is then:

\begin{equation}
    \mathrm{CAPEX}_\mathrm{ann}(X) = c \cdot \mathrm{CRF}(r,n) \cdot X
    \quad [\text{\euro/yr}].
    \label{eq:capex_ann_linear}
\end{equation}

\noindent A key advantage of the CRF approach is that it enables a fair comparison between technologies with different economic lifetimes.
A PV array with a 25-year lifetime and a battery with a 15-year lifetime cannot be compared on raw investment cost alone: the battery must be replaced sooner and its annualised cost per year is therefore higher relative to its upfront price.
By multiplying each technology's specific cost by its own $\mathrm{CRF}(r, n_k)$, all CAPEX terms are expressed in the same unit (\euro/yr) and can be added directly in the objective function without distorting the trade-off between technologies.

\noindent In practice, the specific cost per unit capacity varies with installation size, which is addressed by the linear-affine CAPEX model described next.

% ------------------------------------------------------------
\subsection{Linear-Affine CAPEX Model}
\label{sub:capex_model}
% ------------------------------------------------------------

Rather than applying a generic economies-of-scale power-law, the capital cost of each
technology is modelled as a \textbf{linear-affine} function of installed capacity,
fitted directly to manufacturer and supplier price quotes (the underlying market data and fitted curves for each technology are presented in Section~\ref{sub:tech_params}):

\begin{equation}
    \mathrm{CAPEX}(X) = c_f + c_v \cdot X \quad [\text{\euro}],
    \label{eq:capex_affine}
\end{equation}

\noindent where $c_f$ [\euro] is a fixed one-time cost covering installation labour,
mounting, wiring, and commissioning, and $c_v$ [\euro/unit] is the per-unit hardware cost.
The fixed component implicitly captures the qualitative behaviour of economies of scale:
the specific cost $c_f/X + c_v$ decreases as installed capacity grows, without
requiring a non-linear formulation.

This approach has two practical advantages over the power-law alternative.
First, no piecewise-linear approximation of a concave curve is required, eliminating
an additional source of approximation error and reducing solver complexity.
Second, $c_f$ and $c_v$ are directly identifiable from market price data, making the
model straightforward to update when prices change.

Each one-time cost is annualised by multiplying by the CRF:

\begin{equation}
    \mathrm{CAPEX}_\mathrm{ann}(X)
    = \bigl(c_f + c_v \cdot X\bigr) \cdot \mathrm{CRF}(r,\,n)
    = \underbrace{c_f \cdot \mathrm{CRF}}_{c_f^\mathrm{ann}}
    + \underbrace{c_v \cdot \mathrm{CRF}}_{c_v^\mathrm{ann}} \cdot X
    \quad [\text{\euro/yr}],
    \label{eq:capex_ann_affine}
\end{equation}

\noindent which is linear in $X$ and embeds directly as a linear equality constraint
in the MILP.
The fixed annual cost $c_f^\mathrm{ann}$ is gated by the technology-selection binary
$y \in \{0,1\}$ of each component, so it is only incurred when the technology is
actually installed.

% ------------------------------------------------------------
\subsection{Technology Parameters}
\label{sub:tech_params}
% ------------------------------------------------------------

\autoref{tab:econ_params} lists the one-time CAPEX parameters for all technologies,
fitted from current manufacturer and supplier price data.
Annualised values follow from multiplying by the CRF at $r = 5\,\%$.

\begin{table}[H]
\centering
\caption{One-time CAPEX parameters per technology, fitted from manufacturer and supplier
         market data. $c_f$: fixed cost component; $c_v$: variable per-unit cost.
         CRF computed at $r = 5\,\%$ via \eqref{eq:crf}.
         ``Data range'' is the capacity range covered by the market data used for the fit.}
\label{tab:econ_params}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llccccll}
\hline
\textbf{Technology} & \textbf{Unit}
    & $c_f$ [\euro] & $c_v$ [\euro/unit]
    & \textbf{Lifetime} [yr] & \textbf{CRF}
    & \textbf{Data range} & \textbf{Sources} \\
\hline
HTHP
    & kW$_\mathrm{th}$
    & 9\,516 & 644
    & 20 & 0.0802
    & 15--60\,kW$_\mathrm{th}$
    & \cite{DaikinEWYECZ} \\
PV array
    & kW$_\mathrm{p}$
    & 919 & 532
    & 25 & 0.0710
    & 2.3--14\,kW$_\mathrm{p}$
    & \cite{GreenAkku2024,Solarwinkel2024,SuwiePVQuote2023} \\
BESS
    & kWh
    & 848 & 456
    & 15 & 0.0963
    & 4--34\,kWh
    & \cite{SolarBouwmarkt2024} \\
LTES (PCM)
    & kWh$_\mathrm{th}$
    & 721 & 197
    & 20 & 0.0802
    & 3--50\,kWh$_\mathrm{th}$
    & \cite{FlamcoCapex2024,SunapThermino2024} \\
WTES (water tank)
    & L
    & 921 & 1.165
    & 20 & 0.0802
    & 100--3\,000\,L
    & \cite{VaillantCapex2024,BoschCapex2024,ViessmannCapex2024,RemehaCapex2024} \\
\hline
\end{tabular}%
}
\end{table}

\noindent For PV, the fixed and variable cost components were back-calculated from a real
installation quote issued by Suwie in 2023 (reproduced in
Appendix~\ref{appendix:suwie_quote}).
The quote itemises labour, mounting, inverter placement, and inspection as largely
system-size-independent charges, which map directly to the fixed term $c_f$, while the
panel hardware cost scales with installed peak power and forms the variable term $c_v$.
It should be noted that the 2023 quote reflects hardware prices at a time when PV module
costs were substantially higher than current market levels; module prices have fallen
significantly since then.
The installation cost components (labour, mounting, wiring, inspection), however, remain
largely unchanged and are therefore still representative.
The resulting fitted parameters are listed in \autoref{tab:econ_params}.
Market data scatter plots and fitted curves for the HTHP, PV, and BESS are collected in
Appendix~\ref{appendix:capex_fits}
(Figures~\ref{fig:capex_hthp}--\ref{fig:capex_bess}).
For the HTHP, the fixed-plus-variable structure follows directly from the manufacturer
specific-cost data (Figure~\ref{fig:capex_hthp}).
For the BESS, quotes from BYD, SolarEdge, and Huawei are used
(Figure~\ref{fig:capex_bess}).
For the two TES options, the fits are derived from manufacturer quotes for LTES and WTES
respectively.
The WTES market data is quoted per litre of vessel volume; to express it on the same
thermal-energy basis as the LTES data, each data point is converted using the sensible
heat capacity of water and the system temperature swing:
\begin{equation}
    c_\mathrm{v,th} = \frac{c_\mathrm{v,L}}{\rho_w\, c_{p,w}\, \Delta T_\mathrm{HC} / 3.6 \times 10^6},
    \label{eq:wtes_eur_per_kwh}
\end{equation}
where $\rho_w = 971.8\,\mathrm{kg/m^3}$, $c_{p,w} = 4190\,\mathrm{J/(kg\cdot K)}$, and
$\Delta T_\mathrm{HC} = 20\,\mathrm{K}$ is the supply-to-return temperature difference
of the M-accent heating circuit as specified in the P\&ID.
This yields a conversion factor of approximately $0.0226\,\mathrm{kWh/L}$, so the
variable cost of ${\approx}$\,\euro1.17/L becomes ${\approx}$\,\euro52/kWh$_\mathrm{th}$.
\textbf{This unit conversion is highly sensitive to the assumed temperature swing: halving
$\Delta T_\mathrm{HC}$ to 10\,K would double the specific cost to
${\approx}$\,\euro104/kWh$_\mathrm{th}$, while a wider swing of 40\,K would halve it to
${\approx}$\,\euro26/kWh$_\mathrm{th}$.
The value of 20\,K is specific to the M-accent P\&ID and must be revisited if the
model is applied to a system with a different supply/return temperature regime.}
The resulting specific-cost curves for both TES types are overlaid and compared directly in
Figure~\ref{fig:capex_tes_compare} (Appendix~\ref{appendix:capex_fits}).

% ------------------------------------------------------------
\subsection{Other Economic Parameters}
\label{sub:other_econ}
% ------------------------------------------------------------

\autoref{tab:other_econ} lists the remaining economic parameters that appear in the
objective function but are not technology-specific investment costs.

\begin{table}[H]
\centering
\caption{Other economic parameters used in the objective function.}
\label{tab:other_econ}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccc}
\hline
\textbf{Parameter} & \textbf{Value} & \textbf{Unit} & \textbf{Source} \\
\hline
Discount rate (WACC)        & 5.0       & \%             & \cite{IRENA2023Costs} \\
VAT (applied to all costs)  & 21.0      & \%             & Belgian tax legislation \\
Capacity network tariff     & 60        & \euro/kW/yr    & Appendix~\ref{appendix:tariefkaart} \\
Maximum annual CAPEX budget & 250\,000  & \euro/yr       & Design assumption \\
\hline
\end{tabular}%
}
\end{table}

\noindent The 5\,\% WACC follows the standard assumption used by IRENA for utility-scale
renewable energy projects in OECD/Europe countries~\cite{IRENA2023Costs}.
The role of the maximum annual CAPEX budget is discussed in depth in
Eq.~\eqref{eq:capex_budget}.
The tariff values are derived in Section~\ref{subsec:price_data}; the capacity tariff
is formalised as a MILP constraint in Eq.~\eqref{eq:peak_track}--\eqref{eq:opex_cap}.

% ============================================================
\section{Objective Function}
\label{sec:objective_function}
% ============================================================

The MILP minimises the \textbf{total annualised system cost}, which is the sum of
the annualised capital expenditure (CAPEX) for all installed technologies and the
annual operational expenditure (OPEX) including electricity purchase, feed-in
revenue, network tariffs, and a fixed connection cost.
VAT at 21\,\% is applied to all cost components — both CAPEX and OPEX.

% ------------------------------------------------------------
\subsection*{Annualised Investment Cost (CAPEX)}
% ------------------------------------------------------------

Each technology contributes a term that is linear in its sizing variable.
The TES type-selection binary ($y_\mathrm{ltes}$, $y_\mathrm{wtes}$) forces
exactly one storage technology to be active, so only one of the two TES terms
is non-zero at optimality:
\begin{equation}
    \mathrm{CAPEX}_\mathrm{ann}
    =\; \alpha_\mathrm{PV}\,C_\mathrm{PV}
      + \alpha_\mathrm{bat}\,C_\mathrm{bat}
      + \alpha_\mathrm{HP}\,C_\mathrm{HP}
      + \alpha_\mathrm{ltes}\,C_\mathrm{ltes}
      + \alpha_\mathrm{wtes}\,h_\mathrm{wtes},
    \label{eq:capex_ann}
\end{equation}
where $\alpha_k$ denotes the annualised unit cost of technology $k$ (computed via
the Capital Recovery Factor, Section~\ref{sub:capex_model}), and the sizing
variables $C_\mathrm{PV}$, $C_\mathrm{bat}$, $C_\mathrm{HP}$, $C_\mathrm{ltes}$,
and $h_\mathrm{wtes}$ are the MILP continuous decision variables defined in
Sections~\ref{sec:pv_model}--\ref{sec:bess_model} and~\ref{sec:tes}.
The fixed-cost intercepts ($\alpha_{\cdot,0}$) are absorbed into the
technology-selection binaries as described in Section~\ref{sub:capex_model} and
are omitted from~\eqref{eq:capex_ann} for clarity; their values are listed in
\autoref{tab:tech_params}.

% ------------------------------------------------------------
\subsection*{Annual Operational Cost (OPEX)}
% ------------------------------------------------------------

Three operational cost terms are summed over the full 8\,760-hour horizon
$\mathcal{T} = \{1,\ldots,8760\}$:

\paragraph{Electricity purchase.}
\begin{equation}
    C_\mathrm{buy} = \sum_{t \in \mathcal{T}} \lambda_t^\mathrm{buy}\,P_\mathrm{buy}(t)\,\Delta t,
    \label{eq:opex_buy}
\end{equation}
where $\lambda_t^\mathrm{buy}$ [\euro/kWh] is the hourly all-in purchase price
(Section~\ref{subsec:price_data}) and $P_\mathrm{buy}(t) \geq 0$ [kW] is the
grid import power.

\paragraph{Feed-in revenue.}
Surplus renewable generation can be exported to the grid at a regulated feed-in
tariff $\lambda_t^\mathrm{sell}$:
\begin{equation}
    C_\mathrm{sell} = \sum_{t \in \mathcal{T}} \lambda_t^\mathrm{sell}\,P_\mathrm{sell}(t)\,\Delta t.
    \label{eq:opex_sell}
\end{equation}
This term appears with a negative sign in the objective (it is revenue). The
constraint~\eqref{eq:pv_sellcap} prevents the model from purchasing cheap
grid electricity purely to resell it at a profit.

\paragraph{Capacity (network) tariff.}
Belgian distribution tariffs charge a monthly fee proportional to the highest
15-minute grid-import power recorded in that month~\cite{Fluvius2023}.
This is modelled by tracking a per-month peak variable $\hat{P}_m \geq 0$ [kW]
for each calendar month $m \in \{1,\ldots,12\}$ via the constraints
\begin{equation}
    \hat{P}_m \;\geq\; P_\mathrm{buy}(t), \qquad
    \forall\; t \in \mathcal{T}_m,
    \label{eq:peak_track}
\end{equation}
where $\mathcal{T}_m$ is the set of hours belonging to month $m$.
The capacity tariff cost is then:
\begin{equation}
    C_\mathrm{cap} = c_\mathrm{cap} \sum_{m=1}^{12} \hat{P}_m,
    \label{eq:opex_cap}
\end{equation}
with $c_\mathrm{cap} = 5\;\text{\euro/kW/month}$ (see \autoref{tab:other_econ}).
Incorporating $\hat{P}_m$ into the objective incentivises the BESS and TES to
absorb peak demand spikes, thereby reducing the monthly network charge.

% ------------------------------------------------------------
\subsection*{Complete Objective Function}
% ------------------------------------------------------------

Combining all terms, the MILP minimises:
\begin{equation}
\boxed{
    \min\;\;
    \Bigl(
        \mathrm{CAPEX}_\mathrm{ann}
        + C_\mathrm{buy}
        - C_\mathrm{sell}
        + C_\mathrm{cap}
        + C_\mathrm{fixed}
      \Bigr)\!\cdot\!(1 + \nu_\mathrm{VAT}),
}
    \label{eq:objective}
\end{equation}
where $C_\mathrm{fixed} = 200\;\text{\euro/yr}$ is the fixed annual grid-connection
fee and $\nu_\mathrm{VAT} = 0.21$ is the VAT rate applied to all cost components
(\autoref{tab:other_econ}).

% ------------------------------------------------------------
\subsection*{Investment Budget Constraint}
% ------------------------------------------------------------

To reflect realistic capital budgets, the annualised investment expenditure is
capped by an exogenous parameter $\overline{\mathrm{CAPEX}}_\mathrm{ann}$:
\begin{equation}
    \mathrm{CAPEX}_\mathrm{ann} \;\leq\; \overline{\mathrm{CAPEX}}_\mathrm{ann},
    \label{eq:capex_budget}
\end{equation}
with $\overline{\mathrm{CAPEX}}_\mathrm{ann} = 250\,000\;\text{\euro/yr}$
(\autoref{tab:other_econ}).
This constraint is deliberately loose relative to the expected solution and
acts primarily as a guard against unbounded investment in degenerate scenarios.
Its sensitivity is explored in Section~\ref{subsec:invest_budget}.

% ------------------------------------------------------------
\subsection*{Summary}
% ------------------------------------------------------------

\autoref{tab:obj_summary} collects all objective-function terms together with
their units, the corresponding code expression, and the section where the
underlying variables are defined.

\begin{table}[H]
\centering
\caption{Summary of all terms in the objective function~\eqref{eq:objective}.
         $\Delta t = 1\,\mathrm{h}$; all variables are MILP decision variables
         unless marked as parameters.}
\label{tab:obj_summary}
\begin{tabular}{llll}
\hline
\textbf{Term} & \textbf{Expression} & \textbf{Unit} & \textbf{Defined in} \\
\hline
\multicolumn{4}{l}{\textit{CAPEX (subject to 21\,\% VAT)}} \\
PV investment
    & $\alpha_\mathrm{PV}\,C_\mathrm{PV}$
    & \euro/yr & Sec.~\ref{sec:pv_model} \\
BESS investment
    & $\alpha_\mathrm{bat}\,C_\mathrm{bat}$
    & \euro/yr & Sec.~\ref{sec:bess_model} \\
HP investment
    & $\alpha_\mathrm{HP}\,C_\mathrm{HP}$
    & \euro/yr & Sec.~\ref{sec:hthp_model} \\
LTES investment (if selected)
    & $\alpha_\mathrm{ltes}\,C_\mathrm{ltes}$
    & \euro/yr & Sec.~\ref{sec:tes_model} \\
WTES investment (if selected)
    & $\alpha_\mathrm{wtes}\,h_\mathrm{wtes}$
    & \euro/yr & Sec.~\ref{sec:tes_model} \\
\hline
\multicolumn{4}{l}{\textit{OPEX (subject to 21\,\% VAT)}} \\
Electricity purchase
    & $\sum_t \lambda_t^\mathrm{buy}\,P_\mathrm{buy}(t)\,\Delta t$
    & \euro/yr & Sec.~\ref{subsec:price_data} \\
Feed-in revenue
    & $-\sum_t \lambda_t^\mathrm{sell}\,P_\mathrm{sell}(t)\,\Delta t$
    & \euro/yr & Sec.~\ref{subsec:price_data} \\
Capacity network tariff
    & $c_\mathrm{cap}\sum_m \hat{P}_m$
    & \euro/yr & \autoref{tab:other_econ} \\
Fixed connection cost
    & $C_\mathrm{fixed} = 200$\;\euro/yr
    & \euro/yr & \autoref{tab:other_econ} \\
\hline
\end{tabular}
\end{table}

% ============================================================
\section{Solver Configuration and Solution Strategy}
\label{sec:solver_settings}
% ============================================================

\subsection*{Solver and Interface}

All optimisation runs are executed with \textbf{Gurobi}~\cite{gurobi}, a
state-of-the-art commercial MILP solver, accessed through the native
\texttt{gurobipy} Python API.
Gurobi is widely regarded as the fastest general-purpose MILP solver available
and is used under an academic licence for this research~\cite{gurobi}.
The model is constructed entirely in Python: decision variables, constraints, and
the objective function are assembled programmatically before being handed off to
Gurobi for solution.

\subsection*{Problem Size}

The full-year formulation with an hourly time resolution covers $T = 8\,760$
time steps.
The resulting MILP contains on the order of $\mathcal{O}(10^5)$ decision
variables (continuous and binary) and $\mathcal{O}(10^5)$ linear constraints.
The binary variables arise from the on/off state of the heat pump ($u_\mathrm{hp}(t)$),
the technology-selection decisions ($y_\mathrm{PV}$, $y_\mathrm{bat}$,
$y_\mathrm{HP}$, $y_\mathrm{ltes}$, $y_\mathrm{wtes}$), and the
charge/discharge mutually-exclusive binaries for the BESS.
The large number of binary variables makes the problem computationally intensive,
which motivates the solver settings described below.

\subsection*{MIP Optimality Gap}

Gurobi's branch-and-bound algorithm maintains two bounds throughout the
search: a \textbf{lower bound} obtained by relaxing the integrality
requirements (the LP relaxation), and an \textbf{upper bound} given by the
best integer-feasible solution found so far.
The \textbf{MIP gap} is defined as the relative difference between these two
bounds:
\begin{equation}
    \delta_\mathrm{MIP}
    = \frac{z_\mathrm{UB} - z_\mathrm{LB}}{|z_\mathrm{UB}|},
    \label{eq:mip_gap}
\end{equation}
where $z_\mathrm{UB}$ and $z_\mathrm{LB}$ are the current upper and lower
bound on the objective value.
The solver terminates as soon as $\delta_\mathrm{MIP}$ falls below a
user-specified tolerance \texttt{MIPGap}.
A gap of 0\,\% certifies global optimality; a small positive gap means the
returned solution is within that percentage of the true optimum.
For investment decisions the quality of the solution is not meaningfully
affected by a gap of a few percent, so a non-zero tolerance is set to avoid
the disproportionate computation time required to close the final fraction of
the gap.
The exact tolerance used in each simulation run is reported alongside the
results in Chapter~\ref{chap:results}.

\subsection*{Time Limit}

To keep individual scenario runs tractable, a wall-clock time limit is imposed
via Gurobi's \texttt{TimeLimit} parameter.
If the solver reaches this limit before the MIP gap criterion is met, it
returns the best integer-feasible solution found so far together with the
achieved gap.
The code checks explicitly for this case:
\begin{itemize}
    \item If the time limit is reached \emph{with} at least one feasible
          solution, the run is accepted and the achieved gap is recorded.
    \item If the time limit is reached \emph{without} any feasible solution,
          the run is flagged as infeasible and excluded from analysis.
\end{itemize}
The specific time limit per run is chosen to balance result quality against
the total computation time for the full scenario sweep; the value used for
each simulation is reported in the results.

\subsection*{Hardware Environment}

All computations were performed on a laptop equipped with an
\textbf{Intel Core i7-10750H} processor (6 physical cores, 12 logical
threads at a base clock of 2.60\,GHz) and \textbf{16\,GB} of RAM, running
Windows 11.
Gurobi exploits multi-threading natively: the branch-and-bound tree is
explored in parallel across all available logical threads, which substantially
reduces wall-clock time for the 8\,760-step annual MILP.








% ============================================================
\chapter{Results}
\label{chap:results}
% ============================================================

% ============================================================
\section{Baseline Case: Current Situation}
\label{sec:results_baseline}
% ============================================================

The baseline represents the current situation at the Eelko building: two gas boilers
covering the full thermal demand, the existing 34.63\,kW$_\mathrm{p}$ rooftop PV
installation supplying part of the electrical load, no BESS, and grid electricity
covering the remainder.
Its annual cost is calculated directly from measured energy consumption and current
market prices, providing the reference TCO against which every optimised scenario is
compared.

The existing PV system is treated as a sunk investment: its CAPEX is not included
in the baseline cost, as the capital has already been committed.
Only its operating benefit (reduced grid imports and feed-in revenue) is included
in the baseline OPEX.
The optimisation then asks whether additional investment in BESS, HTHP, and TES
is economically justified on top of the existing PV.

% TODO: Report measured annual gas consumption [MWh/yr] and electricity consumption [kWh/yr].
% TODO: Report measured annual PV production [kWh/yr] from the 34.63 kWp system (2023 data).
% TODO: Compute baseline OPEX = gas cost + net electricity cost (buy minus sell)
%       at 2023/2025 tariffs, including capacity tariff on measured monthly peaks.
% TODO: Report annual CO2 emissions: gas combustion + net grid electricity
%       (using Belgian marginal emission factor).
% TODO: No CAPEX term for existing assets: boilers and PV are already installed
%       and their remaining book value is treated as sunk cost.

% ------------------------------------------------------------
\subsection{Current Prices: Static Baseline}
\label{sub:baseline_static}
% ------------------------------------------------------------

% TODO: Calculate baseline OPEX using today's gas and electricity prices.
%       Gas: spot TTF + distribution tariff + taxes [EUR/MWh_gas].
%       Electricity: EPEX spot + buy markup + capacity tariff + fixed connection cost.
% TODO: This is the simplest benchmark and reflects the actual bill the building
%       owner pays today, with the existing PV already reducing grid imports.

% ------------------------------------------------------------
\subsection{Forward-Looking Baseline: Energy Price Trajectories}
\label{sub:baseline_forward}
% ------------------------------------------------------------

A static baseline computed at today's prices is a useful reference point but is
insufficient as a decision-making tool.
The economic case for electrification over a 20--25 year asset lifetime depends
critically on how gas and electricity prices evolve over that period, and there is
substantial uncertainty on both sides.

Two structural drivers are expected to increase the relative cost of gas and shift
the baseline upward over time.

The first is \textbf{ETS2}, the European Union's second Emissions Trading System,
which extends carbon pricing to building heating fuels and road transport.
ETS2 is scheduled to enter into force in 2028, with a transitional price cap and
a market-stability reserve mechanism.
The European Commission projects ETS2 carbon prices rising from an initial
cap of around \euro{45}/tCO$_2$ at launch to well above \euro{100}/tCO$_2$ by
the mid-2030s under central projections~\cite{EuropeanCommission2021fitfor55},
though the range across scenarios is wide.
For natural gas (emission factor $\approx 0.202$\,tCO$_2$/MWh$_\mathrm{gas}$)
an ETS2 price of \euro{100}/tCO$_2$ adds approximately \euro{20}/MWh to the
gas commodity price, a cost increase of roughly 40--60\,\% relative to recent
average prices.
This directly raises the operating cost of the gas boiler baseline with each
passing year after 2027.

The second driver is \textbf{electricity price uncertainty}.
While high renewable penetration is expected to structurally lower average wholesale
prices in the long run, short-term volatility remains high and the pace of
decarbonisation affects the spread between peak and off-peak prices.
Lower average prices reduce PV export revenue and the value of BESS arbitrage,
while higher volatility can increase both.

% TODO: Define three gas price trajectories to bracket the uncertainty:
%       (i)  Static: today's price held constant (lower bound for gas cost).
%       (ii) Central: ETS2 carbon price per European Commission central projection,
%            added on top of a flat gas commodity price.
%       (iii) High: ETS2 at upper projection + gas commodity at historical peak levels.
% TODO: Define two electricity price trajectories:
%       (i)  Low: price_multiplier declining to 0.7 by 2030 (high renewables penetration).
%       (ii) High: price_multiplier rising to 1.4 by 2030 (delayed capacity build-out).
% TODO: Combine into a 3x2 matrix of forward-looking baseline costs over the
%       investment horizon (2025--2050).
%       Present as a time-series band plot showing the range of baseline gas costs
%       and the uncertainty this creates for the payback period of electrification.
% TODO: Use the central trajectory as the reference for the scenario analysis
%       in Section~\ref{sec:results_scenarios}, and treat the high/low bounds
%       as bounding scenarios.

% ============================================================
\section{Optimal System Configuration}
\label{sec:results_optimal}
% ============================================================

% TODO: Present the MILP optimal solution for the base scenario (2023 prices,
%       no scenario perturbations).
% TODO: Report optimal sizing: C_PV [kWp], C_bat [kWh], Q_hp [kW_th],
%       TES type selected and capacity.
% TODO: Total annualised cost breakdown: CAPEX vs OPEX, per technology.
% TODO: Annual energy flows: PV self-consumption, grid buy/sell, HP heat production,
%       TES charging/discharging.
% TODO: Monthly and hourly dispatch profiles (representative days per season).

% ============================================================
\section{Sensitivity Analysis}
\label{sec:results_sensitivity}
% ============================================================

The base-case solution (Section~\ref{sec:results_optimal}) reveals that at current
market conditions the MILP consistently saturates the PV capacity at the roof
constraint of 50\,kW$_\mathrm{p}$: PV is so profitable under 2023 Belgian electricity
prices, a 25-year lifetime, and a 5\,\% discount rate that the capacity bound is always
binding.
This makes the base case a corner solution rather than an interior optimum, which
limits interpretability.
The sensitivity analysis therefore has two goals: (i) identify which parameters
the optimal sizing and TCO are most responsive to, and (ii) locate the breakeven
thresholds at which each technology transitions from fully deployed to partially
or not installed.

The analysis uses a systematic \textbf{one-at-a-time} (OAT) approach.
Each parameter listed in Table~\ref{tab:sens_params} is swept across a range
while all other parameters remain at their baseline values.
For each parameter value the full MILP is re-solved, and the resulting optimal
$C_\mathrm{PV}$, $C_\mathrm{bat}$, $Q_\mathrm{hp}^*$, TES capacity, and total
annualised cost are recorded.

\begin{table}[H]
\centering
\caption{Parameters varied in the sensitivity analysis, their baseline values,
         and the ranges explored.}
\label{tab:sens_params}
\begin{tabular}{llcll}
\hline
\textbf{Parameter} & \textbf{Description} & \textbf{Baseline} & \textbf{Range} & \textbf{Unit} \\
\hline
\texttt{CAPEX\_PV\_UNIT}      & PV variable CAPEX          & 532   & 200 -- 1\,200 & \euro/kW$_\mathrm{p}$ \\
\texttt{CAPEX\_BAT\_UNIT}     & BESS variable CAPEX        & 456   & 150 -- 900    & \euro/kWh \\
\texttt{CAPEX\_HP\_UNIT}      & HTHP variable CAPEX        & 644   & 300 -- 1\,200 & \euro/kW$_\mathrm{th}$ \\
\texttt{CAPEX\_LTES\_UNIT}    & LTES variable CAPEX        & 197   & 100 -- 500    & \euro/kWh$_\mathrm{th}$ \\
\texttt{WTES\_REF\_COST\_EUR\_L} & WTES unit cost          & 1.165 & 0.5 -- 3.0    & \euro/L \\
\texttt{DISCOUNT\_RATE}       & WACC                       & 5.0   & 2 -- 12       & \% \\
\texttt{MAX\_ANNUAL\_CAPEX}   & Investment budget cap      & 250\,000 & 50\,000 -- 500\,000 & \euro/yr \\
\texttt{C\_CAP}               & Capacity network tariff    & 5.0   & 0 -- 15       & \euro/kW/month \\
\texttt{price\_multiplier}    & Electricity price level    & 1.0   & 0.4 -- 2.0    & -- \\
\texttt{GAS\_DEMAND\_YEAR}    & Thermal demand profile year  & 2021  & 2020, 2021, 2022, 2023 & -- \\
\texttt{SOLAR\_YEAR}       & Solar year   & 2023  & 2020, 2021, 2022, 2023 & -- \\
\texttt{temp\_h}              & HP supply temperature      & 70    & 50 -- 75      & $^\circ$C \\
\texttt{COP\_multiplier}      & HP performance scaling     & 1.0   & 0.7 -- 1.3    & -- \\
\hline
\end{tabular}
\end{table}

% ------------------------------------------------------------
\subsection{Tornado Chart: Parameter Ranking by Impact on TCO}
\label{sub:sens_tornado}
% ------------------------------------------------------------

% TODO: For each parameter in Table~\ref{tab:sens_params}, compute the TCO at the
%       low and high end of its range (with all others at baseline).
%       Plot a tornado chart with parameters ranked by (TCO_high - TCO_low).
%       This identifies which parameters dominate and deserve deeper analysis.
%       Expected dominant parameters: price_multiplier, CAPEX_PV_UNIT, DISCOUNT_RATE, C_CAP.

% ------------------------------------------------------------
\subsection{PV CAPEX and Electricity Price: Breakeven Map}
\label{sub:sens_pv_breakeven}
% ------------------------------------------------------------

The 2D breakeven map is the central result of the sensitivity analysis.
Rather than varying one parameter at a time, it sweeps PV CAPEX and the electricity
price multiplier simultaneously over a grid and records the optimal PV capacity
returned by the MILP at each combination.
The result is partitioned into three qualitative regimes: PV not installed, PV
partially installed, and PV saturated at the 50\,kW$_\mathrm{p}$ roof constraint.
The boundaries between these regimes are the breakeven thresholds.

Figure~\ref{fig:breakeven_dummy} shows an illustrative example of this map generated
with fictive but physically motivated data.
The baseline operating point (532\,\euro/kW$_\mathrm{p}$, price multiplier\,$= 1.0$)
is marked with a star and lies firmly in the saturated region, confirming that at
current conditions the MILP always installs PV at full capacity.
Moving along the x-axis (increasing CAPEX at fixed price) or downward along the y-axis
(falling electricity prices at fixed CAPEX) eventually crosses the saturation threshold,
beyond which partial installation becomes optimal, and then the install threshold,
beyond which PV is not economical at all.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\textwidth]{images/breakeven_map_dummy.png}
    \caption{Illustrative PV breakeven map (fictive data).
             Left: continuous optimal PV capacity [kWp] as a function of PV CAPEX
             and electricity price multiplier. Right: discrete regime classification
             (not installed / partial / saturated at 50\,kWp).
             The star marks the baseline operating point.
             Dashed contour lines are the breakeven boundaries.
             \textbf{Note: values are illustrative only; actual results will be
             produced by sweeping the MILP.}}
    \label{fig:breakeven_dummy}
\end{figure}

% TODO: Replace dummy figure with actual MILP sweep results.
%       Sweep CAPEX_PV_UNIT from 200 to 1200 EUR/kWp (15 points) and
%       price_multiplier from 0.4 to 2.0 (15 points) = 225 MILP runs.
%       Record optimal C_PV at each grid point and regenerate both panels.

% ------------------------------------------------------------
\subsection{Discount Rate}
\label{sub:sens_discount}
% ------------------------------------------------------------

% TODO: Sweep DISCOUNT_RATE from 2\% to 12\% in steps of 1\%.
% TODO: Report optimal C_PV, C_bat, Q_hp* and total annualised cost at each step.
% TODO: Higher discount rate penalises long-lived assets (PV 25 yr) more than
%       short-lived ones (BESS 15 yr) — show how the technology mix shifts.
% TODO: Identify the discount rate at which PV is no longer installed at full capacity.

% ------------------------------------------------------------
\subsection{Capacity Tariff and BESS Sizing}
\label{sub:sens_ccap}
% ------------------------------------------------------------

% TODO: Sweep C_CAP from 0 to 15 EUR/kW/month.
% TODO: The capacity tariff is the primary economic driver for BESS investment:
%       a higher tariff makes peak shaving more valuable, increasing optimal BESS size.
% TODO: Report optimal C_bat and monthly peak reduction at each tariff level.
% TODO: Identify the minimum tariff at which BESS becomes economical.

% ------------------------------------------------------------
\subsection{Heat Pump COP and Supply Temperature}
\label{sub:sens_hp}
% ------------------------------------------------------------

% TODO: Sweep COP_multiplier from 0.7 to 1.3 and temp_h from 55 to 80 degC.
% TODO: Higher supply temperature degrades Lorenz COP, increasing electricity
%       consumption of the HP and its operating cost.
% TODO: Show the impact on annual HP electricity consumption and total TCO.
% TODO: Discuss the implication for system design: lowering supply temperature
%       (e.g. via improved heat distribution) has measurable economic benefit.

% ------------------------------------------------------------
\subsection{Investment Budget Cap}
\label{sub:sens_budget}
% ------------------------------------------------------------

% TODO: Sweep MAX_ANNUAL_CAPEX from 50,000 to 500,000 EUR/yr.
% TODO: Identify the budget level at which each technology enters the solution
%       (ordered by profitability): PV first, then BESS, then HP/TES.
% TODO: This traces the efficient investment frontier as a function of available capital.

% ============================================================
\section{Scenario Analysis}
\label{sec:results_scenarios}
% ============================================================

While the sensitivity analysis varies parameters individually to locate breakeven
thresholds, the scenario analysis combines parameters into coherent narratives
representing plausible future conditions.
Each scenario is defined in Table~\ref{tab:scenarios} and produces a complete
optimal system configuration and TCO that can be compared against the base case.

\begin{table}[H]
\centering
\caption{Scenario definitions. Parameters not listed take their baseline values
         from Table~\ref{tab:sens_params}.}
\label{tab:scenarios}
\begin{tabular}{lp{3.5cm}p{7cm}}
\hline
\textbf{Scenario} & \textbf{Label} & \textbf{Parameter changes} \\
\hline
A & Base case (2025)
  & All parameters at baseline. \\
B & High energy prices
  & \texttt{price\_multiplier} = 1.5.
    Represents sustained high spot prices. \\
C & Low PV cost future
  & \texttt{CAPEX\_PV\_UNIT} = 300\,\euro/kW$_\mathrm{p}$.
    Reflects continued module cost decline to 2030 projections. \\
D & Tight capital
  & \texttt{DISCOUNT\_RATE} = 8\,\%,
    \texttt{MAX\_ANNUAL\_CAPEX} = 100\,000\,\euro/yr.
    Represents a building owner with limited access to low-cost financing. \\
E & Low energy prices
  & \texttt{price\_multiplier} = 0.6.
    Represents a future with high renewable penetration and low wholesale prices. \\
F & Pessimistic
  & \texttt{price\_multiplier} = 0.6,
    \texttt{CAPEX\_PV\_UNIT} = 900\,\euro/kW$_\mathrm{p}$,
    \texttt{DISCOUNT\_RATE} = 8\,\%.
    Tests whether any electrification investment remains justified. \\
G & High price volatility
  & \texttt{PRICE\_YEAR} = 2025 (actual ENTSO-E time series).
    As shown in Figure~\ref{fig:low_price_hours}, 2025 already exhibits
    a sharp increase in near-zero and negative price hours driven by
    high renewable generation, while residual scarcity events still
    produce peak prices above 200\,\euro/MWh.
    This scenario tests how the co-optimisation of storage dispatch and
    PV sizing responds to a price distribution with high spread, rather
    than a uniformly scaled mean. \\
\hline
\end{tabular}
\end{table}

% TODO: For each scenario report: optimal sizing (C_PV, C_bat, Q_hp*, TES),
%       total annualised cost, breakdown CAPEX/OPEX, self-consumption fraction,
%       annual CO2 reduction vs baseline.
% TODO: Present results in a comparative bar chart (one bar per scenario,
%       stacked by cost component) and a sizing comparison table.

% ------------------------------------------------------------
\subsection{Scenario A: Base Case}
\label{sub:scenario_base}
% ------------------------------------------------------------

% TODO: Repeat and summarise the base-case optimal result here for comparison.
%       Cross-reference Section~\ref{sec:results_optimal}.

% ------------------------------------------------------------
\subsection{Scenario B: High Energy Prices}
\label{sub:scenario_highprice}
% ------------------------------------------------------------

% TODO: Does the already-saturated PV remain at 50 kWp?
%       Does BESS sizing increase due to higher arbitrage value?
%       Does the HP become more attractive relative to residual gas use?

% ------------------------------------------------------------
\subsection{Scenario C: Low PV Cost Future}
\label{sub:scenario_lowpv}
% ------------------------------------------------------------

% TODO: PV is already maxed in the base case. Does lower CAPEX free up budget
%       for more BESS or larger TES? Or does the budget cap become non-binding?

% ------------------------------------------------------------
\subsection{Scenario D: Tight Capital}
\label{sub:scenario_capital}
% ------------------------------------------------------------

% TODO: Higher discount rate and lower budget cap — which technology is dropped first?
%       Expected: BESS and TES are dropped before PV since PV has fastest payback.

% ------------------------------------------------------------
\subsection{Scenario E: Low Energy Prices}
\label{sub:scenario_lowprice}
% ------------------------------------------------------------

% TODO: At price_multiplier = 0.6, does PV remain profitable?
%       Cross-reference the breakeven map from Section~\ref{sub:sens_pv_breakeven}.

% ------------------------------------------------------------
\subsection{Scenario F: Pessimistic}
\label{sub:scenario_pessimistic}
% ------------------------------------------------------------

% TODO: Stress test — does any electrification investment survive?
%       If yes, which technology is the last to remain in the solution?

% ============================================================
\section{Comparison with Traditional Rule-of-Thumb Sizing}
\label{sec:results_traditional}
% ============================================================

Engineering practice has historically relied on simple rules of thumb to size
thermal and electrical storage systems, without solving an optimisation problem.
This section applies those conventional methods to the Eelko building and compares
the resulting system against the MILP optimum, in terms of both total annualised
cost and annual energy performance.
The comparison serves two purposes: it validates that the MILP produces a
meaningfully better solution than naive sizing, and it quantifies the economic value
of optimisation itself.

% ------------------------------------------------------------
\subsection{Traditional Sizing Rules}
\label{sub:trad_rules}
% ------------------------------------------------------------

The following industry rules of thumb are applied, each grounded in standard
engineering practice for commercial building HVAC systems.

\paragraph{Heat pump (HTHP).}
In traditional design the heat pump is sized to cover the design-day peak thermal
load without any storage buffer, giving a conservative, 100\,\% backup capacity.
The measured peak thermal demand at the Eelko building is
120.6\,kW$_\mathrm{th}$ (2021 gas meter data), so the traditional HP size is
$Q_\mathrm{hp,trad} = 120.6\,\mathrm{kW_{th}}$.

\paragraph{Thermal Energy Storage (TES).}
A widely used rule of thumb in energy system modelling sizes TES to cover 6--7
hours of peak thermal load, providing a buffer to allow the heat pump to operate
at a stable part-load point and shift peak demand.
This convention is implemented in the oemof.thermal open-source energy modelling
framework via the parameter \texttt{invest\_relation\_input\_capacity = 1/6},
which couples the storage energy capacity to its rated power in a fixed 6:1 ratio
during investment optimisation~\cite{oemofthermal2024}.
Using the 6-hour rule:
\begin{equation}
    C_\mathrm{TES,trad} = 6 \times Q_\mathrm{hp,trad}
    = 6 \times 120.6 \approx 724\,\mathrm{kWh_{th}}.
    \label{eq:tes_rule}
\end{equation}
The 7-hour rule yields 844\,kWh$_\mathrm{th}$.
A more conservative alternative uses the 95th-percentile load
(70.7\,kW$_\mathrm{th}$) rather than the absolute peak, giving
424--495\,kWh$_\mathrm{th}$.

\paragraph{PV array.}
A common rule sizes PV to offset the annual electrical base load, targeting a
self-consumption ratio of approximately 30--40\,\% of annual grid imports.
With the existing 34.63\,kW$_\mathrm{p}$ already installed, a traditional
engineer would typically add no further PV unless a detailed yield study
was commissioned.
Alternatively, PV is sized to fill the available roof area, which gives the
same 50\,kW$_\mathrm{p}$ upper bound used in the MILP.

\paragraph{Battery Energy Storage (BESS).}
A standard rule sizes BESS to store 2--4 hours of average building electrical
load, providing overnight or peak-period autonomy.
The annual mean electrical demand from the Eelko building is approximately
[TODO: insert from meter data]\,kW, giving a traditional BESS size of
[TODO: 2--4\,h $\times$ mean load]\,kWh.

% ------------------------------------------------------------
\subsection{Cost and Performance Comparison}
\label{sub:trad_comparison}
% ------------------------------------------------------------

Table~\ref{tab:trad_vs_milp} compares the traditionally sized system against
the MILP optimum across the key metrics.

\begin{table}[H]
\centering
\caption{Traditional rule-of-thumb sizing vs MILP optimal sizing.
         All costs are annualised at $r = 5\,\%$.
         Values marked [TODO] to be filled after MILP results are available.}
\label{tab:trad_vs_milp}
\begin{tabular}{lcc}
\hline
\textbf{Metric} & \textbf{Traditional sizing} & \textbf{MILP optimum} \\
\hline
HP capacity [kW$_\mathrm{th}$]      & 120.6          & [TODO] \\
TES capacity [kWh$_\mathrm{th}$]    & 724 (6h rule)  & [TODO] \\
PV capacity [kW$_\mathrm{p}$]       & 34.63 (existing) & [TODO] \\
BESS capacity [kWh]                  & [TODO]         & [TODO] \\
Annual CAPEX [\euro/yr]              & [TODO]         & [TODO] \\
Annual OPEX [\euro/yr]               & [TODO]         & [TODO] \\
Total annualised cost [\euro/yr]     & [TODO]         & [TODO] \\
Annual CO$_2$ reduction vs baseline  & [TODO]         & [TODO] \\
PV self-consumption [\%]             & [TODO]         & [TODO] \\
\hline
\textbf{Cost saving vs traditional}  & reference      & [TODO]\,\% \\
\hline
\end{tabular}
\end{table}

% TODO: Run the MILP with the traditional sizing fixed (i.e. fix C_PV, C_bat,
%       Q_hp, C_TES to the rule-of-thumb values and solve only for dispatch).
%       Compare the resulting dispatch cost against the fully optimised solution.
% TODO: Discuss where the MILP diverges most from the rule of thumb:
%       - Is TES over- or under-sized by the 6h rule relative to the MILP optimum?
%         The 6h rule gives 724 kWh_th; the MILP may find a much smaller TES is
%         sufficient because it can time-shift HP operation more precisely.
%       - Does the MILP select a smaller HP because it relies on TES to flatten peaks,
%         whereas the traditional approach sizes the HP for the worst-case hour?
%       - How much of the TCO difference comes from CAPEX (over-sizing) vs OPEX
%         (suboptimal dispatch)?
% TODO: Quantify the value of optimisation: TCO_traditional - TCO_MILP [EUR/yr]
%       and express as a percentage of the traditional TCO.

% ============================================================
\section{Discussion}
\label{sec:results_discussion}
% ============================================================

% TODO: Synthesise findings across scenarios and sensitivity runs.
% TODO: Highlight which parameters the optimal solution is most sensitive to.
% TODO: Compare against literature benchmarks where available.
% TODO: Discuss model limitations (fixed COP, single representative year,
%       no intra-hour dynamics, no grid congestion modelling).

% ============================================================
\chapter{Conclusion}
\label{chap:conclusion}
% ============================================================

% TODO: Summarise the main findings.
% TODO: Answer the research question stated in the introduction.
% TODO: Recommendations for the M-accent building operator.
% TODO: Directions for future work (multi-year rolling horizon, stochastic prices,
%       grid congestion, real-time control).

\bibliographystyle{unsrt}
\bibliography{bibliography.bib}
\addcontentsline{toc}{chapter}{Bibliography}

\appendix

\chapter{Datasheet HTHP Daikin EWYE-CZ}
\label{appendix:datasheet_hthp}


\includepdf{Appendices/DatasheetHTHP.pdf}

\chapter{Vaillant uniSTOR VPS -- Technical Data}
\label{appendix:vps_datasheet}

\noindent Technical data for the Vaillant uniSTOR VPS buffer storage cylinder series
(VPS~300/3 to VPS~2000/3), used to validate the WTES wall U-value in
Section~\ref{par:wtes_uval}.
Key parameters: cylinder tank capacity [L], outer diameter with and without heat
insulation [mm], standby energy consumption [kWh/24\,h] (test condition: tank at
$65\,^{\circ}\mathrm{C}$, ambient $20\,^{\circ}\mathrm{C}$, per EN~12977-3).

\includepdf[pages=-]{Appendices/vps_technical.pdf}

\chapter{Viessmann Vitocell 100-H SVPC -- Technical Data}
\label{appendix:svpc_datasheet}

\noindent Technical data for the Viessmann Vitocell~100-H SVPC buffer storage cylinder series,
used alongside the Vaillant VPS data to validate the WTES wall U-value in
Section~\ref{par:wtes_uval}.
Both standard and efficient insulation variants are included.

\includepdf[pages=-]{Appendices/viesmann_technical.pdf}

\chapter{CAPEX Market Data Fits -- HTHP, PV, BESS, LTES, WTES}
\label{appendix:capex_fits}

\noindent Market-data scatter plots and fitted CAPEX curves for all five technologies
in \autoref{tab:econ_params}.
The fitted coefficients ($c_f$, $c_v$) used in the MILP are shown on each figure.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.7\textwidth]{images/HTHP_capex_plot.png}
    \caption{HTHP specific CAPEX vs.\ rated heating capacity.
             Data points from the Daikin EWYE-CZ catalogue across the available
             capacity range (15--60\,kW$_\mathrm{th}$).
             The fitted curve $\text{CAPEX}/Q_\mathrm{hp} = 9{,}516/Q_\mathrm{hp} + 644$
             [\euro/kW$_\mathrm{th}$] is equivalent to the total cost model
             $c_f + c_v\,Q_\mathrm{hp} = 9{,}516 + 644\,Q_\mathrm{hp}$ [\euro].}
    \label{fig:capex_hthp}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/PV_capex_plot.png}
    \caption{PV array CAPEX market data and linear fit.
             Data from greenakku.de (DE) and solarwinkel.be (BE).
             Left: total CAPEX vs capacity; right: specific CAPEX vs capacity.
             The shaded area indicates the installation-labour component.
             Fit: $c_f = \euro919$, $c_v = \euro532/\text{kW}_\mathrm{p}$
             (hardware \euro264/kW$_\mathrm{p}$ + installation \euro268/kW$_\mathrm{p}$).}
    \label{fig:capex_pv}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/BESS_capex_plot.png}
    \caption{Battery energy storage (BESS) CAPEX market data and linear fit.
             Data from BYD, SolarEdge, and Huawei product catalogues.
             Left: total CAPEX vs capacity; right: specific CAPEX vs capacity.
             Fit: $c_f = \euro848$, $c_v = \euro456/\text{kWh}$.}
    \label{fig:capex_bess}
\end{figure}

\noindent The individual fits for LTES and WTES are overlaid in the combined specific-cost
comparison below.
Both fits are expressed on a common thermal-energy basis
($\Delta T_\mathrm{HC} = 20\,\mathrm{K}$) to allow a direct cost-per-kWh comparison.
At small capacities the LTES fixed-cost component dominates, making the water tank
cheaper per kWh; above roughly 15--20\,kWh$_\mathrm{th}$ the LTES variable cost
asymptotes to its marginal rate, which remains higher than the WTES marginal rate
throughout the range shown.
This cost gap is the reason the solver only selects LTES when a volume constraint
prevents the WTES from meeting the required capacity
(see Section~\ref{sub:ltes_model}).
\label{appendix:capex_tes_comparison}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/TES_combined_capex.png}
    \caption{Specific CAPEX comparison of WTES and LTES on a per-kWh$_\mathrm{th}$ basis
             at $\Delta T_\mathrm{HC} = 20\,\mathrm{K}$.
             Left: full capacity range (0--100\,kWh$_\mathrm{th}$);
             right: zoomed view (0--25\,kWh$_\mathrm{th}$) highlighting the crossover region.
             WTES fit: $c_f = \euro921$, $c_v \approx \euro52/\text{kWh}_\mathrm{th}$;
             LTES fit: $c_f = \euro721$, $c_v = \euro197/\text{kWh}_\mathrm{th}$.}
    \label{fig:capex_tes_compare}
\end{figure}

\chapter{Sunamp Thermino~e -- Technical Datasheet}
\label{appendix:thermino_datasheet}

\noindent Technical datasheet for the Sunamp Thermino~e latent thermal energy storage
unit range (70\,e to 300\,e), used to derive the standby-loss coefficients in
Section~\ref{sub:ltes_model} (Eq.~\eqref{eq:ltes_loss_fit}).
Key parameters: nominal thermal capacity [kWh$_\mathrm{th}$] and 24-hour standby
heat loss [kWh/24\,h].

\includepdf[pages=-]{Appendices/Thermino-E-V3.pdf}

\chapter{Ecopower Tariff Card -- Dynamische burgerstroom (January 2026)}
\label{appendix:tariefkaart}

\includepdf[pages=-]{Appendices/202601_dbs_tariefkaart (5).pdf}

\chapter{PV Installation Quote -- Suwie (2023)}
\label{appendix:suwie_quote}

\includepdf[pages=-]{Appendices/suwie.pdf}


\includepdf[scale = 0.75, pages=1, pagecommand = \chapter{Title appendix B} \label{appendix:B}, offset=0cm -3cm]{Appendices/AppendixB.pdf}




\end{document}
